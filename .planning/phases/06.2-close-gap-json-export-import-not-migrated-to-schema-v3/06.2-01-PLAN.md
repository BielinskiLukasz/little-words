---
phase: 06.2-close-gap-json-export-import-not-migrated-to-schema-v3
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/features/settings/services/dataManagement.ts
  - src/features/settings/services/dataManagement.test.ts
autonomous: true
requirements:
  - DATA-02
  - PREREL-03

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "buildBackupData sets schemaVersion to the BACKUP_SCHEMA_VERSION constant (value 3), never a hardcoded literal (D-01)"
    - "importData's version check rejects any parsed JSON whose schemaVersion !== BACKUP_SCHEMA_VERSION (exact match, not >=) with Error('wrong-schema-version') — and validateBackupData's own internal version check is updated to the same constant so a valid v3 backup is not itself rejected as corrupt by a stale internal literal (D-01, D-02)"
    - "A v2-shaped backup (schemaVersion: 2, wordFormMeanings rows missing firstObservationDate/lastUsedDate/isActive) is rejected via Error('wrong-schema-version') with zero rows written to any table (D-03)"
    - "isValidWordFormMeaning requires wordFormId: number, meaningId: number, firstObservationDate: string, lastUsedDate: string, and isActive: boolean — a v3-shaped backup missing or mistyping any one of these on a wordFormMeanings row is rejected via Error('corrupt') (D-05)"
    - "A valid v3 backup (schemaVersion: 3, populated per-pair fields) round-trips: buildBackupData produces it, importData accepts and fully restores all four entity arrays including wordFormMeanings' three per-pair fields (D-06 test 1)"
    - "An empty-array v3 backup (all four entity arrays empty) validates as true and imports without error — the empty-array edge case (spec-less probe DATA-02/empty)"
    - statement: "If importData is interrupted mid-restore (e.g. bulkAdd throws on a later table), the existing db.transaction('rw', [db.childProfile, db.wordForms, db.meanings, db.wordFormMeanings], ...) wrapping around the clear+bulkAdd sequence (unmodified by this phase) rolls back atomically — no partial replacement of existing data (spec-less probe DATA-02/concurrency)"
      verification: backstop
  artifacts:
    - src/features/settings/services/dataManagement.ts
    - src/features/settings/services/dataManagement.test.ts
  key_links:
    - "buildBackupData -> BACKUP_SCHEMA_VERSION -> exportData's downloaded JSON file (the schemaVersion field a future import reads back)"
    - "importData -> BACKUP_SCHEMA_VERSION version check -> validateBackupData -> isValidWordFormMeaning (three-stage validation gate, all three now v3-aware and using the same constant)"
  prohibitions:
    - requirement_id: DATA-02
      category: safety
      statement: "MUST NOT clear existing IndexedDB tables (childProfile, wordForms, meanings, wordFormMeanings) before the imported file has been fully parsed, version-checked, and structurally validated — for an irreplaceable, un-backed-up child speech-development record, any validation failure discovered after data has already been cleared is unrecoverable data loss"
    - requirement_id: DATA-02
      category: safety
      statement: "MUST NOT accept and partially restore data from a backup whose schemaVersion does not exactly match BACKUP_SCHEMA_VERSION — reject atomically before any table is cleared, never coerce or backfill a mismatched-shape file into passing validation (D-02, D-03)"
---

<objective>
Close milestone v1.0 audit blocker 2: `dataManagement.ts` hardcodes `schemaVersion: 2` in three places (`buildBackupData`, `validateBackupData`'s internal check, and `importData`'s version-check branch) and `isValidWordFormMeaning` never validates the v3 per-pair fields (`firstObservationDate`, `lastUsedDate`, `isActive`) that Phase 6 moved onto `WordFormMeaning`. Today a restored backup silently carries stale/absent per-pair rollup fields, corrupting the exact aggregation this phase's sibling gap-closure (Phase 06.1) just fixed at the write path (DATA-02, PREREL-03).

This is a single-file-pair fix (service + its test) with no UI/API/DB layers to slice through — the whole phase scope is one service module's validation logic. Per tracer-first guidance, this plan's one TDD feature IS the tracer: the thinnest end-to-end change that closes the gap (export sets v3 -> import validates v3 -> round-trip restores correctly), proven by a real vitest run, not a horizontal layer.

Purpose: Restore the invariant this phase's goal states — "Settings JSON export/import correctly reflects the Dexie v3 schema" — so a parent's exported backup is a faithful, restorable snapshot instead of a silently-corrupting one.
Output: `dataManagement.ts` exports a single `BACKUP_SCHEMA_VERSION = 3` constant used consistently by all three version-check sites; `isValidWordFormMeaning` enforces the three v3 per-pair fields; `dataManagement.test.ts` proves round-trip success, v2-shaped rejection, and v3-missing-field rejection.
</objective>

<execution_context>
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-close-gap-json-export-import-not-migrated-to-schema-v3/06.2-CONTEXT.md
@.planning/phases/06.2-close-gap-json-export-import-not-migrated-to-schema-v3/06.2-PATTERNS.md
@src/features/settings/services/dataManagement.ts
@src/features/settings/services/dataManagement.test.ts
@src/db/schema.ts

Key decisions: D-01 (single `BACKUP_SCHEMA_VERSION` constant), D-02 (exact `===` match, not `>=`), D-03 (v2 backups rejected outright, no auto-migration), D-05 (strict per-pair field validation), D-06 (three specific test cases). D-04 (i18n string reword) is handled in the sibling plan `06.2-02-PLAN.md` — not in scope here.
</context>

<feature>
  <name>JSON export/import validated against Dexie schema v3 (BACKUP_SCHEMA_VERSION)</name>
  <files>
    src/features/settings/services/dataManagement.ts,
    src/features/settings/services/dataManagement.test.ts
  </files>
  <read_first>
    - src/features/settings/services/dataManagement.ts — read the entire file (202 lines). `BackupData` interface (lines 4-11, where the new `BACKUP_SCHEMA_VERSION` constant goes per D-01/discretion); `buildBackupData` (lines 14-29, hardcoded `schemaVersion: 2` at line 22); `isValidChildProfile` (lines 31-40) and `isValidMeaning` (lines 48-57) — the exact strict-typing validator style (null check, cast to `Record<string, unknown>`, individual `typeof`/`Array.isArray` checks, all fields required) to mirror; `isValidWordFormMeaning` (lines 60-64, today only checks `wordFormId`/`meaningId` — the validation gap D-05 closes); `validateBackupData` (lines 67-80) — note its OWN internal `if (d.schemaVersion !== 2) return false` at line 70. This third hardcoded `2` is not called out by line number in CONTEXT.md's canonical_refs, but it MUST also be switched to `BACKUP_SCHEMA_VERSION` — if left at `!== 2`, a perfectly valid v3 backup (schemaVersion: 3) would pass `importData`'s version check but then fail `validateBackupData`'s own stale internal check and throw `'corrupt'` instead of restoring, breaking the round-trip (D-06 test 1). `importData` (lines 165-201, version check at lines 174-183, doc comment at lines 161-164).
    - src/features/settings/services/dataManagement.test.ts — read the entire file (265 lines). Every `schemaVersion: 2` literal (lines 20-27, 96-99, 101-104, 106-109, 111-122, 219-234, 236-251, 259-264) needs updating to reflect `BACKUP_SCHEMA_VERSION`/3. Note lines 69-77 and 188-191 already use v3-shaped `wordFormMeanings` fixtures (`firstObservationDate`/`lastUsedDate`/`isActive` already present) — these two do NOT need field-shape changes, only nearby `schemaVersion` literals in their surrounding tests if any. The `vi.mock('@/db/db', ...)` test-db injection at the top (lines 9-17) and the `beforeEach`/`afterEach` `Dexie.delete`/`new AppDB()` reset pattern (lines 208-217) are unchanged — reuse as-is for any new test in the `importData` describe block.
    - src/db/schema.ts — read-only, confirm exact v3 field names/types on `WordFormMeaning` (lines 48-55): `wordFormId: number`, `meaningId: number`, `firstObservationDate: string`, `lastUsedDate: string`, `isActive: boolean`. Do not modify this file — it is the source of truth `isValidWordFormMeaning` must match, not a fix site.
    - src/features/settings/components/DataSection.tsx — read-only, confirm the error-message contract (lines 52-58: `message.includes('wrong-schema-version')` -> version i18n key, else -> corrupt i18n key) is consumed unchanged; this plan does not touch this file.
    - .planning/phases/06.2-close-gap-json-export-import-not-migrated-to-schema-v3/06.2-CONTEXT.md — D-01 through D-06, canonical fix placement and scope boundary.
    - .planning/phases/06.2-close-gap-json-export-import-not-migrated-to-schema-v3/06.2-PATTERNS.md — the exact strict-typing validator pattern excerpts to mirror.
  </read_first>
  <behavior>
    RED phase: update/add all tests below, run the test file, confirm the new/updated assertions FAIL against current code for the stated reason, then commit `test(06.2-01): add failing tests for v3 export/import`.

    In `describe('dataManagement - buildBackupData', ...)`:
    - Update the test at lines 20-27 ("returns schemaVersion === 2") to import `BACKUP_SCHEMA_VERSION` alongside `buildBackupData` and assert `expect(result.schemaVersion).toBe(BACKUP_SCHEMA_VERSION)`; rename the test description to reference the constant by value (3). MUST fail against current code (still returns 2).

    In `describe('dataManagement - validateBackupData', ...)`:
    - Update the test at lines 96-99 (currently uses `schemaVersion: 1`) to use `schemaVersion: 2` instead — a realistic legacy version, still expected to return `false`. Update its description accordingly.
    - Update the `schemaVersion: 2` literals at lines 101-104, 106-109, and 111-122 to `schemaVersion: 3` (these tests target other fields, not the version itself — isolate them by using the now-current valid version).
    - Add a new test: "returns false for a v3 schemaVersion but a wordFormMeanings row missing isActive" — construct a backup object with `schemaVersion: 3`, empty `childProfile`/`wordForms`/`meanings`, and `wordFormMeanings: [{ wordFormId: 1, meaningId: 1, firstObservationDate: '2024-01-01', lastUsedDate: '2024-01-01' }]` (no `isActive` key at all); assert `validateBackupData(...)` returns `false`. MUST fail against current code (today's `isValidWordFormMeaning` only checks `wordFormId`/`meaningId`, so this row currently validates as `true`).

    In `describe('dataManagement - importData', ...)`:
    - Update the backup literals at lines 219-234 and 236-251 from `schemaVersion: 2` to `schemaVersion: 3`.
    - Add a new test: "imports wordFormMeanings records with all three per-pair fields from a valid v3 backup file" (D-06 test 1, round-trip) — build a `BackupData`-shaped object with `schemaVersion: 3` and `wordFormMeanings: [{ id: 1, wordFormId: 1, meaningId: 1, firstObservationDate: '2025-01-01', lastUsedDate: '2025-06-01', isActive: true }]`; call `importData(file)`; assert `testDb.wordFormMeanings.toArray()` has length 1 and the restored row's `firstObservationDate`/`lastUsedDate`/`isActive` equal the input values exactly. MUST fail against current code both because `buildBackupData`/manual literal still says 2 mismatches nothing here directly, but because `validateBackupData`'s stale internal `!== 2` check (once `importData`'s outer check is fixed to accept 3) would otherwise reject this valid v3 payload as corrupt — this test proves the full chain, not just one function.
    - Rewrite the test at lines 259-264 ("throws with wrong-schema-version message when schemaVersion is not 2") to use a realistic v2-shaped fixture per D-06 test 2: `{ schemaVersion: 2, childProfile: [], wordForms: [], meanings: [], wordFormMeanings: [{ id: 1, wordFormId: 1, meaningId: 2 }] }` (v2-shaped — missing the per-pair fields entirely). Assert `await expect(importData(file)).rejects.toThrow('wrong-schema-version')`, AND add a follow-up assertion that no data was written: `expect(await testDb.childProfile.count()).toBe(0)` (reinforces the no-partial-import prohibition). Rename the test description to reference the v2-shaped scenario explicitly.
    - Add a new test: "throws with corrupt message for a v3-shaped backup missing a required per-pair field (isActive)" (D-06 test 3) — build `{ schemaVersion: 3, childProfile: [], wordForms: [], meanings: [], wordFormMeanings: [{ wordFormId: 1, meaningId: 1, firstObservationDate: '2024-01-01', lastUsedDate: '2024-01-01' }] }` (missing `isActive`); assert `await expect(importData(file)).rejects.toThrow('corrupt')`. MUST fail against current code (today's loose `isValidWordFormMeaning` would validate this row as true, so the import would currently succeed instead of throwing).

    Run `npx vitest run C:/my-code/vibe-coding/little-words/src/features/settings/services/dataManagement.test.ts` and confirm: the version-constant test, the round-trip test, and the missing-field rejection test FAIL; the v2-shaped rejection test's throw assertion passes already (version check already rejects any non-matching numeric version today) but its NEW no-partial-import assertion is new coverage; all pre-existing unrelated tests (CSV, non-version validateBackupData checks once literals are updated to 3) continue to pass.
  </behavior>
  <implementation>
    GREEN phase: implement the minimal fix below, re-run the test file, confirm all new/updated assertions now pass, then commit `feat(06.2-01): validate JSON export/import against schema v3`.

    1. Add `export const BACKUP_SCHEMA_VERSION = 3` in `dataManagement.ts` immediately above the `BackupData` interface (D-01, discretion: placement per CONTEXT.md).
    2. In `buildBackupData` (line 22), replace the hardcoded `schemaVersion: 2` with `schemaVersion: BACKUP_SCHEMA_VERSION`.
    3. In `isValidWordFormMeaning` (lines 60-64), extend the returned boolean expression to also require `typeof wfm.firstObservationDate === 'string'`, `typeof wfm.lastUsedDate === 'string'`, and `typeof wfm.isActive === 'boolean'` — same multi-line `&&`-chained style already used in `isValidMeaning` (lines 48-57), D-05.
    4. In `validateBackupData` (line 70), replace `if (d.schemaVersion !== 2) return false` with `if (d.schemaVersion !== BACKUP_SCHEMA_VERSION) return false`. Update the doc comment directly above (line 66) from "schemaVersion=2" to reference `BACKUP_SCHEMA_VERSION` (currently 3).
    5. In `importData`'s version check (lines 174-183), replace `(parsed as Record<string, unknown>).schemaVersion !== 2` with `!== BACKUP_SCHEMA_VERSION`. Update the doc comment above `importData` (lines 161-164) from "schemaVersion !== 2" to "schemaVersion !== BACKUP_SCHEMA_VERSION".
    6. Preserve the existing ordering exactly: version check first (specific error), then `validateBackupData` (generic corrupt error), then the `db.transaction` clear+bulkAdd — no reordering, no new DB calls, no changes to `buildMeaningsCSV`/`exportMeaningsCSV` (out of scope per CONTEXT.md deferred section).

    REFACTOR phase (only if needed): if the four `BACKUP_SCHEMA_VERSION` call sites read awkwardly, no structural refactor is expected — this is a minimal constant-extraction fix. Only commit `refactor(06.2-01): ...` if an actual cleanup was made; otherwise skip this commit.
  </implementation>
  <acceptance_criteria>
    - `dataManagement.ts` contains exactly one declaration `export const BACKUP_SCHEMA_VERSION = 3`
    - `grep -c "BACKUP_SCHEMA_VERSION" src/features/settings/services/dataManagement.ts` is at least 4 (declaration + buildBackupData + validateBackupData + importData, all four sites converted from the literal `2`)
    - `isValidWordFormMeaning` checks `typeof wfm.firstObservationDate === 'string'`, `typeof wfm.lastUsedDate === 'string'`, and `typeof wfm.isActive === 'boolean'` in addition to the existing `wordFormId`/`meaningId` number checks
    - All tests added/updated in `dataManagement.test.ts` per the `<behavior>` block exist and pass
    - All pre-existing tests in `dataManagement.test.ts` (CSV export tests, corrupt-JSON test) continue to pass unchanged
    - `npm run build` (tsc + vite build) exits 0 — no type errors introduced
    - `buildMeaningsCSV`/`exportMeaningsCSV` source lines are byte-for-byte unchanged from the pre-fix version
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run C:/my-code/vibe-coding/little-words/src/features/settings/services/dataManagement.test.ts 2>&1 | tail -25</automated>
    <fails_when>non-zero exit code, or the summary line contains "failed" (e.g. "1 failed" / "X failed")</fails_when>
  </verify>
  <verify>
    <automated>npm --prefix C:/my-code/vibe-coding/little-words run build 2>&1 | tail -10</automated>
    <fails_when>non-zero exit code, or output contains "error TS"</fails_when>
  </verify>
  <verify>
    <automated>grep -c "BACKUP_SCHEMA_VERSION" C:/my-code/vibe-coding/little-words/src/features/settings/services/dataManagement.ts</automated>
    <fails_when>count is less than 4 (constant not consistently used at all three former hardcoded-`2` call sites plus its own declaration)</fails_when>
  </verify>
  <reversibility rating="reversible">Extracting a named constant and tightening a type-guard's field checks is a pure code + test change with no schema migration and no external side effects — fully revertible via git revert with zero data-loss risk (matches D-02/D-03's own reversibility rationale: a future phase can loosen the exact-match or add v2 auto-migration if a real need arises).</reversibility>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parent's file system -> `importData(file: File)` | A user-supplied JSON file is parsed and trusted as the source of truth for fully replacing all four IndexedDB tables — the untrusted-input crossing point for this entire feature |
| Parsed `unknown` JSON -> `isValidChildProfile`/`isValidMeaning`/`isValidWordFormMeaning` type guards | Structural validation gate between untyped JSON and the strongly-typed `BackupData` the app then persists via `bulkAdd` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-01 | Tampering | `isValidWordFormMeaning` (new v3 field checks) | high | mitigate | Strict `typeof` checks (D-05) reject type-confused payloads (e.g. `isActive: "true"` as a string, `firstObservationDate: 12345` as a number) rather than coercing them — mirrors the existing `isValidChildProfile`/`isValidMeaning` strict-typing pattern; any mistyped field throws `Error('corrupt')`, never silently coerced |
| T-06.2-02 | Tampering | `importData` version check + `validateBackupData`'s internal check (schemaVersion mismatch) | high | mitigate | Exact `schemaVersion === BACKUP_SCHEMA_VERSION` match (D-02), not `>=`, applied consistently at all three former call sites — prevents a legacy v2 file (or a crafted file claiming `schemaVersion: 3` without actually being validated end-to-end, since all three sites now agree) from silently passing and corrupting the rollup calculation on restore, the exact silent-corruption class this phase closes |
| T-06.2-03 | Tampering | A crafted JSON payload passing all `typeof` checks but with semantically invalid values (e.g. `isActive: true` but `firstObservationDate: "not-a-date"`) | medium | accept | Out of scope per D-05 (type-level validation only, no semantic/date-format validation) — matches the existing `isValidMeaning`/`isValidChildProfile` precedent (also `typeof`-only); single-user, single-device, no-account app has no cross-user blast radius, and Dashboard/Doctor Report already tolerate malformed date strings gracefully (unchanged by this phase) |
| T-06.2-04 | Denial of Service | Import of an arbitrarily large JSON file selected by the parent | low | accept | Single-device, single-user local file read via native `<input type=file>`; no network transmission, no server-side resource exhaustion; unchanged from pre-existing behavior and not modified by this phase |
</threat_model>

<edge_probe_dispositions>
## Spec-less Probe — Item Dispositions (no SPEC.md exists for this phase)

The deterministic edge probe ran against DATA-02/PREREL-03 requirement text. Every probe item below is either lifted into `must_haves.truths` (in-scope for this phase's export/import validation fix) or recorded here as an explicit assumption (never silently dropped).

| Probe Item | Category | Disposition | Rationale |
|-----------|----------|-------------|-----------|
| DATA-02 adjacency | adjacency | Lifted to `must_haves.truths` (explicit) | Maps directly to D-02: `schemaVersion === BACKUP_SCHEMA_VERSION` exact match, not `>=` — see truth #2 |
| DATA-02 empty | empty | Lifted to `must_haves.truths` (explicit) | Maps to the existing "all four arrays present and empty" `validateBackupData` test (updated to schemaVersion 3) — see truth #6 |
| DATA-02 ordering | ordering | Out of scope (explicit assumption) | No spec or existing code states an ordering guarantee for restored records; `bulkAdd` preserves input array order but this is an implementation detail, not a documented contract, and is unchanged by this phase |
| DATA-02 concurrency | concurrency | Lifted to `must_haves.truths` (backstop) | The existing `db.transaction('rw', ...)` wrapping around clear+bulkAdd (unmodified by this phase) already provides atomic rollback on interruption — see backstop truth #7; not exercised by a new dedicated test in this phase, so authored as a backstop marker rather than an explicit assertion |
| PREREL-03 unclassified | unclassified | Out of scope (explicit assumption) | PREREL-03's broader schema-migration scope (Dexie upgrade, service-layer queries) was already completed in Phase 6; this phase's narrow slice is JSON export/import consistency only, already fully captured by truths #1-6 above |

No-silent-drop check: 5 probe items in, 3 lifted to truths (2 explicit + 1 backstop) + 2 recorded here as explicit out-of-scope assumptions = 5 accounted for.

## Prohibition Recall — Stage 1/2 Summary

Adversarial question applied to DATA-02/PREREL-03's export/import validation surface: "What could this feature silently become that the author would NOT want, but the spec does not forbid?" Stage 1 over-produced ~10 raw candidates (type coercion passing validation, partial v2 import, generic unhelpful errors, clearing data before validating, field-name confusion between `firstUseDate`/`firstObservationDate`, accepting unknown extra fields, PII/console leaks, input mutation, restore-order changes, loose forward-compat version acceptance). Stage 2 dropped routine-engineering items already owned by the edge probe or D-05's type checks, and canon-referral-dropped the PII/console-log candidate (generic privacy hygiene, not bespoke to this feature — breadcrumb to `/gsd-secure-phase`, not minted here). Two bespoke values/safety prohibitions survived and are authored descriptor-less, flagged-unverified, into `must_haves.prohibitions` per the spec-less fallback protocol (no wired `check_*` descriptor — there is no human available to wire one this run).
</edge_probe_dispositions>

<verification>
<verify>
  <automated>npx vitest run C:/my-code/vibe-coding/little-words/src/features/settings/services/dataManagement.test.ts 2>&1 | tail -25</automated>
  <fails_when>non-zero exit code, or the summary line contains "failed"</fails_when>
</verify>
<verify>
  <automated>npm --prefix C:/my-code/vibe-coding/little-words run build 2>&1 | tail -10</automated>
  <fails_when>non-zero exit code, or output contains "error TS"</fails_when>
</verify>
<verify>
  <automated>git log --oneline --grep="^test(06.2-01)" | head -1 && git log --oneline --grep="^feat(06.2-01)" | head -1</automated>
  <fails_when>either grep returns no matching commit line (missing RED or GREEN gate commit)</fails_when>
</verify>
</verification>

<success_criteria>
- `BACKUP_SCHEMA_VERSION = 3` is the single source of truth used by `buildBackupData`, `validateBackupData`, and `importData`'s version check (D-01)
- Import requires exact `schemaVersion === BACKUP_SCHEMA_VERSION` match, not `>=` (D-02)
- A v2-shaped backup is rejected via `'wrong-schema-version'` with zero rows written (D-03)
- `isValidWordFormMeaning` requires all five WordFormMeaning fields including the three v3 per-pair fields (D-05)
- Round-trip, v2-shaped rejection, and v3-missing-field rejection tests all exist and pass (D-06)
- `npm run build` exits 0
- RED (`test(06.2-01)`) and GREEN (`feat(06.2-01)`) gate commits both exist in git log
</success_criteria>

## Artifacts This Phase Produces

No new files — modification of two existing tracked files:

- `BACKUP_SCHEMA_VERSION: number` (new named export, value `3`) in `src/features/settings/services/dataManagement.ts` — the single source of truth for all three version-check call sites.
- `isValidWordFormMeaning(item: unknown): boolean` — signature unchanged; behavior changed to require the three v3 per-pair fields.
- `buildBackupData`, `validateBackupData`, `importData` — signatures unchanged; internal literal `2` replaced with `BACKUP_SCHEMA_VERSION` at all three sites.
- `buildMeaningsCSV`, `exportMeaningsCSV`, `exportData`, `isValidChildProfile`, `isValidWordForm`, `isValidMeaning`, `escapeCSVCell` — unchanged.

<output>
Create `.planning/phases/06.2-close-gap-json-export-import-not-migrated-to-schema-v3/06.2-01-SUMMARY.md` when done, including: RED (what tests were written/updated, which failed and why), GREEN (what implementation change made them pass), REFACTOR (if any), and the list of commits produced.
</output>
