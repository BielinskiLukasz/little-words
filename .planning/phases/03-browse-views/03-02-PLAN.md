---
phase: 03
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/pages/MeaningsPage.tsx
  - src/pages/MeaningDetailPage.tsx
  - src/router/index.tsx
  - src/db/services/meaning.service.ts
autonomous: true
requirements:
  - BROWSE-01
must_haves:
  truths:
    - Meanings list page displays scrollable list of all meanings sorted by creation date (newest first) by default
    - List page shows sort toggle button (Newest first / A–Z) that changes sort order in-component; sort preference resets on navigation
    - When category query param is present (?category=Nouns), Meanings list filters to show only meanings in that category
    - Filter chip appears below the list title showing "Filtering: [Category] ×" with × clearing the filter
    - Tapping any meaning in the list navigates to /meanings/:id detail page
    - Meaning detail page displays the meaning text, categories (as badges), first/last use dates, and linked word forms as read-only
    - Detail page has Active/Inactive toggle (Switch component) that immediately updates the database without confirmation
    - Detail page has lastUseDate date picker (Calendar + Popover) that allows updating the last-use date
    - Detail page has a back button or back navigation that returns to the Meanings list
    - Empty states render correctly: "No meanings yet. Add your first one with the + button." when meanings.length === 0
  artifacts:
    - MeaningsPage.tsx renders with sort toggle, optional filter chip, scrollable list, and empty state
    - MeaningDetailPage.tsx renders with read-only fields, editable Switch and Calendar components, and back navigation
    - src/router/index.tsx updated with route { path: 'meanings/:id', element: <MeaningDetailPage /> }
    - meaning.service.ts contains toggleMeaningActive and updateLastUseDate (already exist; verify they're used)
    - meaning.service.ts contains getMeaningsByCategory for filtered list queries
  key_links:
    - MeaningsPage sort state → local component state (not persisted)
    - MeaningsPage category filter → URL query param (?category=) read via useSearchParams
    - Meaning detail toggle → toggleMeaningActive service function → useLiveQuery reactive update
    - Meaning detail date picker → updateLastUseDate service function → useLiveQuery reactive update
  prohibitions:
    - statement: Do not persist sort preference across page navigation (local state resets per navigation)
      verification: backstop
    - statement: Do not render Meaning text as editable in Phase 3 (read-only per D-06)
      verification: backstop
    - statement: Do not allow category editing from detail page in Phase 3 (Phase 4+ scope per deferred ideas)
      verification: backstop
---

<objective>
A parent can scroll through all meanings in a list, tap to open a detail page, and toggle the Active/Inactive status or update the last-use date. The list supports category filtering via query parameter and sorting by creation date or alphabetically.

Purpose: Expand the Dashboard's read path to a full browse interface for meanings, introduce the Active/Inactive toggle and lastUseDate editor that appear on detail pages across the app, and validate routing to detail pages.

Output: Two new pages (Meanings list and Meaning detail), extended service queries, and router configuration for detail routes.
</objective>

<execution_context>
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-browse-views/03-CONTEXT.md
@.planning/phases/03-browse-views/03-UI-SPEC.md
@.planning/phases/03-browse-views/03-PATTERNS.md
@.planning/phases/03-browse-views/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Implement Meanings list page with sort toggle and category filter</name>
  <files>
    src/pages/MeaningsPage.tsx
    src/db/services/meaning.service.ts
  </files>
  <read_first>
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-PATTERNS.md (MeaningsPage pattern, filter chip pattern, sort state pattern, shared useLiveQuery and Badge patterns)
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-CONTEXT.md (D-13 through D-16 for list ordering, filtering, sorting)
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-UI-SPEC.md (Copywriting Contract section, empty states, sort toggle labels)
    C:/my-code/vibe-coding/little-words/src/pages/DashboardPage.tsx (reference useLiveQuery loading pattern, layout structure)
    C:/my-code/vibe-coding/little-words/src/components/ui/badge.tsx (filter chip component)
    C:/my-code/vibe-coding/little-words/src/components/ui/button.tsx (sort toggle button styling)
  </read_first>
  <action>
Create MeaningsPage.tsx with:

**Imports:**
- useLiveQuery from dexie-react-hooks
- useTranslation from react-i18next
- useSearchParams, useNavigate from react-router
- Badge and Button from shadcn
- db from @/db/db

**Page Structure:**
- Page title "Meanings" with Heading typography
- Sort toggle button below title (default "Newest first") that switches between "Newest first" (by firstUseDate desc) and "A–Z" (by text asc)
- Sort state is local component state via useState; resets to default on each navigation
- Optional filter chip showing "Filtering: [Category] ×" when ?category= query param is active (D-13, D-14)
  - Read category filter via useSearchParams().get('category')
  - × button clears filter by calling setSearchParams({})
- Scrollable list of meanings with each item rendering as a tappable row
  - Row structure: meaning text (Body typography) + optional category badges
  - Tap navigates to `/meanings/${meaning.id}` via useNavigate
- Empty state when meanings.length === 0: render "No meanings yet. Add your first one with the + button." (per UI-SPEC Copywriting Contract)

**Database Query:**
Extend meaning.service.ts with:
1. `getMeaningsByCategory(category: string): Promise<Meaning[]>` — query meanings where categories includes the given category (see PATTERNS section for implementation details)

In the page component, write the useLiveQuery like this:
```typescript
const meanings = useLiveQuery(() => {
  let query = db.meanings.toCollection()
  if (categoryFilter) {
    query = query.and(m => m.categories.includes(categoryFilter))
  }
  return query.toArray()
})
```

Then sort in-memory based on sortOrder state:
```typescript
const sorted = meanings ? [...meanings].sort((a, b) => {
  if (sortOrder === 'newest') {
    return new Date(b.firstUseDate).getTime() - new Date(a.firstUseDate).getTime()
  } else {
    return a.text.localeCompare(b.text)
  }
}) : []
```

**Styling & Accessibility:**
- Use Tailwind utilities (flex, gap, padding per UI-SPEC Spacing)
- List rows responsive and touch-friendly (48px minimum height)
- All text via i18n keys; no hardcoded strings
- Filter chip uses Badge component variant="outline"
  </action>
  <verify>
    <automated>npm test -- --filter=meanings 2>&1 | grep -c "PASS\|passed" && echo "Meanings list tests pass"</automated>
  </verify>
  <done>
MeaningsPage displays a scrollable list of meanings, sort toggle switches between "Newest first" and "A–Z" with in-memory sorting, category filter works via ?category= query param showing/hiding the filter chip, empty state renders when no meanings exist, and tapping any meaning row navigates to the detail page.
  </done>
</task>

<task type="auto">
  <name>Implement Meaning detail page with Active/Inactive toggle and lastUseDate picker</name>
  <files>
    src/pages/MeaningDetailPage.tsx
    src/router/index.tsx
  </files>
  <read_first>
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-PATTERNS.md (MeaningDetailPage pattern, detail page loading pattern, Active/Inactive toggle pattern, date picker pattern, read-only fields pattern)
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-CONTEXT.md (D-05, D-06, D-07 for detail routing and editable fields)
    C:/my-code/vibe-coding/little-words/src/pages/ProfileEditPage.tsx (reference detail page async action pattern with loading state)
    C:/my-code/vibe-coding/little-words/src/components/ui/switch.tsx (Switch component from Plan 01)
    C:/my-code/vibe-coding/little-words/src/components/ui/calendar.tsx (Calendar from Phase 2)
    C:/my-code/vibe-coding/little-words/src/components/ui/popover.tsx (Popover from Phase 2)
    C:/my-code/vibe-coding/little-words/src/db/services/meaning.service.ts (toggleMeaningActive, updateLastUseDate functions)
  </read_first>
  <action>
Create MeaningDetailPage.tsx with:

**Imports:**
- useState, Fragment from react
- useNavigate, useParams from react-router
- useLiveQuery from dexie-react-hooks
- useTranslation from react-i18next
- db from @/db/db
- Switch from @/components/ui/switch
- Calendar and Popover from @/components/ui/
- toggleMeaningActive, updateLastUseDate from @/db/services/meaning.service

**Page Structure:**
- Load meaning by ID from URL param using useParams()
- Display loading state while meaning === undefined
- Once loaded, render:
  1. Meaning text as Heading typography (read-only)
  2. Categories as Badge components (read-only, variant="outline")
  3. First use date as Label typography (read-only)
  4. **Active/Inactive Toggle (D-06, D-07):**
     - Label: "Active" with Switch component (use @/components/ui/switch)
     - Switch checked state bound to meaning.isActive
     - onCheckedChange calls toggleMeaningActive(meaning.id, newState)
     - Set disabled={isSaving} during async operation
     - No confirmation needed; toggle is immediate
  5. **Last use date editor (D-06):**
     - Label: "Last use date"
     - Popover trigger: Button showing the current lastUseDate formatted
     - Popover content: Calendar component in "single" mode
     - onSelect handler calls updateLastUseDate(meaning.id, selectedDate)
     - Set disabled={isSaving} during async operation
  6. **Linked word forms (read-only):**
     - Query: get all WordForm records linked to this meaning via wordFormMeanings junction table
     - Display as list with word form text
     - Empty state if no linked word forms: "No word forms linked yet." (per UI-SPEC)
  7. Back button or back navigation:
     - Button with onClick={() => navigate(-1)} to return to Meanings list
     - Or use back icon from lucide-react

**Async State Handling:**
- useState for isSaving flag
- Wrap async calls (toggleMeaningActive, updateLastUseDate) in try/catch
- Set isSaving during async, clear in finally
- On error, log to console (Phase 3 does not have user-facing error toast yet)

**Styling:**
- Page padding and spacing per UI-SPEC (24px lg margins, 16px md internal spacing)
- Read-only fields: render as `<div>` with Label typography label + Body typography value
- Editable fields: Switch and Calendar components with full Shadcn styling
- All text via i18n keys
  </action>
  <verify>
    <automated>npm test -- --filter=meaning-detail 2>&1 | grep -c "PASS\|passed" && echo "Meaning detail tests pass"</automated>
  </verify>
  <done>
MeaningDetailPage loads and displays a single meaning with text, categories, dates, and linked word forms. Active/Inactive toggle is functional and updates the database. LastUseDate date picker is functional and updates the database. Back navigation works. Empty state for unlinked word forms renders correctly.
  </done>
</task>

<task type="auto">
  <name>Update router to add Meaning detail route</name>
  <files>
    src/router/index.tsx
  </files>
  <read_first>
    C:/my-code/vibe-coding/little-words/src/router/index.tsx (current routes)
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-PATTERNS.md (router pattern section for route addition)
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-CONTEXT.md (D-05 for detail route nesting)
  </read_first>
  <action>
Update src/router/index.tsx to:

1. Add import for MeaningDetailPage at the top:
```typescript
import { MeaningDetailPage } from '../pages/MeaningDetailPage'
```

2. Add the detail route as a child of the root layout (same level as `/meanings`):
```typescript
{ path: 'meanings/:id', element: <MeaningDetailPage /> }
```

The route should be nested inside the RootLayout so that the bottom nav remains visible on the detail page. Verify the route structure matches the existing pattern for other pages (e.g., `/dashboard`, `/meanings`).
  </action>
  <verify>
    <automated>grep -c "meanings/:id" src/router/index.tsx && echo "Route added successfully"</automated>
  </verify>
  <done>
Router is updated with the /meanings/:id route pointing to MeaningDetailPage. Navigating to `/meanings/1` loads the Meaning detail page.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → Page load | meaning.id from URL param is validated by useLiveQuery (returns undefined if not found) |
| User toggle → Service | Active/Inactive toggle calls toggleMeaningActive which uses Dexie transactions for atomicity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | MeaningDetailPage toggle | medium | mitigate | toggleMeaningActive uses Dexie transaction for atomic updates; detail page disables toggle during async operation |
| T-03-04 | Information Disclosure | Linked word forms list | low | accept | Word forms are the parent's own data; no external disclosure |

</threat_model>

<verification>
1. Meanings list renders: navigate to `/#/meanings` and verify scrollable list appears
2. Sort toggle works: click "A–Z" and verify list reorders alphabetically; click "Newest first" and verify back to date order
3. Filter works: navigate to `/#/meanings?category=Nouns` and verify only Nouns are shown; filter chip displays; click × and filter clears
4. Meaning detail loads: tap any meaning and navigate to `/meanings/:id`; verify text, categories, dates display correctly
5. Toggle updates: click Active/Inactive toggle and verify state changes in database (reflected in other open views)
6. Date picker works: click "Last use date" button, select new date, verify update in database
7. Back navigation: click back button or use browser back; return to Meanings list
8. Empty states: delete all meanings (if possible) or create new test state; verify "No meanings yet" renders
9. Router: verify `src/router/index.tsx` has the `/meanings/:id` route with MeaningDetailPage import
</verification>

<success_criteria>
- Meanings list page displays all meanings sorted by creation date by default
- Sort toggle changes list order to alphabetical; sort state resets on navigation
- Category filter query param (?category=) correctly filters meanings and shows/hides filter chip
- Meaning detail page displays all fields: text, categories, dates, linked word forms
- Active/Inactive toggle is functional and updates the database reactively
- LastUseDate date picker is functional and updates the database reactively
- Back navigation returns to Meanings list
- Empty states render correctly for both list and detail pages
- Router is updated with /meanings/:id route
</success_criteria>

<output>
Create `.planning/phases/03-browse-views/03-02-SUMMARY.md` when complete
</output>
