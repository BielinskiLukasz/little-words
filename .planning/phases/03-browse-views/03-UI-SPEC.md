---
phase: 03
slug: browse-views
status: approved
shadcn_initialized: true
preset: "default, neutral base, OKLCH warm theme, CSS variables"
created: 2026-07-28
---

# Phase 3 — UI Design Contract: Browse Views

> Visual and interaction contract for dashboard, list, and detail view pages. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
> 
> **Phase Goal:** A parent can review all entered data through Dashboard, Meanings, Word Forms, Categories, and Timeline views — the full data model is observable and editable via Active/Inactive toggle and lastUseDate picker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui |
| Preset | default (neutral base, OKLCH warm theme, CSS variables enabled) |
| Component library | Radix UI (via shadcn components) |
| Icon library | lucide-react |
| Font | System default sans-serif (Tailwind v4 sans stack) |
| Dark mode | System `prefers-color-scheme` — automatic light/dark (no toggle UI) |

**Notes:**
- All components sourced from shadcn official registry via `npx shadcn add {component}`
- No third-party registries declared for Phase 3
- 4 new components required (see Registry Safety section): `switch`, `alert-dialog`, `chart`, `table`

---

## Spacing Scale

Declared values (all multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon inline spacing, tightly grouped elements |
| sm | 8px | Compact element spacing (gaps between chips, small padding) |
| md | 16px | Default element spacing (list item padding, button padding, card internal padding, form field gaps) |
| lg | 24px | Section padding (page margins, gaps between major sections, card external gaps) |
| xl | 32px | Layout gaps (spacing between cards in grid, detail page section breaks) |
| 2xl | 48px | Major section breaks (distance between major page sections) |
| 3xl | 64px | Page-level spacing (top padding on pages, hero card spacing) |

**Exceptions:** None for Phase 3. All layout uses the 8-point scale exclusively.

**Sensible Defaults Applied (no explicit override needed):**
- List item row height: 16px padding top/bottom (md) = ~48px total with text
- Dashboard card padding: 16px internal (md), 24px gaps between cards (lg)
- Detail page margins: 24px (lg) page padding
- Button internal padding: 8px vertical (sm) × 16px horizontal (md)

---

## Typography

All sizes use `font-family: system-ui, -apple-system, sans-serif` (Tailwind v4 sans default).

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 16px | 400 | 1.5 | Primary text in lists, detail pages, form fields, paragraph copy |
| Label | 14px | 400 | 1.4 | Form labels, secondary text, status badges, category tags, table cells |
| Heading | 20px | 600 | 1.2 | Page titles (Meanings, Word Forms, Categories, Timeline), list section titles, detail page name |
| Display | 28px | 600 | 1.1 | Dashboard hero card (Active Meanings count label), large metrics |

**Implementation in Tailwind:**
- Body: `text-base font-normal leading-[1.5]`
- Label: `text-sm font-normal leading-[1.4]`
- Heading: `text-xl font-semibold leading-[1.2]`
- Display: `text-2xl font-semibold leading-[1.1]`

---

## Color

**60/30/10 Split:**

| Role | Value | Usage |
|------|-------|-------|
| **Dominant (60%)** | OKLCH warm neutral: `oklch(98.5% 0.005 80)` light / `oklch(12% 0.005 80)` dark | Page backgrounds, large surfaces, default component backgrounds |
| **Secondary (30%)** | OKLCH warm neutral: `oklch(94% 0.01 80)` light / `oklch(22% 0.005 80)` dark | Card backgrounds, list item backgrounds, sidebar/secondary surfaces, input fields |
| **Accent (10%)** | Teal primary: `oklch(55% 0.15 185)` light / `oklch(65% 0.15 185)` dark | Interactive focus states, active nav indicators, "Review these?" section accent text, primary CTA buttons, toggle switch "on" state, meaningful icon highlights |
| **Destructive** | Warm red: `oklch(60% 0.22 25)` | Destructive actions only (delete button, confirmation alert text) |

**Accent Reserved For (10% allocation):**
1. **Interactive focus states** — border/ring on focused form inputs, tabs, buttons
2. **Active bottom nav tab** — the current tab indicator (Dashboard/Meanings/Word Forms/More)
3. **"Review these?" section** — the section title or highlight accent
4. **Primary CTA buttons** — "Save", "Link", navigation CTAs (background color)
5. **Toggle switches in "on" state** — the Switch component when toggled to Active
6. **Meaningful icon highlights** — category badge accents, status icons

**NOT reserved for accent:** General body text links (use primary/secondary/muted hierarchy), secondary buttons (use secondary background), table borders (use border token).

**Semantic Colors Already Configured in index.css:**
- `--foreground`: dark text (oklch 20%)
- `--card-foreground`: card text (oklch 20%)
- `--muted-foreground`: secondary text (oklch 55%)
- `--destructive`: warm red for alerts/deletions
- `--border`: light borders (oklch 90%)
- `--ring`: focus ring (teal, same as primary)

---

## Copywriting Contract

All copy must use `useTranslation('common')` from react-i18next. Keys should match this contract exactly for i18n consistency.

| Element | English Copy | Context |
|---------|--------------|---------|
| **Primary CTA (List views)** | "Add meaning" (on Meanings), "Add word form" (on Word Forms) | FAB button or page-level action; opens sheet or form |
| **Primary CTA (Dashboard)** | "Add your first meaning" | When no meanings exist; directs to word entry flow |
| **Empty state heading (Meanings list)** | "No meanings yet" | Rendered when `meanings.length === 0` |
| **Empty state body (Meanings list)** | "No meanings yet. Add your first one with the + button." | Actionable copy pointing to FAB |
| **Empty state heading (Word Forms list)** | "No word forms yet" | Rendered when `wordForms.length === 0` |
| **Empty state body (Word Forms list)** | "No word forms yet. Add your first one with the + button." | Actionable copy pointing to FAB |
| **Empty state heading (Categories)** | "No meanings yet" | Rendered when no meanings exist to categorize |
| **Empty state body (Categories)** | "No meanings yet. Add your first one with the + button." | Consistent messaging across list views |
| **Empty state heading (Timeline)** | "No data yet" | Rendered when timeline query returns empty |
| **Empty state body (Timeline)** | "No data yet. Log meanings to see growth." | Encourages feature adoption; "growth" ties to app value |
| **"Review these?" empty state** | "All meanings used recently — great job!" | Positive, warm tone; celebrates user behavior |
| **Generic error state** | "Something went wrong. Try refreshing." | Displayed when fetch or render fails; encourages page reload action |
| **Detail page empty (linked items)** | "No word forms linked yet." (Meaning detail) / "No meanings linked yet." (Word Form detail) | Shown when a detail page has no related records |
| **Destructive action: Word Form delete** | Button label: "Delete" / Confirmation heading: "Delete word form?" / Confirmation body: "This will remove the word form, but linked meanings will stay. Continue?" | Transaction-safe UX; clarifies side effects |
| **Destructive action: Confirm button** | "Delete" (secondary action) / "Cancel" (primary action in dialog) | Dialog action buttons; "Delete" is destructive variant, "Cancel" closes |
| **Active/Inactive toggle label** | "Active" (shown with Switch component) | Toggle on Meaning detail page; informs state change |
| **"See all" link (Dashboard)** | "See all [N]" | Links from "Review these?" section to full Meanings list |
| **Sort toggle (Meanings/Word Forms)** | "Newest first" (default) / "A–Z" (alphabetical) | Toggle button at top of list; switches `sortOrder` state |
| **Category filter chip (Meanings)** | "Filtering: [Category name] ×" | Badge displayed when category query param is active; × clears filter |
| **Timeline view toggle** | "Growth rate" (bar chart) / "Total vocabulary" (line chart) | Tab toggle above Timeline chart; switches visualization |
| **Timeline date range** | "Last 6 months" / "Last 12 months" / "All time" | Dropdown selector above Timeline; sets aggregation period |

**Tone & Consistency:**
- **Warm, encouraging:** "great job!", "Let's track", "No meanings yet" (not "empty", "zero", "failed")
- **Transparent about actions:** Confirmation dialogs always clarify what happens ("linked meanings will stay")
- **Action-oriented:** Empty states point toward next step ("Add your first one with the + button")
- **Privacy-forward:** No analytics language; no "learn more" links to external docs

---

## UI Considerations

State coverage for Phase 3 UI elements. Probe-driven (ui-consideration-probe, 2026-07-28). Elements: E1 Meanings list, E2 Word Forms list, E3 Categories view, E4 Timeline chart, E5 Dashboard hero card, E6 "Review these?" section, E7 Meaning detail page, E8 Word Form detail page.

| Category | Element(s) | Status | Resolution |
|----------|------------|--------|-----------|
| **empty** | E1 Meanings list | ✅ covered | "No meanings yet. Add your first one with the + button." rendered when `meanings.length === 0` |
| **empty** | E2 Word Forms list | ✅ covered | "No word forms yet. Add your first one with the + button." rendered when `wordForms.length === 0` |
| **empty** | E3 Categories view | ✅ covered | "No meanings yet." when no meanings exist to categorize; uses same empty-state component as list views |
| **empty** | E4 Timeline | ✅ covered | "No data yet. Log meanings to see growth." rendered when `timeline.length === 0` |
| **empty** | E5 Dashboard hero card | — dismissed | 0 active meanings renders as "0"; no special empty state; count is always a valid numeric value |
| **empty** | E6 "Review these?" section | ✅ covered | "All meanings used recently — great job!" when 0 meanings unused for 30+ days |
| **empty** | E7 Meaning detail | ✅ covered | "No word forms linked yet." when no linked word forms on detail page |
| **empty** | E8 Word Form detail | ✅ covered | "No meanings linked yet." when no linked meanings on detail page |
| **populated** | E1 Meanings list | ✅ covered | List renders rows with sort toggle (Newest first / A–Z); rows are tappable and navigate to detail page |
| **populated** | E2 Word Forms list | ✅ covered | Same as E1; rows navigate to Word Form detail page |
| **populated** | E3 Categories view | ✅ covered | Category groups rendered with name and meaning count; tapping group filters Meanings list by category |
| **populated** | E4 Timeline | ✅ covered | Bar chart (Growth rate) and line chart (Total vocabulary) available via tab toggle; date range selector (6m / 12m / all time) |
| **populated** | E5 Dashboard hero card | ✅ covered | Active Meanings count rendered as Display typography (28px, weight-600, leading-1.1) with "Active meanings" label |
| **populated** | E6 "Review these?" section | ✅ covered | 1–3 unused meanings rendered as list; >3 meanings shows "See all [N]" link to Meanings list |
| **overflow** | E1 Meanings list | ✅ covered | Scrollable container; no pagination in v1; sort toggle aids navigation through large lists |
| **overflow** | E2 Word Forms list | ✅ covered | Same as E1 |
| **overflow** | E3 Categories view | ✅ covered | Scrollable list of category groups; no overflow truncation on category names (max ~25 chars) |
| **overflow** | E4 Timeline | ✅ covered | Chart renders within viewport; date range selector limits data window to 6m / 12m / all time |
| **overflow** | E5 Dashboard hero card | — dismissed | Single numeric value; Display typography slot cannot overflow |
| **overflow** | E6 "Review these?" section | ✅ covered | Hard cap at 3 visible items + "See all [N]" link; overflow is structurally impossible |
| **long-text** | E1 Meanings list | ✅ covered | Body text (16px, leading-1.5); truncate at 2–3 lines with ellipsis; full text visible on Meaning detail page |
| **long-text** | E2 Word Forms list | ✅ covered | Same treatment as E1 |
| **long-text** | E3 Categories view | ✅ covered | Label text (14px); category names fit single line (max ~25 chars in English/Polish) |
| **long-text** | E4 Timeline | ✅ covered | Chart axis labels auto-truncated by Recharts; date range labels use Label (14px) scale |
| **long-text** | E7 Meaning detail | ✅ covered | Full text displayed (no truncation on detail page); Body (16px, leading-1.5); Heading (20px) for meaning name |
| **long-text** | E8 Word Form detail | ✅ covered | Same as E7; full word form text always displayed |
| **zero-one-many** | E1 Meanings list | ✅ covered | 0 → empty state; 1+ → list rows; no count display |
| **zero-one-many** | E2 Word Forms list | ✅ covered | Same as E1 |
| **zero-one-many** | E3 Categories view | ✅ covered | 0 categories → empty state; 1 category → single group; many → scrollable list |
| **zero-one-many** | E4 Timeline | ✅ covered | 0 data points → empty state; 1+ → chart renders; view toggle always available |
| **zero-one-many** | E5 Dashboard hero card | — dismissed | Hero card shows count as Display number; no structural difference between 0, 1, or many |
| **zero-one-many** | E6 "Review these?" section | ✅ covered | 0 → positive empty message; 1–3 → list; >3 → "See all [N]" link to full Meanings list |
| **filtered** | E1 Meanings list | ✅ covered | Filter chip ("Filtering: [Category] ×") displayed when `?category=` query param active; × clears filter via `useSearchParams` |
| **loading** | E1–E8 all views | 🧪 backstop | useLiveQuery is synchronous post-startup; app-level splash (Phase 1) covers startup gap; verify in plan phase that no inline spinner or skeleton is needed for individual views |
| **loading** | E4 Timeline aggregation | 🧪 backstop | Assume `getVocabularyGrowthTimeline` Dexie query is synchronous; no explicit loading spinner for chart; verify in plan phase |
| **error** | E1–E3 list fetch fails | ✅ covered | ErrorBoundary renders "Something went wrong. Try refreshing." for any Dexie query failure |
| **error** | E4 Timeline fetch fails | ✅ covered | ErrorBoundary handles failed aggregation query; same generic error message |
| **error** | E5–E6 dashboard queries fail | ✅ covered | ErrorBoundary at page level; dashboard sections degrade gracefully |
| **error** | E7 Meaning detail — record not found | 🧪 backstop | Assume deletion prevents navigation to detail page; if record missing, render "Record not found" error; verify navigation guard in plan phase |
| **error** | E8 Word Form detail — record not found | 🧪 backstop | Same as E7; verify navigation guard in plan phase |
| **partial** | E1–E8 all elements | 🧪 backstop | Dexie useLiveQuery returns complete results (all-or-nothing); no partial loading expected; verify in plan phase that no streaming or incremental fetch occurs |
| **transition** | E7 Meaning detail — Active/Inactive toggle | ✅ covered | Switch component state change is immediate; no confirmation needed; `lastUseDate` update via date picker is a separate, independent action |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate | Date |
|----------|-------------|-------------|------|
| **shadcn official** | `button`, `badge`, `calendar`, `collapsible`, `popover`, `sheet` (installed Phases 1–2) | not required | — |
| **shadcn official** | `switch` (NEW) | not required | 2026-07-28 |
| **shadcn official** | `alert-dialog` (NEW) | not required | 2026-07-28 |
| **shadcn official** | `chart` (NEW — Recharts wrapper) | not required | 2026-07-28 |
| **shadcn official** | `table` (NEW) | not required | 2026-07-28 |

**Installation:**
```bash
npx shadcn@latest add switch
npx shadcn@latest add alert-dialog
npx shadcn@latest add chart
npx shadcn@latest add table
```

All components installed directly into `src/components/ui/` as TypeScript/JSX source (no runtime package dependency). Third-party registries: none declared.

---

## Key Integration Points

**Routes Added (child of RootLayout):**
- `{ path: 'meanings/:id', element: <MeaningDetailPage /> }`
- `{ path: 'word-forms/:id', element: <WordFormDetailPage /> }`

**Query Parameters (on existing pages):**
- `MeaningsPage` reads `?category=CategoryName` for category filtering
- Category filter applied via Badge chip; × clears via `useSearchParams().set()`

**Service Functions (pre-existing, used as-is):**
- `toggleMeaningActive(id: number, isActive: boolean)` — Meaning detail page Switch component
- `deleteWordForm(id: number)` — Word Form detail page delete confirmation
- `searchMeanings(prefix: string)` — autocomplete (Phase 2)
- `findOrCreateWordForm(text: string)` — word entry (Phase 2)

**Service Functions (NEW, to be added):**
- `getMeaningById(id: number): Meaning`
- `updateLastUseDate(id: number, date: Date): Promise<void>`
- `getMeaningsUnusedFor30Days(): Promise<Meaning[]>` — Dashboard Review These
- `getWordFormById(id: number): WordForm`
- `getMeaningsGroupedByCategory(): Promise<{[category: string]: number}>` — Categories page
- `getVocabularyGrowthTimeline(startDate?: Date, endDate?: Date)` — Timeline aggregation

**Zustand Store (UI state, no persistence):**
- `sortOrder: 'newest' | 'alphabetical'` (local component state, resets per navigation)
- `selectedDateRange: '6m' | '12m' | 'all'` (local state on Timeline page)
- `activeTimelineView: 'growth' | 'total'` (local state on Timeline page)
- `selectedCategory: string | null` (derived from query param, not stored in Zustand)

---

## Checker Sign-Off

- [x] Dimension 1 — Copywriting: All empty/error/CTA strings defined per contract (FLAG: date picker copy + "Cancel" label — non-blocking)
- [x] Dimension 2 — Visuals: Component install list complete; 4 new shadcn components specified (FLAG: no explicit focal point section; filter icon a11y label — non-blocking)
- [x] Dimension 3 — Color: 60/30/10 split confirmed; accent usage explicitly listed
- [x] Dimension 4 — Typography: 4 sizes (Body/Label/Heading/Display) with weights and line-heights specified
- [x] Dimension 5 — Spacing: 8-point scale with no exceptions; sensible defaults documented
- [x] Dimension 6 — Registry Safety: shadcn official registry only; 4 new blocks listed; no third-party registries

**Approval:** APPROVED — gsd-ui-checker verified 2026-07-28 (4 PASS, 2 FLAG non-blocking)

---

**Phase Contract Created:** 2026-07-28
**Pre-populated From:**
- REQUIREMENTS.md (DASH-01, DASH-02, DASH-03, BROWSE-01–04)
- CONTEXT.md (Phase 3 decisions D-01 through D-16)
- Design system (Phases 1–2: shadcn, OKLCH theme, lucide icons)
- User input (2026-07-28: spacing, typography, color split, copywriting, components)

**Ready for:** gsd-planner (detail view component planning) and gsd-executor (implementation)
