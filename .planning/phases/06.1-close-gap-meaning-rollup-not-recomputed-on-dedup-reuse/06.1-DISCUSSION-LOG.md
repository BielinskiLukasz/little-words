# Phase 06.1: Close gap: meaning rollup not recomputed on dedup reuse - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-16
**Phase:** 06.1-close-gap-meaning-rollup-not-recomputed-on-dedup-reuse
**Areas discussed:** Fix placement, Idempotent re-link edge case, Test coverage depth, Related dead code (unlinkMeaning)

---

## Fix Placement

| Option | Description | Selected |
|--------|-------------|----------|
| Inside linkMeaningToWordForm | Add the call at the end of linkMeaningToWordForm itself, matching the pattern updatePairFields already uses. Every current and future caller gets correct rollup behavior for free. | ✓ |
| Only in addWordEntry's dedup branch | Minimal, surgical diff — add the call right after the linking loop in wordEntry.service.ts, only when existingMeaningId was used. | |

**User's choice:** Inside linkMeaningToWordForm (recommended option)
**Notes:** Matches the existing pattern used by `updatePairFields` and `deleteWordForm`; closes off the class of bug rather than just this one call site.

---

## Idempotent Re-link Edge Case

| Option | Description | Selected |
|--------|-------------|----------|
| No — skip aggregation on no-op | If the pair already exists, nothing changed on it, so there's nothing to re-aggregate. | ✓ |
| Yes — aggregate defensively on every call | Treat linkMeaningToWordForm as always guaranteeing a fresh rollup, even on the no-op path. | |

**User's choice:** No — skip aggregation on no-op (recommended option)
**Notes:** Keeps the fix strictly scoped to the reported gap.

---

## Test Coverage Depth

| Option | Description | Selected |
|--------|-------------|----------|
| Both unit + integration | Unit test in wordFormMeaning.service.test.ts asserting linkMeaningToWordForm triggers aggregation; integration test in wordEntry.service.test.ts reproducing the exact audit scenario. | ✓ |
| Integration test only | Single test in wordEntry.service.test.ts reproducing the audit's exact reported flow. | |

**User's choice:** Both unit + integration (recommended option)
**Notes:** Covers the fix at its source and proves the reported bug is gone end-to-end.

---

## Related Dead Code (unlinkMeaning)

| Option | Description | Selected |
|--------|-------------|----------|
| Defer — out of scope | Not part of the DASH-01/REPORT-01 gap; audit filed it as tech debt, not a blocker. Leave for a future cleanup. | ✓ |
| Remove it now | Since it's confirmed dead, delete unlinkMeaning() as part of this phase's cleanup. | |

**User's choice:** Defer — out of scope (recommended option)
**Notes:** Keeps this phase's diff scoped to the audited gap.

---

## Claude's Discretion

- Exact wording/placement of the doc comment on `linkMeaningToWordForm` noting it now re-aggregates.

## Deferred Ideas

- `unlinkMeaning()` dead code cleanup/removal — future tech-debt cleanup phase or quick task.
- JSON export/import schema v3 migration — already scoped as separate Phase 06.2.
