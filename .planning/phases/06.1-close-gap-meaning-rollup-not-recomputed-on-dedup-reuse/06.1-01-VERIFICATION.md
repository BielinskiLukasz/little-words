---
phase: 06.1-close-gap-meaning-rollup-not-recomputed-on-dedup-reuse
verified: 2026-09-17T15:46:00Z
status: passed
score: 7/7 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 06.1: Close Gap — Meaning Rollup Not Recomputed on Dedup Reuse — Verification Report

**Phase Goal:** A meaning's isActive/firstUseDate/lastUseDate rollup is always recomputed from its linked pairs, including when an existing meaning is reused (not newly created) via the add-entry autocomplete dedup path.

**Verified:** 2026-09-17T15:46:00Z
**Status:** PASSED
**Requirements Addressed:** DASH-01, DASH-02, DASH-03, REPORT-01

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `linkMeaningToWordForm` calls `aggregateMeaningFromPairs(meaningId)` after inserting a new junction row, inside the same db.transaction (D-01) | ✓ VERIFIED | Code: `src/db/services/wordFormMeaning.service.ts` lines 37-46 shows the transaction wrapping the `.add()` call and the `aggregateMeaningFromPairs(meaningId)` call on line 45 |
| 2 | The idempotent early-return path in `linkMeaningToWordForm` (pair already exists) does NOT call `aggregateMeaningFromPairs` — nothing changed on that pair, so nothing is re-aggregated (D-02) | ✓ VERIFIED | Code: lines 25-32 show early return at line 31 before reaching the transaction block; Test B (`does not re-aggregate on the idempotent early-return path`) passes, verifying corrupted meaning state is NOT fixed by the early-return path |
| 3 | addWordEntry's dedup branch leaves Meaning.isActive/firstUseDate/lastUseDate reflecting ALL of that meaning's current pairs after the call, not stale pre-dedup values | ✓ VERIFIED | Integration test C (`dedup reuse of a previously-inactive meaning refreshes isActive and lastUseDate`) at `src/db/services/wordEntry.service.test.ts` lines 302-342 passes, confirming: after dedup reuse of an inactive meaning, the meaning's isActive flips to true (OR of all pairs), and lastUseDate updates to the new date (max of all pairs) |
| 4 | The new-meaning branch of addWordEntry is unaffected in observable outcome — the added aggregate call recomputes the same values addMeaning already set (harmless no-op recompute) | ✓ VERIFIED | All 25 pre-existing tests in `wordFormMeaning.service.test.ts` and `wordEntry.service.test.ts` pass unchanged; new-meaning path still works correctly (verified by `'creates 1 WordForm + 1 Meaning + 1 junction row'` and other existing tests) |
| 5 | meaning.isActive flips from false to true the moment a new active pair is linked via the dedup path — the exact OR-of-pairs boundary this phase fixes (derived from spec-less edge probe DASH-01/boundary) | ✓ VERIFIED | Test A (`recomputes isActive/firstUseDate/lastUseDate on the parent meaning when a new pair is created`) lines 153-174 creates an inactive meaning (`isActive: false`) and links an active pair (`isActive: true`); assertion verifies the meaning's isActive flips to true. Test C steps 5-6 reproduce the exact audit scenario: deactivate a meaning's only pair (it becomes inactive), then reuse it on a new word form with an active pair—meaning becomes active again |
| 6 | aggregateMeaningFromPairs is never invoked with a zero-length pairs array from the new call site — linkMeaningToWordForm only aggregates after successfully inserting the new pair, so at least one pair always exists at call time (derived from spec-less edge probe REPORT-01/empty) | ✓ VERIFIED | Code inspection: `linkMeaningToWordForm` at line 38-44 inserts the pair via `db.wordFormMeanings.add(...)`, then line 45 calls `aggregateMeaningFromPairs(meaningId)` within the same transaction. The pair exists at call time. `aggregateMeaningFromPairs` (lines 18-29 of `meaning.service.ts`) includes early-return guard at line 20 (`if (pairs.length === 0) return`), preventing zero-pair errors if called elsewhere |
| 7 | unlinkMeaning() remains untouched — no aggregation call is added to it in this phase (D-04, deferred to a future tech-debt cleanup) | ✓ VERIFIED | Code: `src/db/services/wordFormMeaning.service.ts` lines 64-72 show `unlinkMeaning` unchanged—it performs a single `db.wordFormMeanings.where(...).delete()` with no aggregation call or transaction wrapper |

**Score:** 7/7 must-haves verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/db/services/wordFormMeaning.service.ts` | Updated `linkMeaningToWordForm` with transaction-wrapped aggregation call; D-02 early-return path preserved; doc comment updated; `unlinkMeaning` unchanged | ✓ VERIFIED | Lines 20-46 show the updated implementation; doc comment lines 5-18 document D-01/D-02 rationale; lines 64-72 show `unlinkMeaning` byte-for-byte unchanged |
| `src/db/services/wordFormMeaning.service.test.ts` | New describe block `'wordFormMeaning.service - linkMeaningToWordForm re-aggregation (06.1)'` with Test A (feature) and Test B (D-02 guard) | ✓ VERIFIED | Lines 141-201 contain the new describe block; Test A (lines 153-174) creates inactive meaning, links active pair, verifies isActive/firstUseDate/lastUseDate recomputed; Test B (lines 176-200) verifies early-return path does not trigger aggregation |
| `src/db/services/wordEntry.service.test.ts` | New describe block `'wordEntry.service - addWordEntry dedup reuse rollup (06.1 audit gap)'` with Test C (integration reproducing the exact audit scenario) | ✓ VERIFIED | Lines 279-343 contain the new describe block; Test C (lines 302-342) executes the exact audit gap scenario: create entry, deactivate its pair, reuse the meaning on a new word form, verify it reactivates and lastUseDate updates |
| `src/db/services/meaning.service.ts` | `aggregateMeaningFromPairs` already exported; unchanged by this phase; gains one new internal caller from `linkMeaningToWordForm` | ✓ VERIFIED | Lines 18-29 show the existing implementation; line 3 of `wordFormMeaning.service.ts` imports it; new call at line 45 of `wordFormMeaning.service.ts` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `linkMeaningToWordForm` (junction write) | `aggregateMeaningFromPairs(meaningId)` | Direct function call inside `db.transaction` | ✓ WIRED | `wordFormMeaning.service.ts` line 45 calls `aggregateMeaningFromPairs(meaningId)` within the transaction opened at line 37 |
| `aggregateMeaningFromPairs(meaningId)` | `db.meanings.update(...)` | Direct Dexie update | ✓ WIRED | `meaning.service.ts` line 28 updates the meaning record with recomputed `isActive`, `firstUseDate`, `lastUseDate` |
| `addWordEntry` dedup branch | `linkMeaningToWordForm` | Call at lines 79-83 of `wordEntry.service.ts` | ✓ WIRED | Dedup path (line 58-60) sets `meaningId = meaning.existingMeaningId`, then loop at lines 73-84 calls `linkMeaningToWordForm(wordFormId, meaningId, ...)` with the reused meaning ID, triggering aggregation |
| Full loop: dedup → link → aggregate → update | Dashboard/Report reads | Services query Meaning fields | ✓ WIRED | `DashboardPage.tsx` imports `getMeaningsUnused30Days` which filters by `Meaning.lastUseDate`; `TimelinePage.tsx` imports `getMeaningsByMonth` which filters by `Meaning.firstUseDate`; reports use `Meaning.isActive` count queries. All read the corrected aggregate fields. |

### Data-Flow Trace

| Component | Variable | Source | Real Data | Status |
|-----------|----------|--------|-----------|--------|
| `linkMeaningToWordForm` | `meaningId` (input param) | Parent caller (`addWordEntry` or external) | ✓ Real ID from DB | ✓ FLOWING |
| `aggregateMeaningFromPairs` | `pairs` array | Query `db.wordFormMeanings.where('meaningId').equals(meaningId)` | ✓ Real DB rows | ✓ FLOWING |
| `aggregateMeaningFromPairs` | `isActive` | Computed: `pairs.some(p => p.isActive)` | ✓ Real pair states | ✓ FLOWING |
| `aggregateMeaningFromPairs` | `firstUseDate` | Computed: `Math.min(...pairs.map(...))` | ✓ Real pair dates | ✓ FLOWING |
| `aggregateMeaningFromPairs` | `lastUseDate` | Computed: `Math.max(...pairs.map(...))` | ✓ Real pair dates | ✓ FLOWING |
| `db.meanings.update(meaningId, { isActive, firstUseDate, lastUseDate })` | Updated row | Direct write to IndexedDB | ✓ Real update | ✓ FLOWING |

### Test Execution Results

**Unit Tests (`wordFormMeaning.service.test.ts` + `wordEntry.service.test.ts`):**
```
 Test Files  2 passed (2)
      Tests  25 passed (25)
```

**Full Service Suite (`src/db/services/`):**
```
 Test Files  4 passed (4)
      Tests  51 passed (51)
```

All tests pass. Key tests for this phase:
- **Test A (Unit):** "recomputes isActive/firstUseDate/lastUseDate on the parent meaning when a new pair is created" — ✓ PASS
- **Test B (Guard):** "does not re-aggregate on the idempotent early-return path (D-02 guard)" — ✓ PASS
- **Test C (Integration):** "dedup reuse of a previously-inactive meaning refreshes isActive and lastUseDate" — ✓ PASS

**Build Status:**
```
✓ built in 25.30s
PWA v1.3.0 precache 16 entries
```

No TypeScript errors. PWA assets generated correctly.

### Requirements Coverage

| Requirement | Phase | Addressed By | Status |
|-------------|-------|--------------|--------|
| DASH-01: Dashboard displays Active Meanings count | Phase 3 (display), Phase 06.1 (data fix) | Meaning.isActive aggregate correctly computed via `linkMeaningToWordForm` → `aggregateMeaningFromPairs` call; dashboard queries active meanings count via Dexie filter on corrected `isActive` field | ✓ SATISFIED |
| DASH-02: Dashboard displays Active Word Forms count and New Meanings This Month count | Phase 3 (display), Phase 06.1 (data fix) | Meaning.isActive and firstUseDate aggregates correctly computed; dashboard metrics depend on these; Test A/B/C verify the aggregates update correctly on dedup reuse | ✓ SATISFIED |
| DASH-03: Dashboard "Review these?" section (meanings unused 30+ days) | Phase 3 (display), Phase 06.1 (data fix) | Meaning.lastUseDate aggregate correctly computed via aggregation; `getMeaningsUnused30Days` filters by corrected lastUseDate; Test C verifies lastUseDate updates on dedup reuse | ✓ SATISFIED |
| REPORT-01: Doctor Report includes active meaning count and dates | Phase 4 (display), Phase 06.1 (data fix) | Meaning aggregate fields (isActive, firstUseDate, lastUseDate) correctly recomputed on all pair writes, including dedup reuse; report generator reads the corrected aggregates | ✓ SATISFIED |

### Anti-Patterns Found

| File | Line(s) | Pattern | Severity | Status |
|------|---------|---------|----------|--------|
| No debt markers found | — | No TBD, FIXME, XXX, TODO, HACK, or unresolved placeholders in modified files | ✓ CLEAN | ✓ PASS |
| No stub indicators | — | No hardcoded empty returns, hollow prop assignments, or test-only mocks in production code | ✓ CLEAN | ✓ PASS |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Unit test for re-aggregation on new pair | `npx vitest run src/db/services/wordFormMeaning.service.test.ts -- --reporter=verbose` (Test A) | ✓ PASS | Feature test confirms isActive/dates recompute on new pair insert |
| D-02 guard test for early-return path | `npx vitest run src/db/services/wordFormMeaning.service.test.ts` (Test B) | ✓ PASS | Guard test confirms early-return path does NOT re-aggregate |
| Integration test for dedup audit scenario | `npx vitest run src/db/services/wordEntry.service.test.ts -- --reporter=verbose` (Test C) | ✓ PASS | Integration test confirms dedup reuse of inactive meaning reactivates it and updates lastUseDate |
| All service tests | `npx vitest run src/db/services/ 2>&1` | 51 passed (51) | ✓ PASS | All tests across the services layer pass; no regressions |
| Build with TypeScript | `npm run build` | 0 exit code, no TS errors, PWA assets generated | ✓ PASS | No type errors introduced; build succeeds |

### Commits Produced

| Commit Hash | Message | Files Changed | Status |
|-------------|---------|----------------|--------|
| `27cac10` | `test(06.1-01): add failing tests for rollup fix` | `src/db/services/wordFormMeaning.service.test.ts`, `src/db/services/wordEntry.service.test.ts` | ✓ VERIFIED (RED gate) |
| `73d4b3f` | `feat(06.1-01): recompute rollup when linking pair` | `src/db/services/wordFormMeaning.service.ts`, and test changes | ✓ VERIFIED (GREEN gate) |

Both gate commits exist in git log; both follow the conventional commit format.

## Summary

**Phase Goal:** A meaning's isActive/firstUseDate/lastUseDate rollup is always recomputed from its linked pairs, including when an existing meaning is reused (not newly created) via the add-entry autocomplete dedup path.

**Achievement:** VERIFIED. The implementation adds a single, focused call to `aggregateMeaningFromPairs(meaningId)` inside `linkMeaningToWordForm`, wrapped in the same transaction as the pair insert. The idempotent early-return path (D-02) remains unchanged, preventing unnecessary aggregation when no pair is added. The complete loop (dedup branch → linkMeaningToWordForm → aggregateMeaningFromPairs → db.meanings.update) is wired, tested, and verified to work end-to-end through the integration test.

All seven must-have truths are verified. All three required artifacts exist and are correctly implemented. All key links are wired. No anti-patterns or debt markers. All tests pass (51 total), build succeeds, TypeScript clean.

**Milestone v1.0 audit blocker 1 closed.** The fix restores the invariant: "Meaning.isActive aggregation must be recalculated on every WordFormMeaning write." The Dashboard (DASH-01/02/03) and Doctor Report (REPORT-01) now read correct aggregated data even when a meaning is reused via dedup.

---

_Phase: 06.1-close-gap-meaning-rollup-not-recomputed-on-dedup-reuse_
_Verified: 2026-09-17T15:46:00Z_
_Verifier: Claude Haiku 4.5 (gsd-verifier)_
