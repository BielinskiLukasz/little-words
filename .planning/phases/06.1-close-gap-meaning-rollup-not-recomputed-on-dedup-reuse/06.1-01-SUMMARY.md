---
phase: 06.1-close-gap-meaning-rollup-not-recomputed-on-dedup-reuse
plan: 01
subsystem: database
tags: [dexie, indexeddb, transaction, aggregation]

requires:
  - phase: 06-pre-release-polish
    provides: aggregateMeaningFromPairs, updatePairFields transaction pattern
provides:
  - linkMeaningToWordForm re-aggregates parent Meaning on every new-pair insert
affects: [dashboard, doctor-report]

actuals:
  tokens: 1870
  tasks: 1
  commits: 2

tech-stack:
  added: []
  patterns: [transaction-wrapped aggregate recompute on junction write]

key-files:
  created: []
  modified: [src/db/services/wordFormMeaning.service.ts, src/db/services/wordFormMeaning.service.test.ts, src/db/services/wordEntry.service.test.ts]

key-decisions:
  - "D-01: aggregate call lives inside linkMeaningToWordForm itself, wrapped in the same db.transaction as the pair insert"
  - "D-02: idempotent early-return path (pair already exists) stays aggregate-free"

requirements-completed: [DASH-01, DASH-02, DASH-03, REPORT-01]

coverage:
  - id: D1
    description: "linkMeaningToWordForm recomputes isActive/firstUseDate/lastUseDate on new-pair insert"
    requirement: "DASH-01"
    verification:
      - kind: unit
        ref: "src/db/services/wordFormMeaning.service.test.ts#recomputes isActive/firstUseDate/lastUseDate on the parent meaning when a new pair is created"
        status: pass
    human_judgment: false
  - id: D2
    description: "idempotent early-return path does not re-aggregate (D-02 guard)"
    verification:
      - kind: unit
        ref: "src/db/services/wordFormMeaning.service.test.ts#does not re-aggregate on the idempotent early-return path (D-02 guard)"
        status: pass
    human_judgment: false
  - id: D3
    description: "addWordEntry dedup-reuse of a previously-inactive meaning refreshes isActive and lastUseDate end-to-end"
    requirement: "REPORT-01"
    verification:
      - kind: integration
        ref: "src/db/services/wordEntry.service.test.ts#dedup reuse of a previously-inactive meaning refreshes isActive and lastUseDate"
        status: pass
    human_judgment: false

duration: 15min
completed: 2026-09-17
status: complete
---

# Phase 06.1 Plan 01: linkMeaningToWordForm recomputes Meaning rollup via db.transaction on every new-pair insert Summary

## Performance
- **Duration:** ~15 min
- **Started:** 2026-09-17T08:03Z (approx, prior context-gathering sessions)
- **Completed:** 2026-09-17T08:17Z
- **Tasks:** 1
- **Files modified:** 3

## Accomplishments
- Closed milestone v1.0 audit blocker 1: `linkMeaningToWordForm` now re-aggregates the parent `Meaning`'s `isActive`/`firstUseDate`/`lastUseDate` rollup after every new-pair insert, wrapped in the same `db.transaction('rw', [db.wordFormMeanings, db.meanings], ...)` block already used by `updatePairFields`.
- The idempotent early-return path (pair already exists) performs zero aggregation calls, per D-02 — verified by a dedicated guard test.
- Added a unit test (`wordFormMeaning.service.test.ts`) proving the aggregation call fires on new-pair insert, and an integration test (`wordEntry.service.test.ts`) reproducing the exact milestone-audit dedup scenario end-to-end through `addWordEntry`.
- All 51 tests across `src/db/services/` pass; `npm run build` exits 0; `unlinkMeaning` remains byte-for-byte unchanged (D-04).

## Task Commits
1. **RED: add failing tests for rollup fix** - `27cac10` (test)
2. **GREEN: recompute rollup when linking pair** - `73d4b3f` (feat)
3. **REFACTOR** - not needed; the transaction wrapping mirrored `updatePairFields` cleanly with no awkward structure to clean up.

**Plan metadata:** (commit to follow)

## Files Created/Modified
- `src/db/services/wordFormMeaning.service.ts` — `linkMeaningToWordForm` now wraps the new-pair insert in `db.transaction(...)` and calls `aggregateMeaningFromPairs(meaningId)` after the insert; doc comment updated to note D-01/D-02 rationale.
- `src/db/services/wordFormMeaning.service.test.ts` — added `'wordFormMeaning.service - linkMeaningToWordForm re-aggregation (06.1)'` describe block with Test A (feature test) and Test B (D-02 guard test).
- `src/db/services/wordEntry.service.test.ts` — added `'wordEntry.service - addWordEntry dedup reuse rollup (06.1 audit gap)'` describe block with Test C (integration test reproducing the audit scenario).

## Decisions Made
- D-01 and D-02 implemented exactly as specified in the plan and context.
- No deviations from the plan's specified test assertions or implementation approach.

## Deviations from Plan

### Git hygiene correction (not a code deviation)

**1. [Process correction] Fixed accidental inclusion of unrelated pre-staged files in the RED commit**
- **Found during:** Task 1 (RED phase commit)
- **Issue:** When staging and committing the two new test files, `git commit` picked up several files that were already staged in the index from prior/concurrent work (`.gitignore`, `.planning/phases/06-pre-release-polish/.continue-here.md` deletion, `06-UAT.md`, `06-VERIFICATION.md`), producing a commit scoped far beyond this plan's task.
- **Fix:** Ran `git reset --soft HEAD~1` (index-only, no working-tree changes lost) and re-committed using `git commit -m "..." -- <the two test files>` so the commit tree only contains this plan's files, leaving the pre-existing staged changes untouched in the index for their original owner to commit separately.
- **Files affected:** none beyond the intended `wordFormMeaning.service.test.ts` / `wordEntry.service.test.ts`.
- **Verification:** `git show --stat` on the corrected commit shows exactly 2 files changed.
- **Commit:** `27cac10`
- **Note:** During this same window, an unrelated commit `3465918` ("chore(git): ignore local .gsd runtime state directory") appeared in the log between the RED and GREEN commits, touching `.gitignore` and the same three pre-existing staged files. This was not created by this execution — no git hooks are configured in `.git/hooks/`, and no command in this session's history produced that message. It appears to be concurrent activity outside this task's scope (possibly another session/process working in the same repo) and was left untouched since reverting it risks destroying work not owned by this plan.

**Total deviations:** 1 auto-fixed (process/git-hygiene, no code impact). **Impact:** None on the shipped feature — both gate commits (`test(06.1-01)`, `feat(06.1-01)`) are clean and correctly scoped.

## Issues Encountered
None beyond the git-hygiene correction documented above.

## Next Phase Readiness
Milestone v1.0 audit blocker 1 closed. Phase 06.2 (JSON export/import schema v3 gap) is next.

---
*Phase: 06.1-close-gap-meaning-rollup-not-recomputed-on-dedup-reuse*
*Completed: 2026-09-17*

## Self-Check: PASSED
- FOUND: .planning/phases/06.1-close-gap-meaning-rollup-not-recomputed-on-dedup-reuse/06.1-01-SUMMARY.md
- FOUND: commit 27cac10 (test(06.1-01))
- FOUND: commit 73d4b3f (feat(06.1-01))
