---
phase: 06-pre-release-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales/pl/common.json
  - src/i18n/locales/en/common.json
  - src/pages/DashboardPage.tsx
autonomous: true
requirements:
  - PREREL-01

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All Phase 6 i18n keys from the UI-SPEC copywriting contract are present in both pl and en common.json (prerequisite for Plans 3–6)"
    - "Dashboard Active Meanings card navigates to /meanings on tap (D-17)"
    - "Dashboard Active Word Forms card navigates to /word-forms on tap (D-17)"
    - "Dashboard New This Month card navigates to /meanings on tap (D-17)"
    - "Card visual appearance is unchanged; Link wrapper adds focus ring for keyboard accessibility"
  artifacts:
    - src/i18n/locales/pl/common.json
    - src/i18n/locales/en/common.json
    - src/pages/DashboardPage.tsx
  key_links:
    - "useTranslation('common') in Plans 3–6 resolves new keys; locale files must precede those plans"
    - "Link from react-router (hash router) wraps Card without altering Card layout structure"
---

<objective>
Add all new Phase 6 i18n keys to both locale files, then wrap the three Dashboard stat cards in React Router Links (D-17).

Purpose: Unblocks all Wave 2 plans by supplying translation keys upfront. Fixes the Dashboard navigation regression identified in pre-1.0 UAT per PREREL-01.
Output: Both locale files updated; DashboardPage.tsx stat cards navigable.
</objective>

<execution_context>
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-pre-release-polish/06-CONTEXT.md
@.planning/phases/06-pre-release-polish/06-UI-SPEC.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: Add all Phase 6 i18n keys to pl/en common.json</name>
  <read_first>
    - src/i18n/locales/pl/common.json — read entire file; find existing top-level keys to determine where to insert new groups
    - src/i18n/locales/en/common.json — read entire file
  </read_first>
  <files>src/i18n/locales/pl/common.json, src/i18n/locales/en/common.json</files>
  <action>
    Add the following keys to both locale files under the correct nested groups. Use Edit (scoped replacement) on each file — never Write the entire file.

    New keys (add these to the existing JSON objects at the correct nesting level; maintain alphabetical ordering within each group if the file follows that convention):

    Under "common" top-level (or root if the file is flat):
    - `"saveChanges"` — PL: "Zapisz zmiany" / EN: "Save changes"
    - `"discardChanges"` — PL: "Odrzuć zmiany" / EN: "Discard changes"

    Check whether `"edit"` key already exists in both locales (it likely does from Phase 3). If absent, add `"edit"` — PL: "Edytuj" / EN: "Edit".

    Under `"nav"` group (add alongside existing nav.dashboard, nav.meanings etc.):
    - `"pairs"` — PL: "Pary" / EN: "Pairs"

    Under `"sort"` group (create if absent):
    - `"newestFirst"` — PL: "Najnowsze" / EN: "Newest first"
    - `"azWordForm"` — PL: "A–Z (forma)" / EN: "A–Z by word form"
    - `"azMeaning"` — PL: "A–Z (znaczenie)" / EN: "A–Z by meaning"

    Under `"pairs"` group (create if absent):
    - `"emptyHeading"` — PL: "Brak par" / EN: "No pairs yet"
    - `"emptyBody"` — PL: "Dodaj pierwsze słowo, aby zobaczyć pary." / EN: "Add your first word entry to see pairs here."

    Under `"pair"` group (create if absent; singular, different from "pairs"):
    - `"firstObserved"` — PL: "Pierwsze użycie" / EN: "First observed"
    - `"lastUsed"` — PL: "Ostatnie użycie" / EN: "Last used"
    - `"active"` — PL: "Aktywna" / EN: "Active"
    - `"goToWordForm"` — PL: "Przejdź do formy" / EN: "Go to word form"
    - `"goToMeaning"` — PL: "Przejdź do znaczenia" / EN: "Go to meaning"

    Under `"report"` group (add alongside existing report keys):
    - `"byCategory"` — PL: "Znaczenia według kategorii" / EN: "Meanings by category"
    - `"recentAdditions"` — PL: "Ostatnio dodane (5)" / EN: "Recently added (5)"
    - `"recentForgotten"` — PL: "Ostatnio zapomniane (5)" / EN: "Recently forgotten (5)"
    - `"yearsMonths"` — PL: "{{years}} lat {{months}} miesięcy" / EN: "{{years}} years {{months}} months"
    - `"months"` — PL: "{{count}} miesięcy" / EN: "{{count}} months"

    Verify both files are valid JSON after editing (no trailing commas, balanced braces).
  </action>
  <verify>
    <automated>npm --prefix C:/my-code/vibe-coding/little-words run build 2>&1 | tail -5</automated>
    <fails_when>Non-zero exit code, or JSON parse error in i18n load, or "error TS" in output</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/i18n/locales/pl/common.json` is valid JSON containing keys: saveChanges, discardChanges, nav.pairs, sort.newestFirst, sort.azWordForm, sort.azMeaning, pairs.emptyHeading, pairs.emptyBody, pair.firstObserved, pair.lastUsed, pair.active, pair.goToWordForm, pair.goToMeaning, report.byCategory, report.recentAdditions, report.recentForgotten, report.yearsMonths, report.months
    - `src/i18n/locales/en/common.json` contains the same keys with English values
    - `npm run build` exits 0
    - No existing i18n key is removed or renamed
  </acceptance_criteria>
  <done>Both locale files contain all Phase 6 i18n keys; build passes; no existing keys broken.</done>
</task>

<task type="auto">
  <name>Wrap Dashboard stat cards in Link for navigation (D-17)</name>
  <read_first>
    - src/pages/DashboardPage.tsx — read entire file; find Active Meanings card, Active Word Forms card, New This Month card and their current wrapper elements
  </read_first>
  <files>src/pages/DashboardPage.tsx</files>
  <action>
    Import `Link` from `'react-router'` (hash router — same import as used in BottomNav and other navigation components).

    Wrap each of the three stat cards with `<Link>` as follows (per D-17):
    - Active Meanings card → `<Link to="/meanings">`
    - Active Word Forms card → `<Link to="/word-forms">`
    - New This Month card → `<Link to="/meanings">`

    The Link wrapper must:
    - Use `className="block"` so it does not affect the Card's flex/grid layout
    - Add `focus-visible:ring-2 focus-visible:ring-ring rounded-lg` for keyboard focus accessibility (matches existing focus ring convention in the project)
    - NOT add any hover color changes to the card interior (the card visual is unchanged per UI-SPEC)

    Do NOT change the Card component markup, stat values, or labels. Only add the Link wrapper around each existing Card element.
  </action>
  <verify>
    <automated>npm --prefix C:/my-code/vibe-coding/little-words run build 2>&1 | tail -5</automated>
    <fails_when>Non-zero exit code, or "error TS" in output, or Link import missing</fails_when>
  </verify>
  <acceptance_criteria>
    - DashboardPage.tsx imports Link from react-router
    - Each of the three stat cards is wrapped in a Link element with the correct to= path
    - Link wrapper has className including "block" and focus-visible ring classes
    - `npm run build` exits 0 with no TypeScript errors
    - Card content (value, label, icon) is visually unchanged (only the click-target changes)
  </acceptance_criteria>
  <done>Dashboard stat cards navigate to /meanings, /word-forms, /meanings respectively on tap. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React Router Link | Navigation targets are hardcoded strings — no user input in route paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-01 | Tampering | i18n locale files | low | accept | Locale files are static build-time assets; no runtime user write path; JSON parse errors surface as build failures caught by CI |
| T-06-02-02 | Tampering | Link to= paths on Dashboard | low | accept | All route targets are hardcoded string literals in source code, not derived from user input; no injection vector |
</threat_model>

<verification>
1. `npm run build` exits 0 — TypeScript and JSON are valid
2. `grep -c "saveChanges" src/i18n/locales/pl/common.json` → at least 1
3. `grep -c "saveChanges" src/i18n/locales/en/common.json` → at least 1
4. `grep -c "to=\"/meanings\"" src/pages/DashboardPage.tsx` → at least 2 (Active Meanings + New This Month)
5. `grep -c "to=\"/word-forms\"" src/pages/DashboardPage.tsx` → at least 1
</verification>

<success_criteria>
- All Phase 6 i18n keys present in both pl and en locale files
- Dashboard stat cards are tappable links routing to their target screens
- Zero build regressions
</success_criteria>

<output>
Create `.planning/phases/06-pre-release-polish/06-02-SUMMARY.md` when done.
</output>
