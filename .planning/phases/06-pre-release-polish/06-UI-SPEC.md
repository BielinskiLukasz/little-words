---
phase: "6"
slug: "pre-release-polish"
status: draft
shadcn_initialized: true
preset: none
created: "2026-09-02"
---

# Phase 6 — Pre-release Polish: UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (default style, local source copies) |
| Preset | not applicable (no preset code) |
| Component library | Radix UI (via shadcn component copies) |
| Icon library | lucide-react@1.22.0 |
| Font | System UI stack (no custom font; inherits OS default) |

Source: `components.json` (style: "default", iconLibrary: "lucide", cssVariables: true).

---

## Component Inventory

Enumerated by `ls src/components/ui/` — 12 components — shadcn/ui local source copies, Radix UI primitives per package.json (no shadcn runtime package) — 2026-09-02.

| Component | Import path | Notes |
|-----------|-------------|-------|
| AlertDialog | `@/components/ui/alert-dialog` | Destructive-action confirmation dialogs |
| Badge | `@/components/ui/badge` | Active/Inactive status indicator; category labels in Pairs rows |
| Button | `@/components/ui/button` | Edit, Save, Cancel buttons in inline edit mode; nav chips |
| Calendar | `@/components/ui/calendar` | Installed but NOT used in Phase 6 — date fields use `<input type="date">` (D-08) |
| Card | `@/components/ui/card` | Dashboard stat cards (now wrapped in Link — D-17) |
| Chart | `@/components/ui/chart` | Timeline view (unchanged in Phase 6) |
| Collapsible | `@/components/ui/collapsible` | Pairs row expand/collapse (D-14); per-pair row expand on detail pages (D-06, D-07) |
| Popover | `@/components/ui/popover` | Not used in Phase 6 |
| Sheet | `@/components/ui/sheet` | AddEntrySheet (unchanged layout; date field updated per D-04) |
| Sonner | `@/components/ui/sonner` | Toast feedback on save, copy, and SW update |
| Switch | `@/components/ui/switch` | Per-pair `isActive` toggle in expanded Collapsible rows (D-06, D-07, D-14) |
| Table | `@/components/ui/table` | Timeline view data table (unchanged in Phase 6) |

This table is non-exhaustive. Executor may use any component the design system exports; check before assuming a component is absent.

---

## Spacing Scale

Standard 8-point scale in use across the project. No Phase 6 exceptions beyond the existing 44px touch-target minimum on interactive chips.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gap inside chip; inline badge padding |
| sm | 8px | Compact element spacing; gap between pair chips in a row |
| md | 16px | Default section padding; input padding; card body padding |
| lg | 24px | Section padding on detail pages; Pairs list row vertical padding |
| xl | 32px | Layout gap between page header and content body |
| 2xl | 48px | Major section breaks (e.g. between report sections) |
| 3xl | 64px | Page-level bottom padding (above bottom nav) |

Exceptions:
- Touch targets for tappable chips (word form chip, meaning chip, nav arrow) must be minimum 44px height to meet mobile touch standards.
- Bottom nav area: existing 64px safe area from Phase 3 — unchanged.

---

## Typography

Three sizes declared. Two primary weights declared. Source: established patterns in `src/pages/MeaningDetailPage.tsx` and `src/shared/components/BottomNav.tsx`.

| Role | Size | Weight | Line Height | Tailwind class |
|------|------|--------|-------------|----------------|
| Display | 24px | 700 (bold) | 1.25 | `text-2xl font-bold` |
| Body | 16px | 400 (regular) | 1.5 | `text-base` |
| Label | 14px | 600 (semibold) | 1.5 | `text-sm font-medium` |

Notes:
- Display: page headings (h1) only — Meanings detail, Word Forms detail, Pairs page, Doctor Report page.
- Body: main content text — meaning text in read mode, word form text in read mode, report body.
- Label: form field labels, input placeholder text, chip text, sort selector options, category badge text, metadata dates, isActive toggle label, Edit/Save/Cancel button labels, nav tab labels.
- `text-muted-foreground` applies to secondary/supporting label text; it does not change font size or weight.

---

## Color

Tokens declared in `src/index.css` using OKLCH color space. Both light and dark modes are defined; Phase 6 introduces no new color tokens.

| Role | CSS Variable | Light Value | Usage |
|------|-------------|-------------|-------|
| Dominant (60%) | `--background` | oklch(98.5% 0.005 80) | Page background; behind all content |
| Secondary (30%) | `--card` / `--secondary` | oklch(100% 0 0) / oklch(94% 0.01 80) | Cards (Dashboard stat cards, Pairs rows, detail page panels); sheet background |
| Accent / Primary (10%) | `--primary` | oklch(55% 0.15 185) | Reserved elements only (see below) |
| Destructive | `--destructive` | oklch(60% 0.22 25) | Destructive actions only (delete confirmation) |

Accent (`--primary`, teal) reserved for:
- Save button (primary variant) in inline edit mode
- Bottom nav active tab indicator
- Link/navigation chips on Pairs rows (chip border or text highlight)
- Focus ring (`--ring`) on interactive inputs and buttons
- Dashboard stat card hover state (Link wrapper)

Accent must NOT be applied to: read-only text, inactive badges, metadata dates, sort selector idle state, or the Edit/Cancel buttons (use outline or ghost variant instead).

Dark mode: all tokens have dark equivalents in the existing `@media (prefers-color-scheme: dark)` block. Phase 6 must not hard-code light-mode values.

---

## Copywriting Contract

All strings must be added to `src/i18n/locales/pl/common.json` (Polish, default) and `src/i18n/locales/en/common.json` (English). Retrieved via `useTranslation('common')`.

| Element | Polish copy | English copy | i18n key |
|---------|-------------|--------------|----------|
| Primary CTA — commit inline edit | Zapisz | Save | `common.save` (likely exists) |
| Secondary — enter edit mode | Edytuj | Edit | `common.edit` (likely exists) |
| Secondary — exit without saving | Anuluj | Cancel | `common.cancel` (likely exists) |
| Pairs page title | Pary | Pairs | `common.nav.pairs` |
| Pairs sort — option 1 (default) | Najnowsze | Newest first | `common.sort.newestFirst` |
| Pairs sort — option 2 | A–Z (forma) | A–Z by word form | `common.sort.azWordForm` |
| Pairs sort — option 3 | A–Z (znaczenie) | A–Z by meaning | `common.sort.azMeaning` |
| Pairs empty state heading | Brak par | No pairs yet | `common.pairs.emptyHeading` |
| Pairs empty state body | Dodaj pierwsze słowo, aby zobaczyć pary. | Add your first word entry to see pairs here. | `common.pairs.emptyBody` |
| Per-pair row: first observed label | Pierwsze użycie | First observed | `common.pair.firstObserved` |
| Per-pair row: last used label | Ostatnie użycie | Last used | `common.pair.lastUsed` |
| Per-pair row: active label | Aktywna | Active | `common.pair.active` (or reuse `common.active`) |
| Per-pair nav arrow aria-label (word form) | Przejdź do formy | Go to word form | `common.pair.goToWordForm` |
| Per-pair nav arrow aria-label (meaning) | Przejdź do znaczenia | Go to meaning | `common.pair.goToMeaning` |
| Report: age format (≥12 months) | X lat Y miesięcy | X years Y months | Constructed from `common.report.yearsMonths` |
| Report: age format (<12 months) | X miesięcy | X months | Constructed from `common.report.months` |
| Report section: per-category list heading | Znaczenia według kategorii | Meanings by category | `common.report.byCategory` |
| Report section: recent additions heading | Ostatnio dodane (5) | Recently added (5) | `common.report.recentAdditions` |
| Report section: inactive heading | Ostatnio zapomniane (5) | Recently forgotten (5) | `common.report.recentForgotten` |
| Error state — generic | Coś poszło nie tak. Spróbuj odświeżyć aplikację. | Something went wrong. Try refreshing the app. | `common.error.generic` (likely exists) |

Destructive actions in Phase 6: no new destructive actions are added. Existing delete confirmation patterns (AlertDialog with current copy) are unchanged.

---

## UI Considerations

Applicable state considerations resolved for Phase 6 screens and interactions.

| Category | Element | Status | Resolution |
|----------|---------|--------|------------|
| empty | Pairs list (`PairsPage`) | ✅ covered | Render heading "No pairs yet" + body "Add your first word entry to see pairs here." per Copywriting Contract above |
| loading | Pairs list (`useLiveQuery` in `PairsPage`) | ✅ covered | `useLiveQuery` returns `undefined` while loading; render `<p className="text-muted-foreground">{t('app.loading')}</p>` — same pattern as all existing list pages |
| loading | Edit mode save (`MeaningDetailPage`, `WordFormDetailPage`) | ✅ covered | Disable Save button and show spinner during async Dexie write; re-enable on completion in `finally` block — reuse existing `finally` reset pattern |
| long-text | Word form chip and meaning chip in Pairs row | 🧪 backstop | Chips must truncate with `truncate` and a `max-w-[...px]` cap so two chips always fit on one row on 320px viewport; exact max-width TBD by executor based on measured layout |
| long-text | Meaning text input in inline edit mode | 🧪 backstop | Textarea or multiline input must not overflow the card boundary; executor to verify at 320px minimum width with 60+ character meaning text |
| overflow | Pairs list with 100+ pairs | ⚠ unresolved | No virtualization planned for v1; planner assumes native scroll is sufficient — add note if UAT shows jank |
| zero-one-many | Category chips in edit mode — 0 categories selected | ✅ covered | CategoryChips renders an empty selection state showing all chips unselected; this is valid and saveable — "no category" is equivalent to "Other" per existing spec |
| zero-one-many | Per-pair rows on Meaning detail — meaning with exactly 1 linked pair | ✅ covered | Single-row display is the base case; no edge behavior needed |
| zero-one-many | Per-pair rows on Word Form detail — word form with 0 linked pairs | ⚠ unresolved | Word forms with no linked pairs should not exist (schema constraint), but if they appear after a v3 migration edge case, render "No meanings linked" muted text |
| empty | Pairs screen — sort selector with 0 results after filter | ✅ covered | Sort selector does not filter; 0-result state only occurs on a fresh app (covered by empty state above) |
| state — interaction | Inline date input (per-pair firstObservationDate / lastUsedDate) | ✅ covered | Blur-triggered save — same pattern as `handleNotesBlur` in `DoctorReportPage.tsx`; no save on every keystroke |
| state — interaction | Dashboard stat cards now navigable | ✅ covered | Wrap existing Card in `<Link to="/meanings">` etc.; existing visual unchanged; add focus ring via Tailwind `focus-visible:ring-2 focus-visible:ring-ring` to satisfy keyboard accessibility |
| state — migration | Dexie v3 schema upgrade on first load after deploy | ✅ covered | Upgrade runs inside Dexie's built-in `onblocked`/`onversionchange` flow; no app-level loading UI needed; Dexie handles the transaction |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | alert-dialog, badge, button, calendar, card, chart, collapsible, popover, sheet, sonner, switch, table | not required — all components are local source copies from the official shadcn registry |

No third-party registries declared for Phase 6. Registry vetting gate not applicable.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS
- [ ] Dimension 7 Inventory Provenance: PASS

**Approval:** pending
