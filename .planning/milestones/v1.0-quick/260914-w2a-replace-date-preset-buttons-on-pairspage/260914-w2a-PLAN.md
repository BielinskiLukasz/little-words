---
phase: quick-260914-w2a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/PairsPage.tsx
  - src/i18n/locales/en/common.json
  - src/i18n/locales/pl/common.json
autonomous: true
requirements: []

must_haves:
  truths:
    - Filter bar shows two date-range rows (First observed, Last used), each with a from and to date input
    - Preset pill buttons (Last 7 days / Last 30 days / Learned after) are gone
    - All four date inputs are optional; an empty input means the range is open-ended
    - A pair passes the date filter if its firstObservationDate >= firstObservedFrom (when set) AND <= firstObservedTo (when set), and lastUsedDate >= lastUsedFrom (when set) AND <= lastUsedTo (when set)
    - Text search and status toggle remain unchanged
    - i18n keys pairs.last7Days, pairs.last30Days, pairs.learnedAfter are removed; new keys pairs.firstObservedFrom, pairs.firstObservedTo, pairs.lastUsedFrom, pairs.lastUsedTo are present in both en and pl locale files
  artifacts:
    - src/pages/PairsPage.tsx
    - src/i18n/locales/en/common.json
    - src/i18n/locales/pl/common.json
  key_links:
    - PairsPage filter logic reads firstObservationDate and lastUsedDate fields from the pair objects returned by getPairsWithDetails
---

<objective>
Replace the date preset pill buttons on PairsPage with four open-ended date range inputs covering firstObserved and lastUsed ranges.

Purpose: Give parents precise date range control rather than fixed relative presets, supporting consultation prep where specific observation windows matter.
Output: Updated PairsPage.tsx with new filter state and UI; updated EN and PL translation files.
</objective>

<execution_context>
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/little-words/.planning/ROADMAP.md
@C:/my-code/vibe-coding/little-words/src/pages/PairsPage.tsx
@C:/my-code/vibe-coding/little-words/src/i18n/locales/en/common.json
@C:/my-code/vibe-coding/little-words/src/i18n/locales/pl/common.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update i18n locale files — remove preset keys, add date-range keys</name>
  <files>src/i18n/locales/en/common.json, src/i18n/locales/pl/common.json</files>
  <action>
In both locale files, under the "pairs" object:

REMOVE these three keys:
- "last7Days"
- "last30Days"
- "learnedAfter"

ADD these four keys:
- "firstObservedFrom": label text for the "First observed" date-range row (before the from-input)
- "firstObservedTo": short label for the "–" separator / to-input (e.g. "to")
- "lastUsedFrom": label text for the "Last used" date-range row (before the from-input)
- "lastUsedTo": short label for the to-input (e.g. "to")

English values:
  "firstObservedFrom": "First observed"
  "firstObservedTo": "to"
  "lastUsedFrom": "Last used"
  "lastUsedTo": "to"

Polish values:
  "firstObservedFrom": "Pierwsze użycie"
  "firstObservedTo": "do"
  "lastUsedFrom": "Ostatnie użycie"
  "lastUsedTo": "do"

Do not alter any other keys in either file.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>Both locale files have the three old preset keys absent and the four new date-range keys present with correct EN/PL values.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor PairsPage date filter — replace presets with four date-range inputs</name>
  <files>src/pages/PairsPage.tsx</files>
  <action>
STATE — remove these two useState declarations:
  const [datePreset, setDatePreset] = useState&lt;'all' | 'last7' | 'last30' | 'custom'&gt;('all')
  const [customAfterDate, setCustomAfterDate] = useState('')

STATE — add these four:
  const [firstObservedFrom, setFirstObservedFrom] = useState('')
  const [firstObservedTo, setFirstObservedTo] = useState('')
  const [lastUsedFrom, setLastUsedFrom] = useState('')
  const [lastUsedTo, setLastUsedTo] = useState('')

FILTER LOGIC — remove the cutoff calculation block (const msPerDay ... const cutoff ...) and the existing date check line in the filter predicate (if cutoff !== null ...).

Replace with this logic inside the filter predicate, after the status check:
  if (firstObservedFrom && p.firstObservationDate < firstObservedFrom) return false
  if (firstObservedTo && p.firstObservationDate > firstObservedTo) return false
  if (lastUsedFrom && p.lastUsedDate < lastUsedFrom) return false
  if (lastUsedTo && p.lastUsedDate > lastUsedTo) return false

The date fields on pair objects are ISO date strings (YYYY-MM-DD), so lexicographic string comparison is correct.

isAnyFilterActive — update to replace datePreset and customAfterDate references:
  const isAnyFilterActive = searchText !== '' || statusFilter !== 'all' || firstObservedFrom !== '' || firstObservedTo !== '' || lastUsedFrom !== '' || lastUsedTo !== ''

clearFilters — update to reset the four new state vars instead of the two old ones:
  setFirstObservedFrom('')
  setFirstObservedTo('')
  setLastUsedFrom('')
  setLastUsedTo('')

UI — in the filter bar, remove the entire "Date presets" div (the one containing the Last 7 days button, Last 30 days button, learnedAfter span, and the single date input). Replace it with two labeled rows placed after the status toggle group, still within the same flex-wrap container div:

Row 1 — first observed range:
  A flex items-center gap-1 div containing:
  - span with t('pairs.firstObservedFrom') and class "text-sm text-muted-foreground"
  - date input bound to firstObservedFrom / setFirstObservedFrom
  - span with t('pairs.firstObservedTo') and class "text-sm text-muted-foreground"
  - date input bound to firstObservedTo / setFirstObservedTo

Row 2 — last used range:
  A flex items-center gap-1 div containing:
  - span with t('pairs.lastUsedFrom') and class "text-sm text-muted-foreground"
  - date input bound to lastUsedFrom / setLastUsedFrom
  - span with t('pairs.lastUsedTo') and class "text-sm text-muted-foreground"
  - date input bound to lastUsedTo / setLastUsedTo

All four date inputs use: type="date", className="rounded border border-border bg-background px-2 py-1 text-sm"

The "Clear filters" button and "No results" clear button remain unchanged (they call clearFilters which now resets the four new vars).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>PairsPage compiles without TypeScript errors. The filter bar shows no preset pill buttons. Two date-range rows (first observed and last used) appear in the filter bar. Filtering by date range works correctly: setting firstObservedFrom hides pairs with firstObservationDate before that date; setting lastUsedTo hides pairs with lastUsedDate after that date. Clear filters resets all four inputs. Text search and status toggle are unaffected.</done>
</task>

</tasks>

<threat_model>
No new trust boundaries introduced — all filtering is client-side in memory over locally-stored data. Date string inputs from HTML date pickers are browser-validated to YYYY-MM-DD format; they are used only for lexicographic string comparison against stored date strings, never executed or sent to any server.
</threat_model>

<verification>
npm run build — TypeScript check must pass with no errors.
npm run lint — no new lint violations.
npx vitest run — existing tests must continue to pass.
Manual spot-check: open PairsPage in browser; confirm preset buttons are absent; set firstObservedFrom to a recent date and verify older pairs disappear; clear filters and verify all pairs return.
</verification>

<success_criteria>
- npm run build exits 0
- No console errors in browser on PairsPage
- Preset pill buttons (Last 7 days / Last 30 days / Learned after) are not rendered
- Two labeled date-range rows render correctly in both English and Polish
- Date range filtering works open-endedly: from-only, to-only, and from+to all produce correct subsets
- Clear filters resets all four date inputs
</success_criteria>

<output>
Create `.planning/quick/260914-w2a-replace-date-preset-buttons-on-pairspage/260914-w2a-SUMMARY.md` when done
</output>
