---
phase: 03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/DashboardPage.tsx
  - src/db/services/meaning.service.ts
  - src/components/ui/switch.tsx
  - src/components/ui/alert-dialog.tsx
  - src/components/ui/chart.tsx
  - src/components/ui/table.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - Dashboard displays the Active Meanings count as a large hero card at the top of the page
    - Dashboard displays Active Word Forms and New Meanings This Month as secondary metric cards in a 2-column grid
    - "Review these?" section displays meanings unused for 30+ days (max 3 items shown); empty state shows "All meanings used recently — great job!"
    - All dashboard metrics update reactively when meanings are added/modified (via useLiveQuery)
    - Switch component is installed and ready for Meaning detail page toggle
    - Chart component is installed and ready for Timeline bar/line charts
    - AlertDialog component is installed and ready for Word Form delete confirmation
    - Table component is installed (optional but available for Timeline data table)
  artifacts:
    - DashboardPage renders with three card sections (hero, 2-column metrics, review section)
    - meaning.service.ts contains new functions: getMeaningById, getMeaningsUnused30Days
    - src/components/ui/switch.tsx, alert-dialog.tsx, chart.tsx, table.tsx all present and importable
  key_links:
    - DashboardPage useLiveQuery → meaning.service.ts queries
    - Dashboard metrics → Active/Inactive toggle in Meaning detail (Phase 3 Plan 02)
    - "Review these?" meaning list → Meaning detail page (Phase 3 Plan 02)
  prohibitions:
    - statement: Do not add greeting text to Dashboard (go straight to metrics per D-01)
      verification: backstop
    - statement: Do not render Shadcn components in non-UI files (all Shadcn imports are in page or component files)
      verification: backstop
---

<objective>
A parent opens the app and sees the Dashboard with the Active Meanings count as a prominent hero card, secondary metrics showing Word Forms and New This Month, and a "Review these?" section listing old meanings. All metrics are reactive to database changes via useLiveQuery.

Purpose: Establish the end-to-end read path (Database → Service Queries → React Component → UI), validate the architecture before expanding to detail pages, and install all required Shadcn components.

Output: Working Dashboard page with metrics, four new Shadcn components ready for downstream pages, and extended service layer queries for dashboard and detail pages.
</objective>

<execution_context>
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-browse-views/03-CONTEXT.md
@.planning/phases/03-browse-views/03-UI-SPEC.md
@.planning/phases/03-browse-views/03-PATTERNS.md
@.planning/phases/02-onboarding-data-entry/02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Install Shadcn components: switch, alert-dialog, chart, table</name>
  <files>
    src/components/ui/switch.tsx
    src/components/ui/alert-dialog.tsx
    src/components/ui/chart.tsx
    src/components/ui/table.tsx
  </files>
  <read_first>
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-UI-SPEC.md (Registry Safety section)
    C:/my-code/vibe-coding/little-words/src/components/ui/badge.tsx (reference existing Shadcn component structure)
  </read_first>
  <action>
Install four Shadcn components using the official registry (D-07, D-12, D-08 from context decisions). Run these commands sequentially:
1. `npx shadcn@latest add switch` — for Meaning detail page Active/Inactive toggle
2. `npx shadcn@latest add alert-dialog` — for Word Form delete confirmation dialog
3. `npx shadcn@latest add chart` — Recharts wrapper for Timeline bar/line charts
4. `npx shadcn@latest add table` — optional but simplifies Timeline data table (can use simple Tailwind `<table>` if unavailable)

Verify that each component copies a .tsx file into `src/components/ui/` as source code with no runtime package dependency. Only official shadcn registry; no third-party registries.
  </action>
  <verify>
    <automated>test -f src/components/ui/switch.tsx && test -f src/components/ui/alert-dialog.tsx && test -f src/components/ui/chart.tsx && test -f src/components/ui/table.tsx && echo "All 4 components installed"</automated>
  </verify>
  <done>
All four Shadcn components are installed as .tsx source files in `src/components/ui/` and are importable without runtime dependencies.
  </done>
</task>

<task type="tracer">
  <name>End-to-end Dashboard page: hero card + secondary metrics + Review section</name>
  <files>
    src/pages/DashboardPage.tsx
    src/db/services/meaning.service.ts
  </files>
  <read_first>
    C:/my-code/vibe-coding/little-words/src/pages/DashboardPage.tsx (current stub)
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-PATTERNS.md (DashboardPage pattern section, MeaningsPage/CategoriesPage reference patterns)
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-CONTEXT.md (D-01 through D-04)
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-UI-SPEC.md (Copywriting Contract, Typography, Spacing, Color)
    C:/my-code/vibe-coding/little-words/src/db/services/meaning.service.ts (existing functions, transaction patterns)
    C:/my-code/vibe-coding/little-words/.planning/phases/02-onboarding-data-entry/02-SUMMARY.md (existing app structure from Phase 2)
  </read_first>
  <action>
Replace the DashboardPage stub with a complete implementation that renders three card sections:

**Hero Card (D-01, D-03):**
- Full-width card at the top with `flex flex-col gap-4 p-6` layout
- Display typography: "28px bold" with "Active meanings" label (per UI-SPEC Typography section)
- Numeric display via useLiveQuery query for count of meanings where isActive=true
- Example structure: label above the number, centered alignment

**Secondary Metrics Grid (D-02):**
- 2-column grid directly below hero card with `grid grid-cols-2 gap-4` (Tailwind utilities)
- Card 1: Active Word Forms count — query count of all wordForms in database
- Card 2: New Meanings This Month — query count of meanings where firstUseDate is within current calendar month
- Each card uses "Heading" typography (20px, 600 weight) for the metric and Label typography (14px) for the label

**Review These? Section (D-04):**
- Always shown; title "Review these?" with Heading typography
- Query: meanings where lastUseDate is 30+ days old AND isActive=true (limit to 3 for display)
- If 0 results: render positive empty state message "All meanings used recently — great job!" in muted foreground color
- If 1-3 meanings: render as list of meaning names with a one-tap shortcut to `/meanings/:id`
- If >3 meanings: show 3 items + "See all [N]" link to `/meanings` (unfiltered Meanings list)
- Use link styling consistent with D-14 and UI-SPEC

**Service Layer Additions (extend meaning.service.ts):**
Add these new query functions to support Dashboard and downstream detail pages (per PATTERNS section):
1. `getMeaningById(id: number): Promise<Meaning | undefined>` — fetches single meaning by ID (used by MeaningDetail)
2. `getMeaningsUnused30Days(): Promise<Meaning[]>` — query meanings where lastUseDate < 30 days ago AND isActive=true, limit 3 (used by Dashboard Review section, per D-04)

See PATTERNS file "meaning.service.ts" section for example implementations. Use Dexie transaction-safe patterns (reference wordForm.service.ts).

**Styling & Layout:**
- Use Tailwind utility-first (no CSS modules)
- Card padding: 16px internal (md), 24px gaps between cards (lg) per UI-SPEC Spacing section
- Page margins: 24px (lg) padding
- All text via useTranslation('common') from react-i18next
- Use existing Badge component for category tags if needed in Review section

**Reactive Updates:**
- All database reads via useLiveQuery (established pattern from Phase 2)
- Dashboard should reactively update when meanings are added/toggled/deleted from other views
- Loading state: show placeholder text while data loads (per PATTERNS loading pattern)
  </action>
  <verify>
    <automated>npm test -- --filter=dashboard 2>&1 | grep -c "PASS\|passed" && echo "Dashboard tests pass"</automated>
  </verify>
  <done>
DashboardPage renders a working Dashboard with hero Active Meanings card showing the correct reactive count, secondary metrics for Word Forms and New This Month showing correct counts, "Review these?" section showing up to 3 unused meanings or a positive empty state message, and all sections update reactively when database changes. Service layer functions getMeaningById and getMeaningsUnused30Days are implemented and used by Dashboard.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database → UI | useLiveQuery reads from Dexie; assumes DB data is consistent |
| User input → Service | No direct user input in Dashboard (read-only); Review section shows existing data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | meaning.service.ts | low | accept | Service layer makes read-only queries; write operations (toggleActive, updateDate) are in Phase 3 Plan 02 and use transaction-safe patterns |
| T-03-02 | Information Disclosure | DashboardPage useLiveQuery | low | accept | All data in IndexedDB is already on the parent's device; no network exposure in Phase 3 |

</threat_model>

<verification>
1. Shadcn components installed: run `ls src/components/ui/ | grep -E 'switch|alert-dialog|chart|table'` — all four files present
2. Dashboard page renders: run `npm run dev` and navigate to `/#/dashboard` — hero card, metrics grid, and Review section visible
3. Metrics are reactive: add a new meaning in a separate tab; Dashboard updates without page reload
4. getMeaningById and getMeaningsUnused30Days are exported from meaning.service.ts and called by Dashboard
5. All text strings use i18n keys (no hardcoded English/Polish); verify with `grep -n "t('" src/pages/DashboardPage.tsx`
</verification>

<success_criteria>
- Dashboard page displays all three sections with correct styling (hero, metrics grid, review section)
- Active Meanings count is reactive (updates when meanings are added/toggled in other views)
- Word Forms and New Meanings This Month counts are accurate
- "Review these?" section shows up to 3 unused meanings or the positive empty state message
- Shadcn switch, alert-dialog, chart, and table components are installed and ready for downstream plans
- New service functions getMeaningById and getMeaningsUnused30Days are implemented and tested
</success_criteria>

<output>
Create `.planning/phases/03-browse-views/03-01-SUMMARY.md` when complete
</output>
