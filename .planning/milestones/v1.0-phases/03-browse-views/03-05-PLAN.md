---
phase: 03-browse-views
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/MeaningsPage.tsx
  - src/pages/MeaningDetailPage.tsx
  - src/pages/WordFormDetailPage.tsx
  - src/db/services/wordEntry.service.ts
autonomous: true
gap_closure: true
gap_ids: [G-03-5a, G-03-7, G-03-11]
requirements: [BROWSE-02, ENTRY-03]
must_haves:
  truths:
    - "Category filter chip and category badges display in Polish when Polish language is selected"
    - "Date fields on Meaning and Word Form detail pages render in the device's configured language locale"
    - "Creating a word entry with blank meaning fields skips those meanings; no empty-string meanings are saved"
  artifacts:
    - "src/pages/MeaningsPage.tsx — category names translated via t(`category.${...}`)"
    - "src/pages/MeaningDetailPage.tsx — formatDate uses i18n.language locale"
    - "src/pages/WordFormDetailPage.tsx — formatDate uses i18n.language locale"
    - "src/db/services/wordEntry.service.ts — filters empty meanings before save"
  key_links:
    - "useTranslation() hook → i18n.language destructure"
    - "addWordEntry() → skip meanings with empty text"
    - "toLocaleDateString(i18n.language) → date locale"
---

<objective>
Close three trivial UAT gaps: category translation in Meanings view, date locale on detail pages, and empty-string meaning filtering.

Purpose: Polish UI localization and data quality (prevent user error of accidental blank meanings)
Output: Consistent Polish category names, correctly formatted dates, cleaner database
</objective>

<execution_context>
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-browse-views/03-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Fix category name translation in Meanings list and detail (G-03-5a)</name>
  <files>src/pages/MeaningsPage.tsx</files>
  <action>Open src/pages/MeaningsPage.tsx. Find the filter chip label on line 73 that renders `{categoryFilter}` inside the chip text. Replace it with `{t(\`category.${categoryFilter}\`)}`. Then find the category badges loop around line 104 that renders `{cat}` for each category. Replace each `{cat}` with `{t(\`category.${cat}\`)}`. These changes wire the existing i18n translations (already present from phase 03-02) to display category names in the active language. Verify that the `useTranslation()` hook is already imported at the top of the file.</action>
  <verify>
    <automated>npm run dev &amp;&amp; sleep 3 &amp;&amp; curl -s http://localhost:5173 | grep -q "MeaningsPage" &amp;&amp; echo "App runs"</automated>
  </verify>
  <done>Category filter chip label and category badges display in Polish when Polish language is selected; English when English is selected. The translation keys `category.Nouns`, `category.Verbs`, etc. are correctly resolved.</done>
</task>

<task type="auto">
  <name>Fix date locale on Meaning and Word Form detail pages (G-03-7)</name>
  <files>src/pages/MeaningDetailPage.tsx, src/pages/WordFormDetailPage.tsx</files>
  <action>Open src/pages/MeaningDetailPage.tsx. In the `useTranslation()` hook call (around line 77), add `i18n` to the destructuring. The line should read `const { t, i18n } = useTranslation();`. Then find the `formatDate()` helper function or inline `toLocaleDateString()` call that hardcodes `'en-US'` and replace it with `i18n.language`. Repeat the same change in src/pages/WordFormDetailPage.tsx around line 55. Test by switching to Polish and verifying that dates render in Polish format (e.g., "29 lipca 2026" instead of "7/29/2026").</action>
  <verify>
    <automated>grep -n "i18n.language" src/pages/MeaningDetailPage.tsx src/pages/WordFormDetailPage.tsx | grep -c "toLocaleDateString"</automated>
  </verify>
  <done>Both detail pages destructure `i18n` from `useTranslation()`. All `toLocaleDateString()` calls pass `i18n.language` instead of hardcoded `'en-US'`. Dates render in the active language locale.</done>
</task>

<task type="auto">
  <name>Filter empty meanings on word entry save (G-03-11)</name>
  <files>src/db/services/wordEntry.service.ts</files>
  <action>Open src/db/services/wordEntry.service.ts. Find the `addWordEntry()` function (around line 50). Before the loop that saves meanings, add a filter: `const validMeanings = meanings.filter(m => m.text.trim().length > 0);`. Then change the loop to iterate over `validMeanings` instead of the raw `meanings` array. This ensures empty strings are never persisted. If the intention is to filter client-side before calling the service, the filter can alternatively be added in useAddEntry.ts before the service call (check current implementation and apply the fix at the nearest validation point).</action>
  <verify>
    <automated>grep -A 5 "function addWordEntry" src/db/services/wordEntry.service.ts | grep -q "trim"</automated>
  </verify>
  <done>Empty meanings (text.trim().length === 0) are filtered before save. No empty-string meanings are created in the database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new in this plan. Existing boundaries (client-only data on device) remain unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| (none) | - | - | - | - | - |

</threat_model>

<verification>
After all tasks complete:
1. Switch app language to Polish via Settings → Language
2. Navigate to Meanings list — verify category filter chip shows Polish name (e.g., "Rzeczowniki" not "Nouns")
3. Open any meaning detail page — verify first use date is in Polish format
4. Open any word form detail page — verify date is in Polish format
5. Create a word entry, add word form, add a blank meaning row (do not fill it), save — verify no empty meanings appear in the Meanings list
</verification>

<success_criteria>
All three gaps closed:
- Category names translate when language is switched (G-03-5a closed)
- Dates render in active language locale (G-03-7 closed)
- No empty-string meanings are saved to database (G-03-11 closed)
</success_criteria>

<output>
Create `.planning/phases/03-browse-views/03-05-SUMMARY.md` after execution
</output>
