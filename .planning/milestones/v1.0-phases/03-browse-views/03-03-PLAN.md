---
phase: 03
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/pages/WordFormsPage.tsx
  - src/pages/WordFormDetailPage.tsx
  - src/router/index.tsx
  - src/db/services/wordForm.service.ts
autonomous: true
requirements:
  - BROWSE-02
must_haves:
  truths:
    - Word Forms list page displays scrollable list of all word forms sorted by creation date (newest first) by default
    - List page shows sort toggle button (Newest first / A–Z) that changes sort order in-component; sort preference resets on navigation
    - Tapping any word form in the list navigates to /word-forms/:id detail page
    - Word form detail page displays the word form text, linked meanings (as links to Meaning detail), first/last use dates, and linked meaning count (read-only)
    - Detail page has a delete button with confirmation dialog (D-08); dialog clarifies that only the word form is removed and linked meanings survive
    - Delete operation removes the word form record and its junction table entries but leaves Meaning records intact
    - Detail page has a back button or back navigation that returns to the Word Forms list
    - Empty states render correctly: "No word forms yet. Add your first one with the + button." when wordForms.length === 0
  artifacts:
    - WordFormsPage.tsx renders with sort toggle, scrollable list, and empty state
    - WordFormDetailPage.tsx renders with read-only fields, delete confirmation dialog, and back navigation
    - src/router/index.tsx updated with route { path: 'word-forms/:id', element: <WordFormDetailPage /> }
    - src/db/services/wordForm.service.ts contains getWordFormById and getWordFormWithMeaningCount functions
  key_links:
    - WordFormsPage sort state → local component state (not persisted)
    - Word form detail delete → deleteWordForm service function → removes record + junctions, leaves meanings intact
    - Word form detail linked meanings → Meaning records via wordFormMeanings junction table
  prohibitions:
    - statement: Do not render Word Form text as editable in Phase 3 (read-only per D-06)
      verification: backstop
    - statement: Do not delete Meaning records when deleting Word Form (only remove junction and word form per D-08)
      verification: backstop
    - statement: Do not show delete confirmation for words with no meanings (always show confirmation dialog per D-08)
      verification: backstop
---

<objective>
A parent can scroll through all word forms in a list, tap to open a detail page showing linked meanings, and delete the word form with a confirmation that clarifies the deletion only removes the word form, not the meanings.

Purpose: Expand to a second browse list for word forms, introduce the delete confirmation pattern used consistently across the app, and validate the junction table relationship between word forms and meanings.

Output: Two new pages (Word Forms list and Word Form detail), extended service queries, and router configuration for word form detail route.
</objective>

<execution_context>
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-browse-views/03-CONTEXT.md
@.planning/phases/03-browse-views/03-UI-SPEC.md
@.planning/phases/03-browse-views/03-PATTERNS.md
@.planning/phases/03-browse-views/03-01-SUMMARY.md
@.planning/phases/03-browse-views/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Implement Word Forms list page with sort toggle</name>
  <files>
    src/pages/WordFormsPage.tsx
  </files>
  <read_first>
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-PATTERNS.md (WordFormsPage pattern section, sort state pattern, shared useLiveQuery and Button patterns)
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-CONTEXT.md (D-16 for sort toggle)
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-UI-SPEC.md (Copywriting Contract section, empty states, sort toggle labels)
    C:/my-code/vibe-coding/little-words/src/pages/DashboardPage.tsx (reference useLiveQuery loading pattern, layout structure)
    C:/my-code/vibe-coding/little-words/src/pages/MeaningsPage.tsx (reference sort toggle implementation, page layout pattern)
    C:/my-code/vibe-coding/little-words/src/components/ui/button.tsx (sort toggle button styling)
  </read_first>
  <action>
Create WordFormsPage.tsx with:

**Imports:**
- useLiveQuery from dexie-react-hooks
- useTranslation from react-i18next
- useNavigate from react-router
- Button from shadcn
- db from @/db/db
- useState for sort state

**Page Structure:**
- Page title "Word Forms" with Heading typography
- Sort toggle button below title (default "Newest first") that switches between "Newest first" (by createdAt desc) and "A–Z" (by text asc)
- Sort state is local component state via useState; resets to default on each navigation
- Scrollable list of word forms with each item rendering as a tappable row
  - Row structure: word form text (Body typography)
  - Tap navigates to `/word-forms/${wordForm.id}` via useNavigate
- Empty state when wordForms.length === 0: render "No word forms yet. Add your first one with the + button." (per UI-SPEC Copywriting Contract)

**Database Query:**
Use useLiveQuery to fetch all word forms from db.wordForms.toCollection().toArray()

In the page component, sort in-memory based on sortOrder state:
```typescript
const sorted = wordForms ? [...wordForms].sort((a, b) => {
  if (sortOrder === 'newest') {
    return new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime()
  } else {
    return a.text.localeCompare(b.text)
  }
}) : []
```

**Styling & Accessibility:**
- Use Tailwind utilities consistent with MeaningsPage
- List rows responsive and touch-friendly (48px minimum height)
- All text via i18n keys; no hardcoded strings
  </action>
  <verify>
    <automated>npm test -- --filter=word-forms 2>&1 | grep -c "PASS\|passed" && echo "Word Forms list tests pass"</automated>
  </verify>
  <done>
WordFormsPage displays a scrollable list of word forms, sort toggle switches between "Newest first" and "A–Z" with in-memory sorting, empty state renders when no word forms exist, and tapping any word form row navigates to the detail page.
  </done>
</task>

<task type="auto">
  <name>Implement Word Form detail page with delete confirmation</name>
  <files>
    src/pages/WordFormDetailPage.tsx
    src/router/index.tsx
    src/db/services/wordForm.service.ts
  </files>
  <read_first>
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-PATTERNS.md (WordFormDetailPage pattern, delete confirmation pattern, shared detail page loading and async action patterns)
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-CONTEXT.md (D-05, D-08 for detail routing and delete confirmation)
    C:/my-code/vibe-coding/little-words/src/pages/MeaningDetailPage.tsx (reference detail page structure, loading state, back navigation)
    C:/my-code/vibe-coding/little-words/src/components/ui/alert-dialog.tsx (AlertDialog component from Plan 01)
    C:/my-code/vibe-coding/little-words/src/components/ui/button.tsx (delete button styling)
    C:/my-code/vibe-coding/little-words/src/db/services/wordForm.service.ts (existing deleteWordForm function)
  </read_first>
  <action>
Create WordFormDetailPage.tsx with:

**Imports:**
- useState from react
- useNavigate, useParams from react-router
- useLiveQuery from dexie-react-hooks
- useTranslation from react-i18next
- db from @/db/db
- deleteWordForm from @/db/services/wordForm.service
- Button from @/components/ui/button
- AlertDialog components from @/components/ui/alert-dialog
- Link from react-router for linked meanings

**Page Structure:**
- Load word form by ID from URL param using useParams()
- Display loading state while wordForm === undefined
- Once loaded, render:
  1. Word form text as Heading typography (read-only)
  2. First and last use dates as Label typography (read-only)
  3. **Linked meanings (read-only):**
     - Query: get all Meaning records linked to this word form via wordFormMeanings junction table
     - Display as list; each item is a clickable link to the Meaning detail page (/meanings/:id)
     - Empty state if no linked meanings: "No meanings linked yet." (per UI-SPEC)
  4. **Delete button with confirmation dialog (D-08):**
     - Button variant="destructive" with label "Delete"
     - AlertDialog trigger shows the delete button
     - Dialog title: "Delete word form?"
     - Dialog description: "This will remove the word form, but linked meanings will stay. Continue?" (per D-08 and UI-SPEC)
     - Dialog has two action buttons:
       - "Cancel" (secondary, closes dialog)
       - "Delete" (destructive, performs deleteWordForm and navigates back)
     - During deletion, disable the delete button (isDeleting state)
     - On error, log to console
  5. Back button or back navigation:
     - Button with onClick={() => navigate(-1)} to return to Word Forms list
     - Or use back icon from lucide-react

**Service Extensions:**
Add to wordForm.service.ts:
1. `getWordFormById(id: number): Promise<WordForm | undefined>` — fetches single word form by ID
2. `getWordFormsWithMeaningCount(id: number): Promise<WordForm & { meaningCount: number }>` — optional enhancement for detail page to show count of linked meanings

**Async State Handling:**
- useState for isDeleting flag
- Wrap deleteWordForm in try/catch
- Set isDeleting during async, clear in finally
- On success, navigate(-1) to return to list
- On error, log to console

**Styling:**
- Page padding and spacing consistent with MeaningDetailPage
- Read-only fields: render as `<div>` with Label typography label + Body typography value
- Delete button: use Button variant="destructive"
- All text via i18n keys
  </action>
  <verify>
    <automated>npm test -- --filter=word-form-detail 2>&1 | grep -c "PASS\|passed" && echo "Word Form detail tests pass"</automated>
  </verify>
  <done>
WordFormDetailPage loads and displays a single word form with text and dates. Linked meanings are displayed as clickable links. Delete confirmation dialog works correctly. Delete operation removes the word form and junctions but leaves Meaning records intact. Back navigation works. Empty state for unlinked meanings renders correctly.
  </done>
</task>

<task type="auto">
  <name>Update router to add Word Form detail route</name>
  <files>
    src/router/index.tsx
  </files>
  <read_first>
    C:/my-code/vibe-coding/little-words/src/router/index.tsx (current routes)
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-PATTERNS.md (router pattern section for route addition)
    C:/my-code/vibe-coding/little-words/.planning/phases/03-browse-views/03-CONTEXT.md (D-05 for detail route nesting)
  </read_first>
  <action>
Update src/router/index.tsx to:

1. Add import for WordFormDetailPage at the top:
```typescript
import { WordFormDetailPage } from '../pages/WordFormDetailPage'
```

2. Add the detail route as a child of the root layout (same level as `/word-forms`):
```typescript
{ path: 'word-forms/:id', element: <WordFormDetailPage /> }
```

The route should be nested inside the RootLayout so that the bottom nav remains visible on the detail page. Verify the route structure matches the existing pattern for other pages.

**Note on merge conflicts:** If Plan 02 and Plan 03 are running in parallel (Wave 2), be careful when editing src/router/index.tsx. Make sure both detail routes (`/meanings/:id` and `/word-forms/:id`) are added. If you see both routes already in the file, skip this task (they may have been added by Plan 02).
  </action>
  <verify>
    <automated>grep -c "word-forms/:id" src/router/index.tsx && echo "Route added successfully"</automated>
  </verify>
  <done>
Router is updated with the /word-forms/:id route pointing to WordFormDetailPage. Navigating to `/word-forms/1` loads the Word Form detail page.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → Page load | wordForm.id from URL param is validated by useLiveQuery (returns undefined if not found) |
| User delete action → Service | Delete button calls deleteWordForm which uses Dexie transactions for atomicity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | WordFormDetailPage delete | high | mitigate | deleteWordForm uses Dexie transaction for atomic delete; junction rows removed atomically; detail page requires confirmation dialog before delete executes |
| T-03-06 | Denial of Service | deleteWordForm transaction | medium | mitigate | Transaction is atomic; no partial deletes; rollback on error |

</threat_model>

<verification>
1. Word Forms list renders: navigate to `/#/word-forms` and verify scrollable list appears
2. Sort toggle works: click "A–Z" and verify list reorders alphabetically; click "Newest first" and verify back to date order
3. Word Form detail loads: tap any word form and navigate to `/word-forms/:id`; verify text and dates display correctly
4. Linked meanings display: verify all meanings linked to the word form appear as clickable links
5. Links work: click a meaning link and navigate to `/meanings/:id`; verify detail page loads
6. Delete confirmation: click delete button and verify dialog appears with correct title and description
7. Delete works: click "Delete" in confirmation and verify word form is removed from list; verify in another tab that linked meanings still exist
8. Back navigation: click back button or use browser back; return to Word Forms list
9. Empty states: create test scenario with no word forms; verify "No word forms yet" renders
10. Router: verify `src/router/index.tsx` has the `/word-forms/:id` route with WordFormDetailPage import
</verification>

<success_criteria>
- Word Forms list page displays all word forms sorted by creation date by default
- Sort toggle changes list order to alphabetical; sort state resets on navigation
- Word Form detail page displays all fields: text and dates
- Linked meanings are displayed as clickable links to Meaning detail page
- Delete confirmation dialog displays correct text clarifying that meanings survive
- Delete operation removes the word form and junctions but leaves Meaning records intact
- Back navigation returns to Word Forms list
- Empty states render correctly for both list and detail pages
- Router is updated with /word-forms/:id route
</success_criteria>

<output>
Create `.planning/phases/03-browse-views/03-03-SUMMARY.md` when complete
</output>
