---
phase: "06.3"
slug: "address-tech-debt-requirements-md-traceability-import-valida"
status: verified
# threats_open = count of OPEN threats at or above workflow.security_block_on severity (the blocking gate)
threats_open: 0
asvs_level: 1
created: "2026-09-18"
---

# Phase 06.3 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|----------------|
| Local file input -> IndexedDB write | `<input type="file">` in DataSection.tsx accepts any file the parent selects (hand-edited export, old/corrupted backup, or a file from an untrusted source) — parsed JSON is untrusted input crossing into a destructive `db.transaction` that clears and rewrites all four tables | Backup JSON (childProfile, wordForms, meanings, wordFormMeanings) |
| Internal documentation edits | Plans 02 and 03 only edit `.planning/` markdown (REQUIREMENTS.md, SUMMARY.md frontmatter, VERIFICATION.md body text) | None — no runtime code, no trust boundary crossed |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-06.3-01 | Tampering | dataManagement.ts#importData / validateBackupData | medium | mitigate | `validateBackupData` rejects `childProfile.length !== 1`; `importData` throws `invalid-child-profile-count` (dataManagement.ts:194-197) before `db.transaction` runs — verified in code: the check precedes the transaction, so a malformed/hand-edited backup with 0 or 2+ profiles is rejected with zero writes | closed |
| T-06.3-02 | Tampering | dataManagement.ts (wordForms/meanings/wordFormMeanings referential integrity, CSV formula injection) | medium | accept | Explicitly deferred per D-06 to a future import-hardening phase; scoped only to the childProfile-count axis (D-05); the other three arrays are legitimately empty for a freshly onboarded child | closed |
| T-06.3-03a | N/A | REQUIREMENTS.md / SUMMARY.md frontmatter (plan 02) | low | accept | Pure documentation edits — no code executes, no trust boundary crossed; out of scope for ASVS L1 | closed |
| T-06.3-03b | Tampering | dataManagement.ts#importData (plan 04 gap closure) | low | mitigate | Widened `childProfile` pre-check (`!Array.isArray(childProfile) \|\| childProfile.length !== 1`, dataManagement.ts:195) to cover missing/null/non-array shapes, not just wrong-length arrays — verified in code | closed |
| T-06.3-04 | N/A | 05-VERIFICATION.md / 06-VERIFICATION.md body text (plan 03) | low | accept | Pure documentation edit correcting a cosmetic status-field inconsistency — no code executes, no trust boundary crossed | closed |

*Status: open · closed · open — below {block_on} threshold (non-blocking)*
*Severity: critical > high > medium > low — only open threats at or above workflow.security_block_on count toward threats_open*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

**Note:** T-06.3-03a (plan 02) and T-06.3-03b (plan 04) both originally used the ID `T-06.3-03` in their respective PLAN.md threat models — a naming collision across plans within this phase, disambiguated here with `a`/`b` suffixes. No functional impact; both threats are independently closed.

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|--------------|------|
| R-06.3-01 | T-06.3-02 | Referential integrity across `wordForms`/`meanings`/`wordFormMeanings` and CSV formula injection are out of scope for this phase's fix, which targets only the `childProfile`-count footgun the v1.0 audit flagged. Deferred per D-06 to a future import-hardening phase. Low likelihood — requires a hand-crafted or corrupted backup file; single-device, no-account app limits blast radius to the user's own data. | milestone audit (v1.0) / phase 06.3 planning | 2026-09-18 |
| R-06.3-02 | T-06.3-03a, T-06.3-04 | Pure documentation edits (REQUIREMENTS.md traceability, SUMMARY.md frontmatter, VERIFICATION.md status text) — no runtime code executes, no trust boundary crossed. | phase 06.3 planning | 2026-09-18 |

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|----------------|--------|------|--------|
| 2026-09-18 | 5 | 5 | 0 | gsd-secure-phase (L1 grep-depth verification against plan-time threat registers; register_authored_at_plan_time: true for all code-touching plans, short-circuited per ASVS L1 policy) |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-09-18
