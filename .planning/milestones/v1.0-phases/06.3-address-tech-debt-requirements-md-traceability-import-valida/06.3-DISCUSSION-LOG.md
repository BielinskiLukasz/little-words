# Phase 06.3: Address tech debt: REQUIREMENTS.md traceability, import validation footgun, verification artifact hygiene - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-17
**Phase:** 06.3-address-tech-debt-requirements-md-traceability-import-valida
**Areas discussed:** Import validation footgun, REQUIREMENTS.md traceability + hygiene scope, Verification artifact hygiene

---

## Import validation footgun

| Option | Description | Selected |
|--------|-------------|----------|
| Require exactly 1 childProfile | Rejects any backup where `childProfile.length !== 1`; matches v1 single-profile model exactly | ✓ |
| Require at least 1 childProfile | Rejects only empty-array case; would silently accept 2+ profiles even though unsupported | |

**User's choice:** Require exactly 1 childProfile
**Notes:** MULTI-01 (multiple profiles) is out of scope for v1 per REQUIREMENTS.md, so exactly-1 is the correct invariant.

| Option | Description | Selected |
|--------|-------------|----------|
| New distinct error case | New thrown error + i18n message separate from generic 'corrupt' | ✓ |
| Fold into existing 'corrupt' error | Reuse generic message; less code but less specific to the parent | |

**User's choice:** New distinct error case
**Notes:** Mirrors the existing `wrong-schema-version` vs `corrupt` distinction already in `importData`.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — explicit test | Dedicated regression test for the empty-childProfile-array case | ✓ |
| No — covered by general tests | Rely on incidental coverage from the strictness fix | |

**User's choice:** Yes — explicit test

| Option | Description | Selected |
|--------|-------------|----------|
| childProfile only | Other arrays (wordForms/meanings/wordFormMeanings) legitimately can be empty; broadening would break valid backups | ✓ |
| Broaden to all four arrays | Would reject a valid backup of a freshly onboarded child with zero entries | |

**User's choice:** childProfile only

| Option | Description | Selected |
|--------|-------------|----------|
| New i18n key, plain explanation | New key alongside existing importErrorDescriptionVersion / corrupt-file keys | ✓ |
| You decide wording | Leave phrasing to planner/executor | |

**User's choice:** New i18n key, plain explanation

**Bundle-scope check:** Asked whether to ride along 3 other `06.2-REVIEW.md` warnings (CSV formula-injection guard, category validation, referential integrity, missing wordForms round-trip test) in this phase. The user's follow-up message ("I mean next area") indicated they wanted to move on rather than expand scope further — recorded as staying scoped to the childProfile-count fix only (the option already recommended), with the three additional warnings deferred rather than bundled in.

---

## REQUIREMENTS.md traceability + hygiene scope

| Option | Description | Selected |
|--------|-------------|----------|
| Both | Add PREREL-01..05 traceability rows AND backfill requirements-completed frontmatter in 14 SUMMARY.md files | ✓ |
| REQUIREMENTS.md traceability only | Just the 5 rows; leave SUMMARY.md frontmatter gaps as accepted debt | |
| You decide | Scope based on effort/risk | |

**User's choice:** Both

| Option | Description | Selected |
|--------|-------------|----------|
| Traceability rows only | 5 rows in existing table pointing at Phase 6; full text stays in ROADMAP.md | ✓ |
| Full section + traceability rows | Add a full "Pre-release Polish" prose section too, duplicating ROADMAP.md text | |

**User's choice:** Traceability rows only

---

## Verification artifact hygiene

| Option | Description | Selected |
|--------|-------------|----------|
| Keep 'passed', fix body text | Change body `Status: human_needed` to `passed` in both files; human-verification checklist stays | ✓ |
| Introduce a distinct status value | New vocabulary e.g. `passed_human_needed` used consistently | |
| Change frontmatter to 'human_needed' | Understates that code-level verification fully passed | |

**User's choice:** Keep 'passed', fix body text

| Option | Description | Selected |
|--------|-------------|----------|
| No — just fix these two files | One-off cleanup; tooling/lint for future files is a separate concern | ✓ |
| Yes — note it for the verify workflow | Flag as deferred idea for gsd-verifier maintainers | |

**User's choice:** No — just fix these two files

---

## Claude's Discretion

- Exact i18n key name for the new import error message (planner/executor picks something consistent with existing `errors.*` naming).
- Whether the new regression test lives in the existing `validateBackupData` describe block or a new one.

## Deferred Ideas

- CSV formula-injection guard on import (06.2-REVIEW.md) — future phase.
- Category validation on import (06.2-REVIEW.md) — future phase.
- Referential integrity check between imported wordForms/meanings/wordFormMeanings (06.2-REVIEW.md) — future phase.
- Missing wordForms round-trip test coverage (06.2-REVIEW.md) — future phase.
- Tooling/lint to prevent future VERIFICATION.md frontmatter/body status drift — noted for gsd-verifier/verify-phase workflow maintainers.
