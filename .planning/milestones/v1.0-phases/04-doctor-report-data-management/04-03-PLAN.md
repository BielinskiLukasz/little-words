---
phase: 04-doctor-report-data-management
plan: 03
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-04-7]
files_modified:
  - src/features/settings/services/dataManagement.ts
  - src/features/settings/services/dataManagement.test.ts
autonomous: true
requirements: [DM-05]

estimate:
  tokens: 18000
  raw_tokens: 9000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - "little-words-meanings.csv header row is wordForms,label,categories,firstUseDate,lastUseDate,active"
    - "The wordForms cell appears as the first column in every data row"
    - "All buildMeaningsCSV unit tests pass with the new column ordering"
  artifacts:
    - src/features/settings/services/dataManagement.ts
    - src/features/settings/services/dataManagement.test.ts
  key_links:
    - "buildMeaningsCSV header string and cells array must be in sync — mismatched ordering would produce wrong column data"
---

<objective>
Fix G-04-7: reorder CSV export columns so wordForms appears as the first column.

Purpose: The current export places wordForms last, making the CSV harder to read. The user asked to move it to the beginning (1st or 2nd position). The agreed new order is wordForms → label → categories → firstUseDate → lastUseDate → active.
Output: dataManagement.ts with reordered header and cells array; dataManagement.test.ts with updated column-index assertions.
</objective>

<execution_context>
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/little-words/.planning/ROADMAP.md
@C:/my-code/vibe-coding/little-words/.planning/STATE.md
@C:/my-code/vibe-coding/little-words/src/features/settings/services/dataManagement.ts
@C:/my-code/vibe-coding/little-words/src/features/settings/services/dataManagement.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reorder CSV columns and update tests</name>
  <files>
    src/features/settings/services/dataManagement.ts,
    src/features/settings/services/dataManagement.test.ts
  </files>
  <behavior>
    - buildMeaningsCSV header: first token is 'wordForms', second is 'label'
    - buildMeaningsCSV data row index 0 contains the joined word forms string
    - buildMeaningsCSV data row index 1 contains meaning.text
    - buildMeaningsCSV data row index 2 contains joined categories
    - buildMeaningsCSV data row index 5 contains 'true' or 'false' for isActive
    - Empty meanings array produces a string equal to the new header only (no trailing newline)
    - A meaning with no linked word forms has an empty string at index 0 in its row
  </behavior>
  <action>
    In dataManagement.ts, locate buildMeaningsCSV (around line 92). Change the header constant to
    place wordForms first: the new header is the six tokens joined by commas in this order —
    wordForms, label, categories, firstUseDate, lastUseDate, active.

    Reorder the cells array inside the map callback to match: move linkedForms to index 0 and
    meaning.text to index 1; the remaining four fields (categories.join(';'), firstUseDate,
    lastUseDate, String(isActive)) stay in their relative order at indices 2-5. No other logic
    in the function changes.

    In dataManagement.test.ts, update every assertion that references the old header string or
    hard-coded column indices to match the new ordering:

    - The two tests that compare against the full header string: update the expected string to the
      new header.
    - "has the meaning text as the first column" test: meaning.text is now at index 1 in the row;
      update the regex or split-index check accordingly, and update the test description to say
      "second column".
    - "joins categories with semicolons in the second column" test: categories is now at index 2;
      update the split index and the test description to say "third column".
    - "active column is 'true'" and "active column is 'false'" tests: active is now at index 5;
      change cols[4] to cols[5] in both.
    - "has empty wordForms column" test: wordForms is now at index 0; change cols[5] to cols[0].
    - "lists multiple linked word forms separated by semicolons" test: wordForms is now at index 0;
      change cols[5] to cols[0].
    - "wraps cell values containing commas in double-quotes" test: the label is now the second cell
      in the row (index 1), so the row begins with the empty wordForms cell then a comma then the
      quoted label; update the regex to anchor on the new position.

    Do not modify any other logic, imports, or tests.
  </action>
  <verify>
    <automated>npx vitest run src/features/settings/services/dataManagement.test.ts</automated>
  </verify>
  <done>
    All dataManagement tests pass. The buildMeaningsCSV header begins with 'wordForms,label'.
    A spot-check: buildMeaningsCSV([{id:1,text:'mama',categories:['People'],isActive:true,
    firstUseDate:'2024-01-01',lastUseDate:'2024-06-01'}],[],[]).split('\n')[1] starts with
    ',mama,' (empty wordForms cell, then the label).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Function output → file download | The CSV string is written to a Blob and offered as a download; no untrusted input crosses this boundary in this fix |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-GC-01 | Information Disclosure | buildMeaningsCSV output | low | accept | Column reordering does not change what data is exported; same fields, different order; no new disclosure surface |
</threat_model>

<verification>
Run the full test suite to confirm no regressions:
npx vitest run src/features/settings/services/dataManagement.test.ts

All tests in the buildMeaningsCSV describe block must pass with the new column ordering.
</verification>

<success_criteria>
- Header row of exported CSV: wordForms,label,categories,firstUseDate,lastUseDate,active
- All 9 tests in the buildMeaningsCSV describe block pass
- No other tests in the file regress
- npm run lint passes with no new errors
</success_criteria>

<output>
Create `.planning/phases/04-doctor-report-data-management/04-03-SUMMARY.md` when done
</output>
