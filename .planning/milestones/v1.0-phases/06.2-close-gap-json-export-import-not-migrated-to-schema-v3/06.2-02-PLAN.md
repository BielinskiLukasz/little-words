---
phase: 06.2-close-gap-json-export-import-not-migrated-to-schema-v3
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales/en/common.json
  - src/i18n/locales/pl/common.json
autonomous: true
requirements:
  - DATA-02

estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "settings.importErrorDescriptionVersion in both en/common.json and pl/common.json no longer implies the backup file is more recent than the app — reworded to version-direction-neutral phrasing describing a version mismatch, in both locales (D-04)"
    - "The importErrorDescriptionVersion key name, its position in the settings namespace, and DataSection.tsx's consumption of it (message.includes('wrong-schema-version') -> t('settings.importErrorDescriptionVersion')) are all unchanged — only the string VALUES change"
  artifacts:
    - src/i18n/locales/en/common.json
    - src/i18n/locales/pl/common.json
  key_links:
    - "DataSection.tsx handleFileSelected -> message.includes('wrong-schema-version') -> t('settings.importErrorDescriptionVersion') -> reworded EN/PL string (wiring unchanged; D-04 only touches the two string values consumed here)"
---

<objective>
Close the user-facing half of milestone v1.0 audit blocker 2: `importErrorDescriptionVersion` (both locales) currently tells the parent their backup "is from a newer version of the app" — factually backwards for the exact v2-to-v3 rejection scenario the sibling plan `06.2-01` implements, since a rejected legacy backup is OLDER, not newer. This surfaced during discussion as a real user-facing bug, not scope creep — it is the precise error path the version check in `06.2-01` produces (D-04).

This is a pure content fix to two static JSON locale files — no code, no tests, no new symbols. Since there is no "layer" to slice through (a config/content-only change), the single task below IS the complete, wired, end-to-end fix: a parent who triggers the version-mismatch error path now sees an accurate message in whichever language they use.

Purpose: A parent seeing this error should get a factually correct explanation, not a confusing (and wrong-direction) one.
Output: Both `en/common.json` and `pl/common.json`'s `settings.importErrorDescriptionVersion` values reworded to be version-direction-neutral, with the key name, JSON structure, and consuming component completely unchanged.
</objective>

<execution_context>
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-close-gap-json-export-import-not-migrated-to-schema-v3/06.2-CONTEXT.md
@.planning/phases/06.2-close-gap-json-export-import-not-migrated-to-schema-v3/06.2-PATTERNS.md
@src/i18n/locales/en/common.json
@src/i18n/locales/pl/common.json

Key decision: D-04 (reword the version-mismatch error string in both locales to remove the incorrect directionality assumption). Claude's discretion covers exact wording as long as it is version-direction-neutral (per CONTEXT.md).
</context>

<tasks>

<task type="tracer">
  <name>Task: Reword importErrorDescriptionVersion to version-direction-neutral phrasing (D-04)</name>
  <files>src/i18n/locales/en/common.json, src/i18n/locales/pl/common.json</files>
  <read_first>
    - src/i18n/locales/en/common.json — read lines 50-62 (the `settings` namespace block containing `importErrorTitle`, `importErrorDescriptionCorrupt`, and the line-60 `importErrorDescriptionVersion` value that needs rewording). Note the surrounding keys' tone/style (short, plain-language, second-person-implicit sentences ending in a period) to match.
    - src/i18n/locales/pl/common.json — read lines 52-64 (the equivalent Polish `settings` namespace block, line 62 holds the Polish equivalent of the same incorrect phrasing).
    - src/features/settings/components/DataSection.tsx — read lines 44-67 (`handleFileSelected`), confirm `message.includes('wrong-schema-version')` maps to `t('settings.importErrorDescriptionVersion')` and that no code change is needed here — this task is read-only against this file.
    - .planning/phases/06.2-close-gap-json-export-import-not-migrated-to-schema-v3/06.2-CONTEXT.md — D-04 and the `<specifics>`/discretion note on exact wording latitude.
    - .planning/phases/06.2-close-gap-json-export-import-not-migrated-to-schema-v3/06.2-PATTERNS.md — suggested replacement wording for both locales (Claude's discretion allows alternative phrasing as long as the incorrect directionality assumption is removed).
  </read_first>
  <action>
    In `src/i18n/locales/en/common.json`, replace the value of `settings.importErrorDescriptionVersion` (currently line 60) with a version-direction-neutral sentence that states the backup was made with a different/incompatible version of the app and cannot be imported — do not assert or imply any direction (older vs. newer). Keep the key name, its position in the `settings` object, and the trailing comma/JSON structure exactly as-is; only the string value changes.

    In `src/i18n/locales/pl/common.json`, apply the equivalent Polish rewording to `settings.importErrorDescriptionVersion` (currently line 62) — same meaning, same direction-neutral framing, natural Polish phrasing consistent with the surrounding strings' tone. Keep the key name and JSON structure unchanged.

    Do not touch `importErrorTitle`, `importErrorDescriptionCorrupt`, `importSuccess`, or any other key in either file. Do not touch `DataSection.tsx` — the error-message-to-i18n-key mapping is unchanged; only the two string values change.
  </action>
  <acceptance_criteria>
    - `settings.importErrorDescriptionVersion` exists in both `en/common.json` and `pl/common.json` with an unchanged key name
    - The EN value no longer contains the phrase describing the backup as more recent than the app (verified by the negative-grep verify below)
    - The PL value no longer contains the Polish word for "newer" (`nowszej`) in that string (verified by the negative-grep verify below)
    - Both JSON files remain valid JSON (parse successfully as part of the Vite build)
    - `npm run build` exits 0 with no TypeScript i18n-key-augmentation errors (key names unchanged, so no new/removed keys)
    - `src/i18n/i18n.test.ts` continues to pass unchanged (no regression to unrelated i18n behavior)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "newer version" C:/my-code/vibe-coding/little-words/src/i18n/locales/en/common.json</automated>
    <fails_when>count is greater than 0 (the old incorrect directional phrasing is still present in the EN locale file)</fails_when>
  </verify>
  <verify>
    <automated>grep -c "nowszej" C:/my-code/vibe-coding/little-words/src/i18n/locales/pl/common.json</automated>
    <fails_when>count is greater than 0 (the old incorrect directional phrasing is still present in the PL locale file)</fails_when>
  </verify>
  <verify>
    <automated>grep -c "importErrorDescriptionVersion" C:/my-code/vibe-coding/little-words/src/i18n/locales/en/common.json C:/my-code/vibe-coding/little-words/src/i18n/locales/pl/common.json</automated>
    <fails_when>either file reports a count of 0 (the key itself was accidentally removed instead of just its value reworded)</fails_when>
  </verify>
  <verify>
    <automated>npm --prefix C:/my-code/vibe-coding/little-words run build 2>&1 | tail -10</automated>
    <fails_when>non-zero exit code, or output contains "error TS"</fails_when>
  </verify>
  <verify>
    <automated>npx vitest run C:/my-code/vibe-coding/little-words/src/i18n/i18n.test.ts 2>&1 | tail -25</automated>
    <fails_when>non-zero exit code, or the summary line contains "failed"</fails_when>
  </verify>
  <done>Both locale files' importErrorDescriptionVersion values are reworded to version-direction-neutral phrasing, the key name and JSON structure are unchanged, and build + i18n tests pass.</done>
  <reversibility rating="reversible">A content-only string change to two static locale JSON files, consumed via an unchanged i18n key — fully revertible via git revert with zero data-loss risk and no schema/API surface affected.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Both files are static, build-time-bundled translation strings with no runtime user input and no variable interpolation in this key (`importErrorDescriptionVersion` takes no `{{var}}` placeholders) — there is no untrusted-input boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-05 | Information Disclosure | Reworded `importErrorDescriptionVersion` string content | low | accept | Static, bundled translation string with no user-data interpolation; rewording is a pure copy-accuracy fix (D-04) with zero information-disclosure, injection, or XSS surface — react-i18next renders it as plain text, not HTML |
</threat_model>

<verification>
<verify>
  <automated>npm --prefix C:/my-code/vibe-coding/little-words run build 2>&1 | tail -10</automated>
  <fails_when>non-zero exit code, or output contains "error TS"</fails_when>
</verify>
<verify>
  <automated>npx vitest run C:/my-code/vibe-coding/little-words/src/i18n/i18n.test.ts 2>&1 | tail -25</automated>
  <fails_when>non-zero exit code, or the summary line contains "failed"</fails_when>
</verify>
</verification>

<success_criteria>
- `settings.importErrorDescriptionVersion` is reworded to version-direction-neutral phrasing in both `en/common.json` and `pl/common.json` (D-04)
- No other keys in either file are modified
- `DataSection.tsx` is unchanged — the error-to-key mapping wiring stays intact
- `npm run build` exits 0
- `src/i18n/i18n.test.ts` continues to pass unchanged
</success_criteria>

## Artifacts This Phase Produces

No new files, no new symbols — a value-only content change to two existing tracked JSON files:

- `src/i18n/locales/en/common.json` — `settings.importErrorDescriptionVersion` value reworded; all other keys unchanged.
- `src/i18n/locales/pl/common.json` — `settings.importErrorDescriptionVersion` value reworded; all other keys unchanged.
- `src/features/settings/components/DataSection.tsx` — unchanged (read-only reference).

<output>
Create `.planning/phases/06.2-close-gap-json-export-import-not-migrated-to-schema-v3/06.2-02-SUMMARY.md` when done.
</output>
