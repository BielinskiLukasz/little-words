# Phase 06.2: Close gap: JSON export/import not migrated to schema v3 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-17
**Phase:** 06.2-close-gap-json-export-import-not-migrated-to-schema-v3
**Areas discussed:** Legacy v2 backup handling, Per-pair validation strictness, Version check semantics

---

## Legacy v2 backup handling

| Option | Description | Selected |
|--------|-------------|----------|
| Reject outright | Same as today's pattern: throw 'wrong-schema-version' | ✓ |
| Auto-migrate on import | Backfill pair fields from parent Meaning, mirroring Dexie v3 upgrade() | |

**User's choice:** Reject outright
**Notes:** Confirmed no real v2 backups exist yet (app hasn't shipped v1.0), so rejection carries no real migration cost (D-03).

| Option | Description | Selected |
|--------|-------------|----------|
| Keep wording as-is | Don't touch i18n error strings | |
| Version-neutral wording | Fix "newer version" wording, which is factually wrong for a v2 (older) file | ✓ |

**User's choice:** Make it version-neutral
**Notes:** Surfaced during discussion that `importErrorDescriptionVersion` says "newer version" in both `en` and `pl` locale files, which is backwards for the v2→v3 case (D-04).

---

## Per-pair validation strictness

| Option | Description | Selected |
|--------|-------------|----------|
| Require all 3, correct types | firstObservationDate/lastUsedDate/isActive strictly typed, reject otherwise | ✓ |
| Require presence only, loose typing | Just check keys exist | |

**User's choice:** Require all 3, correct types, reject otherwise (D-05)

| Option | Description | Selected |
|--------|-------------|----------|
| Round-trip + rejection cases | Round-trip valid v3 fixture, reject v2-shaped fixture, reject v3 fixture missing a field | ✓ |
| Round-trip only | Just update fixtures to v3 shape, skip explicit rejection tests | |

**User's choice:** Round-trip + rejection cases (D-06)

---

## Version check semantics

| Option | Description | Selected |
|--------|-------------|----------|
| Exact match === 3 | Matches existing exact-match pattern | ✓ |
| Accept >= 3 | Forward-compatible but validators don't know future field shapes | |

**User's choice:** Exact match === 3 (D-02)

| Option | Description | Selected |
|--------|-------------|----------|
| Extract a named constant | BACKUP_SCHEMA_VERSION used by both export and import checks | ✓ |
| Just bump the literal to 3 | Minimal diff, matches audit's literal recommendation | |

**User's choice:** Extract a named constant
**Notes:** Directly targets the root cause — the literal was hardcoded in two places and never updated when Dexie moved to v3 (D-01).

---

## Claude's Discretion

- Exact placement/naming of the `BACKUP_SCHEMA_VERSION` constant
- Exact wording of the reworded `importErrorDescriptionVersion` strings in both locales

## Deferred Ideas

- CSV export schema divergence — separate tech-debt item, not this phase's scope
- Auto-migration of legacy v2 backups on import — no real v2 backups exist yet; revisit if one surfaces
