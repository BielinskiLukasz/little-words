---
phase: 06-pre-release-polish
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/pages/MeaningDetailPage.tsx
autonomous: true
requirements:
  - PREREL-01
  - PREREL-02

estimate:
  tokens: 80000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Category badges on Meaning detail show translated names via t('category.' + cat) (D-16)"
    - "Tapping a linked word form in the per-pair row navigates to /word-forms/:id (PREREL-01 D-06)"
    - "Edit button enters inline edit mode; meaning text becomes a textarea; categories become toggleable CategoryChips (D-05)"
    - "Save Changes button commits text + categories via updateMeaning service call; Discard Changes reverts without saving (D-05)"
    - "Per-pair rows are collapsible; expanded state shows firstObservationDate, lastUsedDate inputs and isActive Switch (D-06 D-08)"
    - "Date inputs use <input type='date'> — no Calendar/Popover component (D-08)"
    - "Pair date/isActive changes save on blur via updatePairFields; aggregation updates Meaning aggregate fields (D-06 D-02)"
    - "Meaning.isActive shown as read-only Badge (derived from pairs) — no direct isActive toggle on Meaning level (D-02)"
  artifacts:
    - src/pages/MeaningDetailPage.tsx
  key_links:
    - "updateMeaning imported from meaning.service (Plan 01 deliverable)"
    - "updatePairFields imported from wordFormMeaning.service (Plan 01 deliverable)"
    - "CategoryChips imported from @/features/add-entry/components/CategoryChips"
    - "Collapsible from @/components/ui/collapsible"
    - "Switch from @/components/ui/switch"
---

<objective>
Rewrite MeaningDetailPage to deliver inline editing (D-05), per-pair expandable rows with navigation (D-06), date inputs via input[type=date] (D-08), and category i18n fix (D-16).

Purpose: PREREL-01 + PREREL-02 — the two most visible UAT blockers on the Meaning detail page.
Output: MeaningDetailPage.tsx fully rewritten with inline edit mode and collapsible pair rows.
</objective>

<execution_context>
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pre-release-polish/06-CONTEXT.md
@.planning/phases/06-pre-release-polish/06-UI-SPEC.md

Key decisions: D-02 D-05 D-06 D-08 D-16
Prior plan output: 06-01-SUMMARY.md (updateMeaning, updatePairFields, aggregateMeaningFromPairs)
</context>

<tasks>

<task type="tracer">
  <name>Tracer: Inline edit mode for meaning text and categories + category i18n fix</name>
  <read_first>
    - src/pages/MeaningDetailPage.tsx — read entire file; map all existing state, useLiveQuery calls, handlers, and JSX structure
    - src/features/add-entry/components/CategoryChips.tsx — CategoryChips component props interface
    - src/db/services/meaning.service.ts — confirm updateMeaning is exported (Plan 01 deliverable)
    - src/components/ui/badge.tsx — Badge component props
    - src/components/ui/button.tsx — Button variants (outline, ghost, primary)
  </read_first>
  <files>src/pages/MeaningDetailPage.tsx</files>
  <action>
    Rewrite the header/edit area of MeaningDetailPage.tsx to deliver inline edit mode for meaning text and categories (D-05), and category i18n translation fix (D-16). This tracer task proves the Edit→Save/Discard flow end-to-end. The per-pair rows (Task 2) are added on top.

    State to add:
    - `const [isEditing, setIsEditing] = useState(false)`
    - `const [editText, setEditText] = useState('')`
    - `const [editCategories, setEditCategories] = useState<Category[]>([])`
    - `const [isSaving, setIsSaving] = useState(false)`

    Edit mode entry: clicking the Edit button calls `setIsEditing(true)`, `setEditText(meaning.text)`, `setEditCategories(meaning.categories)`.

    Save handler:
    1. Set `isSaving(true)`
    2. `await updateMeaning(meaning.id!, { text: editText, categories: editCategories })`
    3. In `finally`: `setIsSaving(false)`, `setIsEditing(false)`
    4. On error: fire Sonner `toast.error(t('error.generic'))` and do not exit edit mode

    Discard handler: `setIsEditing(false)` (no DB call).

    In READ mode (isEditing=false):
    - Render meaning text as `<p className="text-base">` (unchanged from current)
    - Render category badges using `t('category.' + cat)` instead of raw `cat` (D-16 fix)
    - Render Meaning.isActive as a read-only `<Badge variant={meaning.isActive ? 'default' : 'secondary'}>` with `t('active')` / `t('inactive')` label — do NOT render a Switch for isActive at the Meaning level (D-02: isActive is aggregated, not directly editable)
    - Render Edit button: `<Button variant="outline" onClick={enterEditMode}>{t('edit')}</Button>`

    In EDIT mode (isEditing=true):
    - Render `<textarea className="w-full border rounded-md p-2 text-base resize-none" value={editText} onChange={e => setEditText(e.target.value)} rows={3} />`
    - Render `<CategoryChips value={editCategories} onChange={setEditCategories} />` — reuse existing component with correct props
    - Render Save Changes button: `<Button disabled={isSaving} onClick={handleSave}>{isSaving ? <span className="animate-spin">...</span> : t('saveChanges')}</Button>`
    - Render Discard Changes button: `<Button variant="ghost" onClick={handleDiscard}>{t('discardChanges')}</Button>`

    Import `updateMeaning` from `@/db/services/meaning.service`. Remove any import of Calendar, Popover, CalendarIcon if present — these are replaced by input[type=date] in Task 2.

    Keep all existing useLiveQuery calls and routing logic (useParams, useNavigate) intact. Do not remove the linked word forms section — it will be extended in Task 2.
  </action>
  <verify>
    <automated>npm --prefix C:/my-code/vibe-coding/little-words run build 2>&1 | tail -5</automated>
    <fails_when>Non-zero exit code, or "error TS" in output, or import for Calendar/Popover still present</fails_when>
  </verify>
  <acceptance_criteria>
    - MeaningDetailPage renders Edit button in read mode
    - Entering edit mode shows textarea with meaning text and CategoryChips
    - Category badges in read mode use t('category.' + cat) per D-16
    - Meaning.isActive shown as a read-only Badge (no Switch at meaning level)
    - Save and Discard buttons present in edit mode with correct i18n keys
    - Calendar and Popover imports are removed
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Inline edit flow is wired end-to-end; category i18n fix applied; build passes.</done>
</task>

<task type="auto">
  <name>Per-pair expandable rows with date inputs, isActive Switch, and word-form navigation arrow</name>
  <read_first>
    - src/pages/MeaningDetailPage.tsx — current state after Task 1 (linked word forms section)
    - src/components/ui/collapsible.tsx — Collapsible, CollapsibleTrigger, CollapsibleContent exports and props
    - src/components/ui/switch.tsx — Switch props (checked, onCheckedChange, id)
    - src/db/services/wordFormMeaning.service.ts — confirm updatePairFields is exported (Plan 01 deliverable)
    - src/features/onboarding/components/OnboardingWizard.tsx — lines around the <input type="date"> to capture exact className pattern
  </read_first>
  <files>src/pages/MeaningDetailPage.tsx</files>
  <action>
    Replace the linked word-forms section in MeaningDetailPage.tsx with a list of expandable per-pair Collapsible rows. Each row represents one WordFormMeaning pair for this meaning.

    Data: the existing useLiveQuery already fetches linked word forms. Update the query to also fetch the pair rows for this meaning: `const pairs = useLiveQuery(() => db.wordFormMeanings.where('meaningId').equals(meaningId).toArray(), [meaningId])`. For each pair, look up the word form text from the already-fetched word forms array.

    Row structure (Collapsible per pair):
    - Collapsed state (CollapsibleTrigger): a row showing the word form text on the left, and a navigation arrow Button `→` on the right that calls `navigate('/word-forms/' + pair.wordFormId)` with aria-label={t('pair.goToWordForm')} — the arrow is NOT the collapsible trigger (clicking the arrow navigates; clicking anywhere else in the row toggles the collapsible)
    - Expanded state (CollapsibleContent): a compact two-column grid with:
      - Label `t('pair.firstObserved')` + `<input type="date" value={pair.firstObservationDate} onBlur={e => handlePairDateBlur(pair.id, 'firstObservationDate', e.target.value)} defaultValue={pair.firstObservationDate} />`
      - Label `t('pair.lastUsed')` + `<input type="date" value={pair.lastUsedDate} onBlur={e => handlePairDateBlur(pair.id, 'lastUsedDate', e.target.value)} defaultValue={pair.lastUsedDate} />`
      - Label `t('pair.active')` + `<Switch checked={pair.isActive} onCheckedChange={checked => handlePairActiveChange(pair.id, checked)} />`

    Use uncontrolled-style date inputs with onBlur (not onChange) to avoid saving on every keystroke — same blur-triggered save pattern as handleNotesBlur in DoctorReportPage.tsx.

    Handlers:
    - `handlePairDateBlur(pairId, field, value)`: if value is an empty string, do nothing (browser returns '' for invalid dates). Otherwise call `updatePairFields(pairId, { [field]: value })`. Wrap in try/catch; on error fire `toast.error(t('error.generic'))`.
    - `handlePairActiveChange(pairId, checked)`: call `updatePairFields(pairId, { isActive: checked })`. Wrap in try/catch.

    Import `updatePairFields` from `@/db/services/wordFormMeaning.service`.
    Import `Collapsible, CollapsibleTrigger, CollapsibleContent` from `@/components/ui/collapsible`.
    Import `Switch` from `@/components/ui/switch`.

    Date input className: reuse the pattern from OnboardingWizard — `"border rounded-md px-2 py-1 text-sm focus:outline-none focus:ring-2 focus:ring-ring w-full"`.

    Navigation arrow Button: use `variant="ghost" size="icon"` with `onClick={e => { e.stopPropagation(); navigate('/word-forms/' + pair.wordFormId) }}` — stopPropagation prevents the Collapsible from toggling when the arrow is tapped.

    Touch target: wrap the CollapsibleTrigger content in a `<div className="min-h-[44px] flex items-center">` to meet the 44px minimum touch target per UI-SPEC.
  </action>
  <verify>
    <automated>npm --prefix C:/my-code/vibe-coding/little-words run build 2>&1 | tail -5</automated>
    <fails_when>Non-zero exit code, or "error TS" in output</fails_when>
  </verify>
  <acceptance_criteria>
    - Each linked word form renders as a Collapsible row with a navigation arrow
    - Navigation arrow calls navigate('/word-forms/:id') and does NOT toggle the collapsible
    - Expanded row shows two date inputs (type="date") and a Switch for isActive
    - Date inputs use onBlur (not onChange) for save
    - Switch onCheckedChange calls updatePairFields immediately
    - Date inputs use className pattern matching OnboardingWizard's input[type=date]
    - Touch target on collapsed row is minimum 44px height
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>MeaningDetailPage fully rewritten: inline edit + per-pair expandable rows with dates, isActive, and navigation. All PREREL-01 and PREREL-02 items for this page are complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inline edit textarea → updateMeaning | User-supplied meaning text crosses into DB write |
| date input onBlur → updatePairFields | User-supplied date string crosses into DB write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03-01 | Tampering | updateMeaning call in Save handler | medium | mitigate | updateMeaning (Plan 01) validates non-empty text before DB write; component also disables Save when editText.trim() is empty |
| T-06-03-02 | Tampering | date input onBlur handler | low | mitigate | Handler guards against empty string (browser returns '' for invalid date input value); only valid YYYY-MM-DD strings are passed to updatePairFields |
| T-06-03-03 | Spoofing | navigate('/word-forms/' + pair.wordFormId) | low | accept | wordFormId is a number from the DB — not user-controlled text; no injection risk in hash router navigation |
</threat_model>

<verification>
1. `npm run build` exits 0 after both tasks
2. `grep -c "updateMeaning" src/pages/MeaningDetailPage.tsx` → at least 1
3. `grep -c "updatePairFields" src/pages/MeaningDetailPage.tsx` → at least 1
4. `grep -c "CollapsibleContent" src/pages/MeaningDetailPage.tsx` → at least 1
5. `grep -c "type=\"date\"" src/pages/MeaningDetailPage.tsx` → at least 2 (firstObservationDate + lastUsedDate)
6. `grep -v "Calendar\|Popover" src/pages/MeaningDetailPage.tsx` — no Calendar or Popover imports remain
</verification>

<success_criteria>
- MeaningDetailPage passes TypeScript build
- Inline edit mode for text and categories implemented per D-05
- Per-pair expandable rows implemented per D-06 with input[type=date] per D-08
- Category badge i18n fix applied per D-16
- Navigation arrow to /word-forms/:id works per D-06
</success_criteria>

<output>
Create `.planning/phases/06-pre-release-polish/06-03-SUMMARY.md` when done.
</output>
