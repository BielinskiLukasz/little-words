---
phase: 06-pre-release-polish
plan: 04
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/pages/WordFormDetailPage.tsx
autonomous: true
requirements:
  - PREREL-02

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Edit button on Word Form detail page enters inline edit mode for word form text (D-07)"
    - "Save Changes commits updated word form text via updateWordForm service call (D-07)"
    - "Discard Changes exits edit mode without saving (D-07)"
    - "Per-pair rows (linked meanings section) are collapsible; expanded state shows pair date inputs and isActive Switch (D-07 D-08)"
    - "Navigation arrow in per-pair row navigates to /meanings/:id (D-07)"
    - "Date inputs use <input type='date'> — no Calendar/Popover (D-08)"
    - "Pair date/isActive changes save on blur via updatePairFields (D-07)"
  artifacts:
    - src/pages/WordFormDetailPage.tsx
  key_links:
    - "updateWordForm imported from wordForm.service (Plan 01 deliverable)"
    - "updatePairFields imported from wordFormMeaning.service (Plan 01 deliverable)"
    - "Collapsible from @/components/ui/collapsible"
    - "Switch from @/components/ui/switch"
---

<objective>
Extend WordFormDetailPage with inline edit mode for the word form text (D-07) and per-pair expandable rows with date inputs and isActive Switch navigating to /meanings/:id.

Purpose: PREREL-02 — word form editing is the second major UAT blocker. Mirrors the MeaningDetailPage pattern established in Plan 03 applied to the word-form side.
Output: WordFormDetailPage.tsx updated with inline edit and collapsible pair rows.
</objective>

<execution_context>
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pre-release-polish/06-CONTEXT.md
@.planning/phases/06-pre-release-polish/06-UI-SPEC.md

Key decisions: D-07 D-08
Prior plan output: 06-01-SUMMARY.md (updateWordForm, updatePairFields)
</context>

<tasks>

<task type="tracer">
  <name>Tracer: Inline edit mode for word form text</name>
  <read_first>
    - src/pages/WordFormDetailPage.tsx — read entire file; map existing state, useLiveQuery, handlers, JSX structure, delete confirmation pattern
    - src/db/services/wordForm.service.ts — confirm updateWordForm is exported (Plan 01 deliverable)
    - src/components/ui/button.tsx — Button variants
  </read_first>
  <files>src/pages/WordFormDetailPage.tsx</files>
  <action>
    Add inline edit mode for the word form text field. Mirror the MeaningDetailPage pattern from Plan 03.

    State to add:
    - `const [isEditing, setIsEditing] = useState(false)`
    - `const [editForm, setEditForm] = useState('')`
    - `const [isSaving, setIsSaving] = useState(false)`

    Edit mode entry: Edit button calls `setIsEditing(true)`, `setEditForm(wordForm.form)`.

    Save handler:
    1. Set `isSaving(true)`
    2. `await updateWordForm(wordForm.id!, editForm)` — service normalizes to lowercase
    3. In `finally`: `setIsSaving(false)`, `setIsEditing(false)`
    4. On error: fire `toast.error(t('error.generic'))`

    Discard handler: `setIsEditing(false)`.

    In READ mode: render word form text as `<h1 className="text-2xl font-bold">` or the heading element currently used; render Edit button `<Button variant="outline">{t('edit')}</Button>`.

    In EDIT mode: render `<input type="text" className="border rounded-md px-2 py-1 text-2xl font-bold w-full focus:outline-none focus:ring-2 focus:ring-ring" value={editForm} onChange={e => setEditForm(e.target.value)} />` — single line input matching the heading style; render Save Changes and Discard Changes buttons with same disabled/spinner/aria pattern as Plan 03.

    Import `updateWordForm` from `@/db/services/wordForm.service`.

    Keep the existing delete confirmation AlertDialog pattern unchanged. Keep all useLiveQuery calls and routing intact.
  </action>
  <verify>
    <automated>npm --prefix C:/my-code/vibe-coding/little-words run build 2>&1 | tail -5</automated>
    <fails_when>Non-zero exit code, or "error TS" in output</fails_when>
  </verify>
  <acceptance_criteria>
    - WordFormDetailPage renders Edit button in read mode
    - Edit mode shows text input pre-filled with word form text
    - Save Changes calls updateWordForm; Discard Changes reverts without DB call
    - Save button shows spinner and is disabled while isSaving is true
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Inline edit for word form text is wired end-to-end; build passes.</done>
</task>

<task type="auto">
  <name>Per-pair expandable rows with date inputs, isActive Switch, and meaning navigation arrow</name>
  <read_first>
    - src/pages/WordFormDetailPage.tsx — current state after Task 1 (linked meanings section)
    - src/components/ui/collapsible.tsx — Collapsible, CollapsibleTrigger, CollapsibleContent exports
    - src/components/ui/switch.tsx — Switch props
    - src/db/services/wordFormMeaning.service.ts — confirm updatePairFields is exported
    - src/features/onboarding/components/OnboardingWizard.tsx — date input className pattern
  </read_first>
  <files>src/pages/WordFormDetailPage.tsx</files>
  <action>
    Replace the linked meanings section with expandable per-pair Collapsible rows. Mirror the pattern from Plan 03 but navigating to meanings.

    Data: add a useLiveQuery to fetch pairs for this word form: `const pairs = useLiveQuery(() => db.wordFormMeanings.where('wordFormId').equals(wordFormId).toArray(), [wordFormId])`. For each pair, look up the meaning text from the already-fetched meanings array.

    Row structure (Collapsible per pair) — identical structure to Plan 03 but with reversed navigation:
    - Collapsed (CollapsibleTrigger): meaning text on the left; navigation arrow Button `→` on the right with onClick navigate('/meanings/' + pair.meaningId) and aria-label={t('pair.goToMeaning')}; arrow uses e.stopPropagation() to avoid toggling Collapsible
    - Expanded (CollapsibleContent): two-column grid with:
      - Label t('pair.firstObserved') + input[type=date] value={pair.firstObservationDate} onBlur handler
      - Label t('pair.lastUsed') + input[type=date] value={pair.lastUsedDate} onBlur handler
      - Label t('pair.active') + Switch checked={pair.isActive} onCheckedChange handler

    Handlers (same pattern as Plan 03):
    - `handlePairDateBlur(pairId, field, value)`: guard empty string; call updatePairFields; catch with toast.error
    - `handlePairActiveChange(pairId, checked)`: call updatePairFields; catch with toast.error

    Import `updatePairFields` from `@/db/services/wordFormMeaning.service`.
    Import Collapsible, CollapsibleTrigger, CollapsibleContent from `@/components/ui/collapsible`.
    Import Switch from `@/components/ui/switch`.

    Defensive fallback for 0 linked pairs (word form with no meanings): render `<p className="text-muted-foreground text-sm">{t('wordForm.noMeaningsLinked')}</p>` — add this i18n key to both locale files (PL: "Brak powiązanych znaczeń" / EN: "No meanings linked") if it doesn't already exist.

    Touch targets: `<div className="min-h-[44px] flex items-center">` wrapper on CollapsibleTrigger content.
  </action>
  <verify>
    <automated>npm --prefix C:/my-code/vibe-coding/little-words run build 2>&1 | tail -5</automated>
    <fails_when>Non-zero exit code, or "error TS" in output</fails_when>
  </verify>
  <acceptance_criteria>
    - Linked meanings section renders Collapsible rows per pair
    - Navigation arrow calls navigate('/meanings/:id') with stopPropagation
    - Expanded row shows date inputs and isActive Switch
    - Date inputs use onBlur (not onChange) for save
    - Defensive empty state renders when no pairs exist
    - Touch target height is at least 44px on collapsed rows
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>WordFormDetailPage fully updated: inline edit for word form text + collapsible per-pair rows with navigation, per D-07 and D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inline edit text input → updateWordForm | User-supplied word form text crosses into DB write |
| date input onBlur → updatePairFields | User-supplied date string crosses into DB write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04-01 | Tampering | updateWordForm call in Save handler | medium | mitigate | updateWordForm (Plan 01) validates non-empty and normalizes to lowercase; component disables Save when editForm.trim() is empty |
| T-06-04-02 | Tampering | date input onBlur handler | low | mitigate | Handler guards against empty string before calling updatePairFields; browser date input returns YYYY-MM-DD or '' |
| T-06-04-03 | Spoofing | navigate('/meanings/' + pair.meaningId) | low | accept | meaningId is a number from DB — not user-controlled text |
</threat_model>

<verification>
1. `npm run build` exits 0 after both tasks
2. `grep -c "updateWordForm" src/pages/WordFormDetailPage.tsx` → at least 1
3. `grep -c "updatePairFields" src/pages/WordFormDetailPage.tsx` → at least 1
4. `grep -c "CollapsibleContent" src/pages/WordFormDetailPage.tsx` → at least 1
5. `grep -c "type=\"date\"" src/pages/WordFormDetailPage.tsx` → at least 2
</verification>

<success_criteria>
- WordFormDetailPage passes TypeScript build
- Inline edit for word form text implemented per D-07
- Per-pair expandable rows with date inputs and isActive per D-07/D-08
- Navigation arrow to /meanings/:id per D-07
</success_criteria>

<output>
Create `.planning/phases/06-pre-release-polish/06-04-SUMMARY.md` when done.
</output>
