---
phase: 06-pre-release-polish
plan: 06
type: tdd
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/features/doctor-report/services/reportGenerator.ts
  - src/features/doctor-report/services/reportGenerator.test.ts
  - src/pages/DoctorReportPage.tsx
autonomous: true
requirements:
  - PREREL-05

estimate:
  tokens: 80000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Age shown as 'X years Y months' for totalMonths >= 12; 'X months' for totalMonths < 12 (D-09)"
    - "Report includes per-category meaning list section with word-form count per meaning (D-10)"
    - "Report includes 5 most recently added active meanings sorted by firstUseDate desc (D-11)"
    - "Report includes 5 most-recently-forgotten inactive meanings sorted by lastUseDate desc (D-12)"
    - "DoctorReportPage queries wordFormMeanings and computes meaningWordFormCounts before calling generateReport"
    - "All pre-existing reportGenerator tests pass unchanged or updated for D-09 age format"
  artifacts:
    - src/features/doctor-report/services/reportGenerator.ts
    - src/features/doctor-report/services/reportGenerator.test.ts
    - src/pages/DoctorReportPage.tsx
  key_links:
    - "ReportInput.meaningWordFormCounts: Record<number, number> added for D-10"
    - "generateReport consumes meaningWordFormCounts to append per-category word-form counts"
    - "DoctorReportPage.tsx passes meaningWordFormCounts computed from db.wordFormMeanings.toArray()"
---

<objective>
Enhance the Doctor Report with four improvements (D-09 through D-12) using strict TDD: write failing tests first, implement to pass, refactor.

Purpose: PREREL-05 — the report is the app's primary value moment; the enhancements make it substantially more useful for specialist consultations.
Output: Updated reportGenerator.ts with four new behaviors, full test coverage, DoctorReportPage.tsx updated to supply pair counts.
</objective>

<execution_context>
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pre-release-polish/06-CONTEXT.md

Key decisions: D-09 D-10 D-11 D-12
</context>

<feature>
  <name>Doctor Report enhancements: age format, per-category list, recent additions, inactive section</name>
  <files>
    src/features/doctor-report/services/reportGenerator.ts,
    src/features/doctor-report/services/reportGenerator.test.ts,
    src/pages/DoctorReportPage.tsx
  </files>
  <read_first>
    - src/features/doctor-report/services/reportGenerator.ts — read entire file; current ReportInput type, generateReport function, age computation, all existing sections
    - src/features/doctor-report/services/reportGenerator.test.ts — read entire file; existing test stub t function, test data shapes, test descriptions
    - src/pages/DoctorReportPage.tsx — current queries (db.meanings.toArray(), db.wordForms.toArray()); how generateReport is called
  </read_first>
  <behavior>
    RED phase: Write all failing tests first, run `npx vitest run src/features/doctor-report/services/reportGenerator.test.ts` to confirm they fail, then commit: `test(06-06): add failing tests for D-09 D-10 D-11 D-12`.

    Test: D-09 age format — under 12 months
    - Given birthDate 11 months ago, generateReport output contains the result of t('report.months', {count: 11}) and does NOT contain t('report.yearsMonths')
    - Given birthDate exactly 12 months ago, output contains t('report.yearsMonths', {years: 1, months: 0}) and does NOT contain t('report.months')
    - Given birthDate 14 months ago, output contains t('report.yearsMonths', {years: 1, months: 2})
    - Given birthDate 24 months ago, output contains t('report.yearsMonths', {years: 2, months: 0})

    Test stub for D-09 (extend existing stub t function):
    ```
    const t = (key: string, opts?: Record<string, unknown>): string => {
      if (key === 'report.yearsMonths' && opts) return `${opts.years}y${opts.months}m`
      if (opts?.count !== undefined) return String(opts.count)
      return key
    }
    ```
    D-09 test assertions check the output string for the stub return value (e.g. '1y2m' for 14-month-old child).

    Test: D-10 per-category meaning list section
    - Given 3 active meanings in category 'Nouns' and 1 meaning in category 'Verbs', the report output contains the string returned by t('report.byCategory')
    - The report output contains the text of each active meaning under its category heading
    - Each meaning entry includes the word-form count in parentheses: meaningText + ' (' + count + ')' style
    - meaningWordFormCounts supplied as {[meaningId]: count} to ReportInput; meanings with count 0 show '(0)'
    - Meanings are sorted alphabetically within each category section
    - Categories with zero active meanings do NOT appear in the section

    Test: D-11 recently added
    - Given 7 active meanings with different firstUseDate values, the report output contains the string returned by t('report.recentAdditions')
    - Only the 5 meanings with the most recent firstUseDate appear in that section (sorted newest first)
    - Meanings with firstUseDate values beyond the top 5 do NOT appear in the recent additions section

    Test: D-12 inactive section
    - Given 7 inactive meanings with different lastUseDate values, the report output contains the string returned by t('report.recentForgotten')
    - Only the 5 meanings with the most recent lastUseDate appear in that section (sorted newest first by lastUseDate)
    - Active meanings do NOT appear in the inactive section

    All new test describe blocks are siblings to (not nested inside) existing describe blocks.
  </behavior>
  <implementation>
    GREEN phase: implement to make all failing tests pass.

    Changes to `ReportInput` type in `reportGenerator.ts`:
    - Add `meaningWordFormCounts: Record<number, number>` to the interface

    Changes to `generateReport`:

    1. Age format (D-09): Replace the current age computation block. Use `differenceInMonths` from date-fns (already imported). `const totalMonths = differenceInMonths(now, parseISO(profile.birthDate))`. If totalMonths < 12: `ageStr = t('report.months', { count: totalMonths })`. If totalMonths >= 12: `const years = Math.floor(totalMonths / 12); const months = totalMonths % 12; ageStr = t('report.yearsMonths', { years, months })`. Remove any reference to the old < 2 years threshold.

    2. Per-category list (D-10): After the existing "top categories" section, add a new section headed by `t('report.byCategory')`. Group active meanings by category; for each category with >= 1 active meaning (sorted categories alphabetically): append the category label, then for each active meaning in that category (sorted alphabetically by text): `'  - ' + meaning.text + ' (' + (meaningWordFormCounts[meaning.id!] ?? 0) + ')'`. Filter meanings by isActive=true.

    3. Recent additions (D-11): Add a new section headed by `t('report.recentAdditions')`. Filter active meanings; sort by firstUseDate descending; take the top 5; append each as `'  - ' + meaning.text`.

    4. Inactive section (D-12): Update the existing inactive section (if any) or add a new section headed by `t('report.recentForgotten')`. Filter inactive meanings (isActive=false); sort by lastUseDate descending; take the top 5; append each as `'  - ' + meaning.text`.

    Changes to `DoctorReportPage.tsx`:
    - Add query for wordFormMeanings: `const pairs = await db.wordFormMeanings.toArray()` (inside the existing data-fetching logic or useLiveQuery)
    - Compute `meaningWordFormCounts`: `const meaningWordFormCounts: Record<number, number> = {}; pairs.forEach(p => { meaningWordFormCounts[p.meaningId] = (meaningWordFormCounts[p.meaningId] ?? 0) + 1 })`
    - Pass `meaningWordFormCounts` to `generateReport({ ..., meaningWordFormCounts })`

    REFACTOR phase: clean up the age computation for readability; ensure section separator lines ('---') and blank lines are consistent with existing report formatting. Run tests again to confirm all pass. Commit: `refactor(06-06): clean up report generator sections`.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generateReport output → clipboard | Report text is generated from DB data and copied; no user input into the generator |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06-01 | Information Disclosure | Doctor Report clipboard | low | accept | Report contains child data that the parent explicitly generates and copies; existing behavior — no change to data exposure |
| T-06-06-02 | Denial of Service | generateReport with large meaning set | low | accept | Pure function over local data; bounded by device storage; no network calls; acceptable for v1 |
| T-06-06-03 | Tampering | meaningWordFormCounts input | low | accept | counts computed from DB data by DoctorReportPage — not user-supplied; generator treats them as display values only |
</threat_model>

<verification>
1. `npx vitest run C:/my-code/vibe-coding/little-words/src/features/doctor-report/services/reportGenerator.test.ts 2>&1 | tail -15` — all tests pass, zero failures
2. `npm --prefix C:/my-code/vibe-coding/little-words run build 2>&1 | tail -5` — exits 0
3. `grep -c "report.yearsMonths" src/features/doctor-report/services/reportGenerator.ts` → at least 1
4. `grep -c "report.byCategory" src/features/doctor-report/services/reportGenerator.ts` → at least 1
5. `grep -c "report.recentAdditions" src/features/doctor-report/services/reportGenerator.ts` → at least 1
6. `grep -c "report.recentForgotten" src/features/doctor-report/services/reportGenerator.ts` → at least 1
7. `grep -c "meaningWordFormCounts" src/pages/DoctorReportPage.tsx` → at least 1
</verification>

<success_criteria>
- All reportGenerator tests pass (existing + new)
- Age shown as 'X years Y months' for >= 12 months; 'X months' for < 12 months
- Per-category meaning list section present in generated report
- Recent additions section (top 5 by firstUseDate desc) present
- Recently forgotten section (top 5 inactive by lastUseDate desc) present
- DoctorReportPage passes meaningWordFormCounts to generateReport
- Zero build regressions
</success_criteria>

<output>
Create `.planning/phases/06-pre-release-polish/06-06-SUMMARY.md` when done.
</output>
