---
phase: 06-pre-release-polish
plan: 05
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/pages/PairsPage.tsx
  - src/shared/components/BottomNav.tsx
  - src/router/index.tsx
autonomous: true
requirements:
  - PREREL-04

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "BottomNav has 5 tabs in order: Dashboard, Meanings, Word Forms, Pairs, More (D-13)"
    - "PairsPage at /#/pairs is accessible from the new Pairs tab (D-13 D-14)"
    - "Each pairs list row shows two tappable chips: word form chip navigates to /word-forms/:id, meaning chip navigates to /meanings/:id (D-14)"
    - "Row body (not chips) taps expand a Collapsible showing firstObservationDate, lastUsedDate, isActive Badge (D-14)"
    - "Sort selector offers Newest first, A-Z by word form, A-Z by meaning; default is Newest first (D-15)"
    - "Empty state renders emptyHeading and emptyBody when no pairs exist (UI-SPEC)"
  artifacts:
    - src/pages/PairsPage.tsx
    - src/shared/components/BottomNav.tsx
    - src/router/index.tsx
  key_links:
    - "getPairsWithDetails() imported from wordFormMeaning.service (Plan 01 deliverable)"
    - "router adds pairs child route pointing to PairsPage"
    - "BottomNav activeTab detection uses useLocation().pathname"
---

<objective>
Create PairsPage, add Pairs tab to BottomNav, and wire the /pairs route — delivering the new 5th screen per D-13/D-14/D-15.

Purpose: PREREL-04 — the Pairs screen lets parents see all word-form/meaning connections and navigate to either side. It is the only screen in Phase 6 that requires new routing and navigation changes.
Output: PairsPage.tsx created; BottomNav.tsx updated to 5 tabs; router/index.tsx adds /pairs route.

Note on D-13 reversibility: Adding a 5th bottom nav tab is marked COSTLY (not one-way) in CONTEXT.md. No checkpoint:decision is required; this is flagged for developer awareness only. The tab order (Dashboard | Meanings | Word Forms | Pairs | More) matches D-13.
</objective>

<execution_context>
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pre-release-polish/06-CONTEXT.md
@.planning/phases/06-pre-release-polish/06-UI-SPEC.md

Key decisions: D-13 (costly — flagged only) D-14 D-15
Prior plan output: 06-01-SUMMARY.md (getPairsWithDetails)
</context>

<tasks>

<task type="tracer">
  <name>Tracer: PairsPage end-to-end — route, BottomNav tab, getPairsWithDetails, dual-chip rows</name>
  <read_first>
    - src/shared/components/BottomNav.tsx — read entire file; current 4-tab structure, tab definition pattern, icon imports, active state detection
    - src/router/index.tsx — read entire file; current child routes under RootLayout
    - src/pages/MeaningsPage.tsx — sort toggle pattern (useState for sort, useLiveQuery reactive list) to replicate
    - src/db/services/wordFormMeaning.service.ts — confirm getPairsWithDetails is exported (Plan 01 deliverable)
    - src/components/ui/badge.tsx — Badge variants
    - src/components/ui/button.tsx — Button variants
  </read_first>
  <files>src/pages/PairsPage.tsx, src/shared/components/BottomNav.tsx, src/router/index.tsx</files>
  <action>
    Step 1 — Create `src/pages/PairsPage.tsx`:

    Data:
    ```
    const pairs = useLiveQuery(() => getPairsWithDetails(), [])
    ```
    Loading state: if pairs === undefined, render `<p className="text-muted-foreground">{t('app.loading')}</p>`.
    Empty state: if pairs.length === 0, render:
    ```
    <div className="flex flex-col items-center gap-2 py-16">
      <h2 className="text-base font-medium">{t('pairs.emptyHeading')}</h2>
      <p className="text-sm text-muted-foreground text-center">{t('pairs.emptyBody')}</p>
    </div>
    ```

    Sort state: `const [sort, setSort] = useState<'newest' | 'azForm' | 'azMeaning'>('newest')`.

    Sort selector: a `<select>` element (same simple native approach as MeaningsPage sort toggle) with options:
    - value="newest": t('sort.newestFirst')
    - value="azForm": t('sort.azWordForm')
    - value="azMeaning": t('sort.azMeaning')

    Sorted list:
    - 'newest': sort pairs by firstObservationDate descending (new Date(b.firstObservationDate).getTime() - new Date(a.firstObservationDate).getTime())
    - 'azForm': sort by wordFormText ascending (localeCompare)
    - 'azMeaning': sort by meaningText ascending (localeCompare)

    Each row — flat div (NOT Collapsible yet in this tracer; Collapsible expansion is Task 2):
    - Word form chip: `<Button variant="outline" size="sm" className="truncate max-w-[45%]" onClick={() => navigate('/word-forms/' + pair.wordFormId)}>{pair.wordFormText}</Button>`
    - Meaning chip: `<Button variant="outline" size="sm" className="truncate max-w-[45%]" onClick={() => navigate('/meanings/' + pair.meaningId)}>{pair.meaningText}</Button>`
    - Both chips on one row with gap-2; row min-height 44px

    Page header: `<h1 className="text-2xl font-bold">{t('nav.pairs')}</h1>`

    Imports: `useNavigate` from 'react-router', `useLiveQuery` from 'dexie-react-hooks', `getPairsWithDetails` from '@/db/services/wordFormMeaning.service', `useTranslation` from 'react-i18next'.

    Step 2 — Update `src/router/index.tsx`:
    Add `{ path: 'pairs', element: <PairsPage /> }` to the children array under RootLayout, alongside existing dashboard/meanings/word-forms routes. Import PairsPage from '@/pages/PairsPage'.

    Step 3 — Update `src/shared/components/BottomNav.tsx`:
    Add a 5th tab entry for Pairs between "Word Forms" and "More". Use `GitBranch` lucide icon (represents connections/pairs). Tab definition:
    - path: '/pairs'
    - label: t('nav.pairs')
    - icon: GitBranch
    - import GitBranch from 'lucide-react'

    Insert between the Word Forms tab and the More tab so the order matches D-13: Dashboard | Meanings | Word Forms | Pairs | More.
  </action>
  <verify>
    <automated>npm --prefix C:/my-code/vibe-coding/little-words run build 2>&1 | tail -5</automated>
    <fails_when>Non-zero exit code, or "error TS" in output, or PairsPage not found by router</fails_when>
  </verify>
  <acceptance_criteria>
    - src/pages/PairsPage.tsx exists and exports a default React component
    - router/index.tsx includes `{ path: 'pairs', element: <PairsPage /> }`
    - BottomNav.tsx has 5 tab entries; Pairs tab is 4th (between Word Forms and More)
    - PairsPage renders dual-chip rows using getPairsWithDetails data
    - Sort selector with 3 options, default 'newest'
    - Empty state renders when pairs array is empty
    - `npm run build` exits 0
  </acceptance_criteria>
  <reversibility rating="costly">D-13: Adding 5th nav tab touches BottomNav, router, and all navigation-related tests. Not a one-way door but changing the tab structure later requires coordinated multi-file edits.</reversibility>
  <done>PairsPage, route, and BottomNav tab wired end-to-end. Pairs screen accessible from bottom nav.</done>
</task>

<task type="auto">
  <name>Collapsible row expansion on PairsPage + chip truncation</name>
  <read_first>
    - src/pages/PairsPage.tsx — current state after Task 1 (flat rows)
    - src/components/ui/collapsible.tsx — Collapsible, CollapsibleTrigger, CollapsibleContent exports
    - src/components/ui/badge.tsx — Badge for isActive in expanded row
  </read_first>
  <files>src/pages/PairsPage.tsx</files>
  <action>
    Wrap each pair row in a Collapsible so tapping the row body (not a chip) expands metadata.

    Structure after refactor:
    ```
    <Collapsible key={pair.id}>
      <div className="flex items-center gap-2 min-h-[44px]">
        <CollapsibleTrigger asChild>
          <div className="flex-1 flex items-center gap-2">
            {/* Both chips are here but CollapsibleTrigger wraps the row background */}
          </div>
        </CollapsibleTrigger>
        {/* Word form chip */}
        <Button ... onClick={e => { e.stopPropagation(); navigate('/word-forms/' + pair.wordFormId) }}>...</Button>
        {/* Meaning chip */}
        <Button ... onClick={e => { e.stopPropagation(); navigate('/meanings/' + pair.meaningId) }}>...</Button>
      </div>
      <CollapsibleContent>
        <div className="grid grid-cols-2 gap-2 px-2 pb-3 text-sm text-muted-foreground">
          <span>{t('pair.firstObserved')}: {pair.firstObservationDate}</span>
          <span>{t('pair.lastUsed')}: {pair.lastUsedDate}</span>
          <span className="col-span-2">
            <Badge variant={pair.isActive ? 'default' : 'secondary'}>
              {pair.isActive ? t('pair.active') : t('inactive')}
            </Badge>
          </span>
        </div>
      </CollapsibleContent>
    </Collapsible>
    ```

    The chip buttons must have `onClick={e => { e.stopPropagation(); navigate(...) }}` so chip taps navigate without toggling the Collapsible.

    Chip truncation: each chip gets `className="truncate max-w-[45vw] min-w-0"` so both chips fit on a single row even at 320px viewport. The 45vw cap is approximate — executor may adjust based on measured layout, but both chips must fit on one visible row at minimum 320px width.

    Import Collapsible, CollapsibleTrigger, CollapsibleContent from '@/components/ui/collapsible'.
    Import Badge from '@/components/ui/badge'.

    Check if `t('inactive')` key exists; if not, use `t('pair.active')` + logic or `String(pair.isActive)` as fallback — do not add new keys not in the UI-SPEC.
  </action>
  <verify>
    <automated>npm --prefix C:/my-code/vibe-coding/little-words run build 2>&1 | tail -5</automated>
    <fails_when>Non-zero exit code, or "error TS" in output</fails_when>
  </verify>
  <acceptance_criteria>
    - Each row wraps Collapsible; tapping the row background toggles expand/collapse
    - Chip onClick has e.stopPropagation() so chip taps do not toggle Collapsible
    - Expanded state shows firstObservationDate, lastUsedDate, and isActive Badge
    - Chip className includes truncate and a max-w cap
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>PairsPage delivers D-13 + D-14 + D-15 in full. Collapsible row expansion with chip navigation and sort selector complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PairsPage chip onClick → navigate | Route target uses pair.wordFormId / pair.meaningId from DB (numbers) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05-01 | Information Disclosure | PairsPage list | low | accept | Shows the same word-form and meaning data already visible on Meanings and Word Forms pages; no new data exposure |
| T-06-05-02 | Spoofing | navigate('/word-forms/' + pair.wordFormId) | low | accept | IDs are numbers from DB — not user-controlled strings; hash router navigation uses client-side only |
| T-06-05-03 | Denial of Service | Large pairs list (100+ rows) | low | accept | UI-SPEC backstop: native scroll for v1; no virtualization in scope; acceptable for typical app data volumes |
</threat_model>

<verification>
1. `npm run build` exits 0
2. `grep -c "PairsPage" src/router/index.tsx` → at least 1
3. `grep -c "pairs" src/shared/components/BottomNav.tsx` → at least 1
4. `grep -c "getPairsWithDetails" src/pages/PairsPage.tsx` → at least 1
5. `grep -c "CollapsibleContent" src/pages/PairsPage.tsx` → at least 1
6. `grep -c "sort" src/pages/PairsPage.tsx` → at least 3 (sort state + 3 option values)
</verification>

<success_criteria>
- PairsPage accessible at /#/pairs from new 5th BottomNav tab
- Dual-chip rows with word form and meaning chips, each navigating to the correct detail page
- Collapsible row expansion shows pair metadata
- Sort selector with 3 options; default newest first
- Empty state shown when no pairs exist
- TypeScript build passes
</success_criteria>

<output>
Create `.planning/phases/06-pre-release-polish/06-05-SUMMARY.md` when done.
</output>
