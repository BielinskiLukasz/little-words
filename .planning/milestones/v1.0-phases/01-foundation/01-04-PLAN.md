---
phase: 01-foundation
plan: 04
type: tdd
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/router/index.tsx
  - src/shared/components/RootLayout.tsx
  - src/shared/components/BottomNav.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/MeaningsPage.tsx
  - src/pages/WordFormsPage.tsx
  - src/pages/MorePage.tsx
  - src/pages/CategoriesPage.tsx
  - src/pages/TimelinePage.tsx
  - src/pages/DoctorReportPage.tsx
  - src/pages/SettingsPage.tsx
  - src/pages/OnboardingPage.tsx
  - src/stores/ui.store.ts
  - src/features/.gitkeep
  - src/shared/hooks/.gitkeep
  - src/shared/utils/.gitkeep
  - src/shared/components/BottomNav.test.tsx
autonomous: true
requirements:
  - FOUND-04
must_haves:
  truths:
    - createHashRouter is used (not createBrowserRouter or HashRouter component)
    - All 9 route paths are defined in the router (dashboard, meanings, word-forms, more, categories, timeline, doctor-report, settings, onboarding)
    - RootLayout renders the Outlet above BottomNav — main content scrolls, nav stays fixed at bottom
    - BottomNav has exactly 4 tabs with Lucide icons and translated labels (D-01)
    - NavLink active state uses text-primary class; inactive uses text-muted-foreground (D-13)
    - OnboardingPage is defined outside the RootLayout route — it renders without bottom nav (D-18)
    - MorePage lists links to Categories, Timeline, Doctor Report, Settings (D-02, D-04)
    - Folder skeleton is complete with .gitkeep files in empty feature directories (D-19)
    - Zustand ui.store stub is at src/stores/ui.store.ts (D-21)
  artifacts:
    - src/router/index.tsx
    - src/shared/components/RootLayout.tsx
    - src/shared/components/BottomNav.tsx
    - src/pages/ (all 9 page stubs)
    - src/stores/ui.store.ts
    - src/features/.gitkeep
    - src/shared/hooks/.gitkeep
    - src/shared/utils/.gitkeep
    - src/shared/components/BottomNav.test.tsx
  key_links:
    - src/router/index.tsx defines the hash router used by RouterProvider in App.tsx (Wave 4)
    - RootLayout Outlet position (above nav) determines scrolling behavior for all pages
    - BottomNav tab order (Dashboard, Meanings, Word Forms, More) is D-01 and must not change in future phases
---

<objective>
Wave 3: Write the full folder skeleton, hash router, RootLayout, BottomNav with 4 tabs, all 9 page stubs, and the Zustand ui store stub. Depends on Wave 2 (both plans) completing first.

Purpose: Prove that hash routing works end-to-end with translated tab labels. This is the visual skeleton — a real user can see the nav and navigate between routes.
Output: A working router with 9 routes, styled bottom nav with 4 tabs and active state, all placeholder pages, and a passing BottomNav test.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<feature>
  <name>Hash router shell — 4-tab BottomNav, 9 route stubs, folder skeleton</name>
  <files>src/router/index.tsx, src/shared/components/**, src/pages/**, src/stores/ui.store.ts</files>
  <behavior>
    - BottomNav.test.tsx: renders exactly 4 navigation links
    - BottomNav.test.tsx: the 4 links have accessible text matching the tab labels (Dashboard/Meanings/Word Forms/More for English locale)
    - BottomNav.test.tsx: each tab has an href containing the correct route path (/dashboard, /meanings, /word-forms, /more)
    - BottomNav.test.tsx: the active tab link receives the active CSS class (text-primary) when on its route
  </behavior>
  <implementation>
RED phase: Write src/shared/components/BottomNav.test.tsx with the behavior assertions above. Run vitest — tests MUST fail (BottomNav does not exist yet).

GREEN phase: Create all files below until vitest passes.

REFACTOR: Ensure tab array is defined as a const (not inline JSX), allowing future phases to extend it without touching render logic.

Implementation notes (from RESEARCH.md — use exactly):

STEP 1 — Create folder skeleton with .gitkeep files:
Create directories and empty .gitkeep files for:
  src/features/.gitkeep
  src/shared/hooks/.gitkeep
  src/shared/utils/.gitkeep

STEP 2 — Write all 9 page stubs:
Each file follows this exact pattern (substitute the page name):

src/pages/DashboardPage.tsx:
  export function DashboardPage() {
    return (
      <div className="flex h-full items-center justify-center p-6">
        <p className="text-muted-foreground">Dashboard coming soon</p>
      </div>
    );
  }

Create in the same pattern (adjust text to match page name):
  src/pages/MeaningsPage.tsx — "Meanings coming soon"
  src/pages/WordFormsPage.tsx — "Word Forms coming soon"
  src/pages/CategoriesPage.tsx — "Categories coming soon"
  src/pages/TimelinePage.tsx — "Timeline coming soon"
  src/pages/DoctorReportPage.tsx — "Doctor Report coming soon"
  src/pages/SettingsPage.tsx — "Settings coming soon"
  src/pages/OnboardingPage.tsx — "Onboarding coming soon" (D-18: styled placeholder)

src/pages/MorePage.tsx (D-02, D-04 — More screen lists Category/Timeline/Report/Settings links):
  import { Link } from 'react-router';
  export function MorePage() {
    return (
      <div className="flex flex-col gap-4 p-6">
        <Link to="/categories" className="text-foreground hover:text-primary">Categories</Link>
        <Link to="/timeline" className="text-foreground hover:text-primary">Timeline</Link>
        <Link to="/doctor-report" className="text-foreground hover:text-primary">Doctor Report</Link>
        <Link to="/settings" className="text-foreground hover:text-primary">Settings</Link>
      </div>
    );
  }

STEP 3 — Write src/stores/ui.store.ts (D-21):
  import { create } from 'zustand';
  interface UIState {
    _placeholder: null;  // Phase 2 adds: addWordSheetOpen, etc.
  }
  export const useUIStore = create<UIState>(() => ({
    _placeholder: null,
  }));

STEP 4 — Write src/shared/components/BottomNav.tsx (D-01, D-13):
- Import NavLink from 'react-router' (NOT react-router-dom)
- Import useTranslation from 'react-i18next'
- Import icons from 'lucide-react': LayoutDashboard, BookOpen, MessageSquare, MoreHorizontal
- Define tabs as a const array (typed as const):
  [
    { to: '/dashboard', icon: LayoutDashboard, labelKey: 'nav.dashboard' as const },
    { to: '/meanings', icon: BookOpen, labelKey: 'nav.meanings' as const },
    { to: '/word-forms', icon: MessageSquare, labelKey: 'nav.wordForms' as const },
    { to: '/more', icon: MoreHorizontal, labelKey: 'nav.more' as const },
  ]
- Render: <nav className="flex border-t border-border bg-background">
- For each tab: <NavLink to={to} className={({ isActive }) => isActive ? 'flex flex-1 flex-col items-center gap-1 py-2 text-xs font-medium text-primary' : 'flex flex-1 flex-col items-center gap-1 py-2 text-xs font-medium text-muted-foreground hover:text-foreground'}>
- Inside each NavLink: <Icon size={20} strokeWidth={1.5} /> and <span>{t(labelKey)}</span>
- Export function BottomNav()

STEP 5 — Write src/shared/components/RootLayout.tsx:
- Import Outlet from 'react-router'
- Import BottomNav from './BottomNav'
- Render:
  <div className="flex flex-col h-[100dvh]">
    <main className="flex-1 overflow-y-auto">
      <Outlet />
    </main>
    <BottomNav />
  </div>
- 100dvh (not 100vh) — dvh adjusts for iOS Safari toolbar; 100vh causes layout issues on mobile

STEP 6 — Write src/router/index.tsx (D-01 through D-04):
- Import createHashRouter, Navigate from 'react-router' (single package — NOT react-router-dom)
- Import RootLayout from '../shared/components/RootLayout'
- Import all 9 page components
- Define and export const router = createHashRouter([...]) with:
  Main layout route (path: '/'):
    - element: <RootLayout />
    - children:
      - { index: true, element: <Navigate to="/dashboard" replace /> }
      - { path: 'dashboard', element: <DashboardPage /> }
      - { path: 'meanings', element: <MeaningsPage /> }
      - { path: 'word-forms', element: <WordFormsPage /> }
      - { path: 'more', element: <MorePage /> }
      - { path: 'categories', element: <CategoriesPage /> }
      - { path: 'timeline', element: <TimelinePage /> }
      - { path: 'doctor-report', element: <DoctorReportPage /> }
      - { path: 'settings', element: <SettingsPage /> }
  Onboarding (OUTSIDE main layout — no bottom nav per D-18):
    - { path: '/onboarding', element: <OnboardingPage /> }

PITFALL: Always import from 'react-router' (the unified v7 package). In v7, react-router-dom is a compatibility shim. In v8, it is removed entirely.
PITFALL: Open question from RESEARCH.md A4 — if RouterProvider is not exported from 'react-router' at runtime, import from 'react-router/dom'. Check after install.

Test implementation:
- BottomNav.test.tsx wraps BottomNav in a MemoryRouter (from react-router) so NavLink resolves
- Use render from @testing-library/react, screen from @testing-library/react
- Use within() to check each nav item
- Check: screen.getAllByRole('link').length === 4
- Check: screen.getByText('Dashboard') is in the document (or Polish equivalent if default locale is pl)
- Note: i18n is initialized globally; the test will use Polish labels unless the test file imports i18n and sets language to 'en'
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router hash URL → rendered page | Hash fragment is client-parsed; no server involvement; XSS via hash is mitigated by React JSX escaping |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | Hash fragment (user-controlled URL) → route rendering | low | accept | React Router parses the hash and maps to registered routes; unregistered paths do not render; no dangerouslySetInnerHTML used in any page stub |
| T-04-02 | Information Disclosure | MorePage hard-coded link labels (not translated yet) | low | accept | MorePage uses hard-coded English labels in Phase 1 stub; Phase 2 wires useTranslation to the More screen for full i18n support |
| T-04-03 | Denial of Service | iOS Safari 100vh layout breaking the bottom nav | low | mitigate | RootLayout uses h-[100dvh] (not h-screen/h-[100vh]) to handle iOS Safari dynamic viewport height |
</threat_model>

<verification>
Run: npx vitest run src/shared/ --reporter=verbose

Expected: All tests in src/shared/components/BottomNav.test.tsx pass.
Also run: npx tsc --noEmit — must exit 0.

Manual smoke test (after Wave 4 completes App.tsx): npm run dev, navigate to /#/dashboard, verify 4 tabs visible with icons, verify active tab is highlighted.
</verification>

<success_criteria>
- createHashRouter used (not createBrowserRouter) — D-02 constraint
- All 9 routes defined in router
- BottomNav renders exactly 4 tabs with correct icons and translated labels (D-01, D-13)
- OnboardingPage is outside the RootLayout (no BottomNav during onboarding) — D-18
- Folder skeleton complete: src/features/, src/shared/hooks/, src/shared/utils/ have .gitkeep files (D-19)
- Zustand ui.store stub exists at src/stores/ (D-21)
- BottomNav.test.tsx passes
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
