---
phase: 02-onboarding-data-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/stores/ui.store.ts
  - src/i18n/locales/pl/common.json
  - src/i18n/locales/en/common.json
  - src/i18n/locales/pl/onboarding.json
  - src/i18n/locales/en/onboarding.json
  - src/components/ui/sheet.tsx
  - src/components/ui/collapsible.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/calendar.tsx
  - src/components/ui/popover.tsx
autonomous: true
requirements:
  - ENTRY-01
  - ENTRY-03
  - ONBD-01

must_haves:
  truths:
    - react-hook-form, zod, date-fns are importable from project source files
    - Shadcn Sheet, Collapsible, Badge, Calendar, Popover components exist in src/components/ui/
    - Zustand UIState has addWordSheetOpen boolean and setAddWordSheetOpen setter
    - i18n locale files have onboarding, addEntry, and settings namespaces with all required keys in both pl and en
    - npm run build and npm test both pass after this plan
  artifacts:
    - src/components/ui/sheet.tsx
    - src/components/ui/collapsible.tsx
    - src/components/ui/badge.tsx
    - src/components/ui/calendar.tsx
    - src/components/ui/popover.tsx
    - src/stores/ui.store.ts (updated)
    - src/i18n/locales/pl/common.json (updated)
    - src/i18n/locales/en/common.json (updated)
    - src/i18n/locales/pl/onboarding.json (populated)
    - src/i18n/locales/en/onboarding.json (populated)
  key_links:
    - Zustand addWordSheetOpen state consumed by AddEntryFAB (Plan 04) and AddEntrySheet (Plan 04)
    - i18n onboarding namespace loaded by OnboardingWizard (Plan 03)
    - Sheet component used by AddEntrySheet (Plan 04)
    - Collapsible used by MedicalContextSection (Plan 03)
    - Badge used by CategoryChips (Plan 04) and LanguageChips (Plan 03)
---

<objective>
Install missing form dependencies, add required Shadcn UI components, extend the Zustand store, and populate i18n locale files — providing the foundational setup that Plans 03 and 04 depend on.

Purpose: Phase 2 features (onboarding form, add-entry sheet, settings) require react-hook-form, zod, date-fns, and Shadcn Sheet/Collapsible/Badge/Calendar components. None of these exist yet. All other Phase 2 plans (03, 04, 05) depend on this setup.

Output: Extended package.json with new dependencies, 5 Shadcn UI component files, an updated Zustand store, and fully-populated i18n locale files for pl and en covering onboarding, addEntry, and settings strings.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-onboarding-data-entry/02-CONTEXT.md
@.planning/phases/02-onboarding-data-entry/02-RESEARCH.md
@src/stores/ui.store.ts
@src/i18n/index.ts
@src/i18n/locales/pl/common.json
@src/i18n/locales/en/common.json
@src/i18n/locales/pl/onboarding.json
@src/i18n/locales/en/onboarding.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install form dependencies and add Shadcn UI components</name>
  <files>package.json, src/components/ui/sheet.tsx, src/components/ui/collapsible.tsx, src/components/ui/badge.tsx, src/components/ui/calendar.tsx, src/components/ui/popover.tsx</files>
  <read_first>
    - package.json — verify react-hook-form, zod, date-fns are absent before installing
    - src/components/ui/button.tsx — confirm Shadcn path alias and import conventions to replicate
  </read_first>
  <action>
    Run in the project root:
    npm install react-hook-form @hookform/resolvers zod date-fns

    Then add Shadcn components (each command copies component source into src/components/ui/):
    npx shadcn@latest add sheet
    npx shadcn@latest add collapsible
    npx shadcn@latest add badge
    npx shadcn@latest add calendar
    npx shadcn@latest add popover

    These commands are non-interactive when run with the --yes flag if prompted. Verify each component file appears in src/components/ui/ after the command. Do NOT manually write these component files — Shadcn generates them with correct Tailwind v4 and Radix UI wiring.
  </action>
  <verify>
    <automated>cd "C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/little-words" && node -e "require('./node_modules/react-hook-form/dist/index.cjs.js'); require('./node_modules/zod/lib/index.cjs'); require('./node_modules/date-fns/index.js'); console.log('deps ok')" 2>/dev/null || node -e "import('./node_modules/react-hook-form/dist/index.esm.mjs').then(() => console.log('deps ok'))"</automated>
  </verify>
  <acceptance_criteria>
    - package.json lists react-hook-form, @hookform/resolvers, zod, date-fns under dependencies
    - src/components/ui/sheet.tsx exists and exports SheetContent, SheetHeader, SheetTitle
    - src/components/ui/collapsible.tsx exists and exports Collapsible, CollapsibleContent, CollapsibleTrigger
    - src/components/ui/badge.tsx exists and exports Badge and badgeVariants
    - src/components/ui/calendar.tsx exists and exports Calendar
    - src/components/ui/popover.tsx exists and exports Popover, PopoverContent, PopoverTrigger
    - npm test passes (all 28 existing tests remain green)
  </acceptance_criteria>
  <done>All 5 Shadcn components exist in src/components/ui/; react-hook-form, zod, date-fns, @hookform/resolvers appear in node_modules; npm test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Zustand UIState with add-entry sheet state</name>
  <files>src/stores/ui.store.ts</files>
  <read_first>
    - src/stores/ui.store.ts — read current placeholder state to understand what to replace
    - .planning/phases/02-onboarding-data-entry/02-PATTERNS.md §Zustand Store Pattern — exact store initialization pattern to follow
  </read_first>
  <action>
    Replace src/stores/ui.store.ts entirely. The new store must:
    - Define UIState interface with: addWordSheetOpen (boolean), iosInstallPromptSeen (boolean)
    - Add setters: setAddWordSheetOpen(open: boolean) and setIosInstallPromptSeen(seen: boolean)
    - Remove the _placeholder field
    - Export useUIStore using create from zustand
    - Use the exact pattern from 02-PATTERNS.md §Zustand Store Pattern (no persist middleware — ephemeral is correct)
    Import: import { create } from 'zustand'
    Export: export const useUIStore = create<UIState>((set) => ({ ... }))
  </action>
  <verify>
    <automated>cd "C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/little-words" && npx tsc --noEmit 2>&1 | grep -c "error" || true</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/ui.store.ts exports useUIStore
    - UIState interface contains addWordSheetOpen: boolean and iosInstallPromptSeen: boolean
    - UIState interface has setAddWordSheetOpen: (open: boolean) => void
    - UIState interface has setIosInstallPromptSeen: (seen: boolean) => void
    - _placeholder field is removed
    - npx tsc --noEmit exits 0
    - npm test passes
  </acceptance_criteria>
  <done>Zustand store has addWordSheetOpen and iosInstallPromptSeen state with typed setters; TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 3: Populate i18n locale files for Phase 2 features</name>
  <files>src/i18n/locales/pl/common.json, src/i18n/locales/en/common.json, src/i18n/locales/pl/onboarding.json, src/i18n/locales/en/onboarding.json</files>
  <read_first>
    - src/i18n/locales/pl/common.json — read existing keys to merge with, not replace
    - src/i18n/locales/en/common.json — read existing keys to merge with, not replace
    - src/i18n/locales/pl/onboarding.json — currently empty {}
    - src/i18n/locales/en/onboarding.json — currently empty {}
    - src/i18n/index.ts — confirms namespace registration (pl.onboarding, en.onboarding, pl.common, en.common)
    - .planning/phases/02-onboarding-data-entry/02-CONTEXT.md — D-01 through D-20 for exact UI strings needed
  </read_first>
  <action>
    Update the four locale files. Merge new keys into existing files — do not remove existing keys.

    For src/i18n/locales/pl/onboarding.json, add:
    - title: "Śledź słówka [child name]"
    - subtitle: "Stwórz profil dziecka, żeby zacząć"
    - field.name: "Imię dziecka"
    - field.birthDate: "Data urodzenia"
    - field.languages: "Języki używane w domu"
    - field.prematureBirth: "Wcześniak"
    - field.speechTherapy: "Logopeda"
    - field.neurologicalCare: "Opieka neurologiczna"
    - field.parentNotes: "Notatki dla lekarza"
    - section.medical: "Opcjonalnie: kontekst medyczny"
    - placeholder.name: "np. Zosia"
    - placeholder.otherLanguage: "Inny język"
    - button.getStarted: "Zaczynamy!"
    - button.saving: "Zapisuję..."
    - button.addLanguage: "Dodaj"
    - chips.polish: "Polski"
    - chips.english: "Angielski"
    - welcome.title: "Witaj, {{name}}!"
    - welcome.subtitle: "Śledźmy razem słówka {{name}}"
    - error.nameRequired: "Imię dziecka jest wymagane"
    - error.birthDateRequired: "Data urodzenia jest wymagana"
    - error.languageRequired: "Wybierz co najmniej jeden język"

    For src/i18n/locales/en/onboarding.json, add:
    - title: "Track {{name}}'s words"
    - subtitle: "Create a child profile to get started"
    - field.name: "Child's name"
    - field.birthDate: "Date of birth"
    - field.languages: "Languages used at home"
    - field.prematureBirth: "Premature birth"
    - field.speechTherapy: "Speech therapy"
    - field.neurologicalCare: "Neurological care"
    - field.parentNotes: "Notes for the doctor"
    - section.medical: "Optional: medical context"
    - placeholder.name: "e.g. Zosia"
    - placeholder.otherLanguage: "Other language"
    - button.getStarted: "Get started!"
    - button.saving: "Saving..."
    - button.addLanguage: "Add"
    - chips.polish: "Polish"
    - chips.english: "English"
    - welcome.title: "Welcome, {{name}}!"
    - welcome.subtitle: "Let's track {{name}}'s words together"
    - error.nameRequired: "Child's name is required"
    - error.birthDateRequired: "Date of birth is required"
    - error.languageRequired: "Select at least one language"

    For src/i18n/locales/pl/common.json, merge these new top-level keys (keep all existing keys):
    - addWord.title: "Dodaj słówko"
    - addWord.wordFormLabel: "Forma słowa (co dziecko powiedziało)"
    - addWord.meaningLabel: "Znaczenie"
    - addWord.dateLabel: "Pierwsza obserwacja"
    - addWord.categoriesLabel: "Kategorie"
    - addWord.addAnotherMeaning: "+ Dodaj kolejne znaczenie"
    - addWord.save: "Zapisz"
    - addWord.saving: "Zapisuję..."
    - addWord.existingLinked: "{{form}} — już połączone z: {{meanings}}"
    - addWord.createNew: "+ Utwórz \"{{text}}\""
    - settings.title: "Ustawienia"
    - settings.language: "Język"
    - settings.profile: "Profil"
    - settings.editProfile: "Edytuj profil"
    - settings.data: "Dane"
    - settings.about: "O aplikacji"
    - settings.exportJson: "Eksportuj JSON"
    - settings.importJson: "Importuj JSON"
    - settings.exportCsv: "Eksportuj CSV"
    - settings.comingSoon: "Wkrótce"
    - settings.version: "Wersja {{version}}"
    - profile.edit.title: "Edytuj profil"
    - profile.edit.save: "Zapisz zmiany"

    For src/i18n/locales/en/common.json, merge the same keys in English:
    - addWord.title: "Add a word"
    - addWord.wordFormLabel: "Word form (what the child said)"
    - addWord.meaningLabel: "Meaning"
    - addWord.dateLabel: "First observed"
    - addWord.categoriesLabel: "Categories"
    - addWord.addAnotherMeaning: "+ Add another meaning"
    - addWord.save: "Save"
    - addWord.saving: "Saving..."
    - addWord.existingLinked: "{{form}} — already linked to: {{meanings}}"
    - addWord.createNew: "+ Create \"{{text}}\""
    - settings.title: "Settings"
    - settings.language: "Language"
    - settings.profile: "Profile"
    - settings.editProfile: "Edit profile"
    - settings.data: "Data"
    - settings.about: "About"
    - settings.exportJson: "Export JSON"
    - settings.importJson: "Import JSON"
    - settings.exportCsv: "Export CSV"
    - settings.comingSoon: "Coming soon"
    - settings.version: "Version {{version}}"
    - profile.edit.title: "Edit profile"
    - profile.edit.save: "Save changes"

    Write the files as valid JSON. Ensure dot-notation keys become nested JSON objects (e.g., "addWord.title" becomes { "addWord": { "title": "..." } }).
  </action>
  <verify>
    <automated>cd "C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/little-words" && node -e "const pl=require('./src/i18n/locales/pl/onboarding.json');const en=require('./src/i18n/locales/en/onboarding.json');console.log(pl.title,en.title,pl.welcome?.title,en.welcome?.title)" 2>/dev/null || node --input-type=module -e "import pl from './src/i18n/locales/pl/onboarding.json' assert {type:'json'};console.log(pl.title,pl.welcome)"</automated>
  </verify>
  <acceptance_criteria>
    - src/i18n/locales/pl/onboarding.json is valid JSON with title, subtitle, field.name (nested), welcome.title, error.nameRequired
    - src/i18n/locales/en/onboarding.json is valid JSON with the same keys in English
    - src/i18n/locales/pl/common.json retains all existing keys and has new addWord and settings nested objects
    - src/i18n/locales/en/common.json retains all existing keys and has new addWord and settings nested objects
    - npm test passes (existing i18n tests still green)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All four locale files are valid JSON with Phase 2 keys for both Polish and English; no existing keys removed; all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Third-party packages installed via npm install |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-02-01-SC | Tampering | npm install (react-hook-form, zod, date-fns) | medium | mitigate | All three packages are established, high-download-count libraries (react-hook-form: 10M+/wk, zod: 8M+/wk, date-fns: 15M+/wk) — verified via npmjs.com before planning; slopcheck passes |
| T-02-01-SC2 | Tampering | npx shadcn@latest | medium | mitigate | Shadcn/UI is the project-standard component library per CLAUDE.md; command generates local source files; no runtime dependency added; files should be reviewed post-generation |
| T-02-01-I1 | Information Disclosure | i18n locale files | low | accept | Translation strings contain no secrets or PII; locale files are bundled and publicly visible in the app |
</threat_model>

<verification>
After all tasks complete, run:
1. npm test — all 28 existing tests must pass
2. npx tsc --noEmit — TypeScript must compile cleanly
3. npm run build — build must succeed

Spot check: grep -r "addWordSheetOpen" src/stores/ui.store.ts — should find the field
Spot check: ls src/components/ui/ — should list button.tsx, sheet.tsx, collapsible.tsx, badge.tsx, calendar.tsx, popover.tsx
</verification>

<success_criteria>
- All 5 Shadcn components created in src/components/ui/
- react-hook-form, @hookform/resolvers, zod, date-fns in package.json dependencies
- Zustand store has addWordSheetOpen + iosInstallPromptSeen with typed setters
- i18n locale files fully populated for onboarding, addEntry, and settings namespaces in both pl and en
- All 28 existing tests pass; TypeScript clean; build succeeds
</success_criteria>

<output>
Create `.planning/phases/02-onboarding-data-entry/02-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**New files:**
- `src/components/ui/sheet.tsx` — Shadcn Sheet component (SheetContent, SheetHeader, SheetTitle, SheetFooter, SheetClose)
- `src/components/ui/collapsible.tsx` — Shadcn Collapsible (Collapsible, CollapsibleContent, CollapsibleTrigger)
- `src/components/ui/badge.tsx` — Shadcn Badge (Badge, badgeVariants)
- `src/components/ui/calendar.tsx` — Shadcn Calendar (Calendar)
- `src/components/ui/popover.tsx` — Shadcn Popover (Popover, PopoverContent, PopoverTrigger)

**Modified files:**
- `src/stores/ui.store.ts` — exports useUIStore with UIState: { addWordSheetOpen, iosInstallPromptSeen, setAddWordSheetOpen, setIosInstallPromptSeen }
- `src/i18n/locales/pl/onboarding.json` — populated with all onboarding strings in Polish
- `src/i18n/locales/en/onboarding.json` — populated with all onboarding strings in English
- `src/i18n/locales/pl/common.json` — extended with addWord.* and settings.* and profile.edit.* keys
- `src/i18n/locales/en/common.json` — extended with addWord.* and settings.* and profile.edit.* keys
</output>
