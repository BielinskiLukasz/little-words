---
phase: 02-onboarding-data-entry
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/db/services/wordForm.service.ts
  - src/db/services/meaning.service.ts
  - src/db/services/wordFormMeaning.service.ts
  - src/db/services/wordEntry.service.ts
  - src/db/services/wordForm.service.test.ts
  - src/db/services/meaning.service.test.ts
  - src/db/services/wordEntry.service.test.ts
autonomous: true
requirements:
  - ENTRY-02
  - ONBD-03
  - ENTRY-01

must_haves:
  truths:
    - findOrCreateWordForm returns existing ID when form text matches (case-insensitive) and does not create a duplicate
    - findOrCreateWordForm creates a new word form when no match exists and returns new ID
    - searchMeanings returns up to 10 matching meanings for a prefix using case-insensitive index; returns empty for empty prefix
    - addWordEntry atomically creates word form, meanings, and junction rows; triggers navigator.storage.persist() after first entry
    - linkMeaningToWordForm is idempotent (no duplicate junction rows for the same pair)
    - All service functions are pure async (no React dependencies)
  artifacts:
    - src/db/services/wordForm.service.ts (findOrCreateWordForm added)
    - src/db/services/meaning.service.ts (searchMeanings added)
    - src/db/services/wordFormMeaning.service.ts (linkMeaningToWordForm idempotent)
    - src/db/services/wordEntry.service.ts (new orchestrator)
    - src/db/services/wordForm.service.test.ts
    - src/db/services/meaning.service.test.ts
    - src/db/services/wordEntry.service.test.ts
  key_links:
    - findOrCreateWordForm called by wordEntry.service.ts and WordFormInput debounced preview (Plan 04)
    - searchMeanings called by useMeaningSearch hook (Plan 04)
    - addWordEntry called by useAddEntry hook (Plan 04)
    - navigator.storage.persist() triggered inside addWordEntry after junction row creation
---

<objective>
Implement and TDD-verify the service layer functions that Phase 2 data entry depends on: findOrCreateWordForm (atomic, case-insensitive), searchMeanings (debounced autocomplete query), addWordEntry (orchestrator with atomicity and storage persist), and idempotent linkMeaningToWordForm.

Purpose: The add-entry flow (Plan 04) and onboarding (Plan 03) both call services that only have stubs. TDD catches atomicity bugs and find-or-create race conditions before UI is built on top of them.

Output: 3 updated service files + 1 new orchestrator service file + 3 test files with full RED→GREEN→REFACTOR cycle.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-onboarding-data-entry/02-RESEARCH.md
@src/db/services/wordForm.service.ts
@src/db/services/meaning.service.ts
@src/db/services/wordFormMeaning.service.ts
@src/db/db.ts
@src/db/schema.ts
@src/db/types.ts
@src/test-setup.ts
</context>

<feature>
  <name>Service Layer: findOrCreateWordForm, searchMeanings, addWordEntry</name>
  <files>
    src/db/services/wordForm.service.ts,
    src/db/services/wordForm.service.test.ts,
    src/db/services/meaning.service.ts,
    src/db/services/meaning.service.test.ts,
    src/db/services/wordFormMeaning.service.ts,
    src/db/services/wordEntry.service.ts,
    src/db/services/wordEntry.service.test.ts
  </files>
  <behavior>
    wordForm.service:
    - findOrCreateWordForm('Pa') where db has { form: 'pa', id: 1 } → returns 1 (existing, case-insensitive)
    - findOrCreateWordForm('ba') where db is empty → returns new number ID (created)
    - findOrCreateWordForm('PA') same as 'pa' → returns same ID as existing 'pa' row
    - addWordForm stub remains untouched (backward compat)

    meaning.service:
    - searchMeanings('') → returns []
    - searchMeanings('go') where db has meanings ['goodbye', 'go away', 'horse'] → returns ['goodbye', 'go away'] (prefix match, max 10)
    - searchMeanings('go') where db is empty → returns []
    - addMeaning stub remains untouched (backward compat)

    wordEntry.service (orchestrator — new file):
    - addWordEntry({ wordForm: 'pa', meanings: [{ text: 'goodbye', categories: ['Social Communication'], firstUseDate: '2025-01-01' }] }) creates 1 WordForm + 1 Meaning + 1 WordFormMeaning and returns { wordFormId, meaningIds: [number] }
    - addWordEntry with same wordForm 'pa' called twice creates only 1 WordForm row (find-or-create)
    - addWordEntry linking same meaning to same word form twice creates only 1 junction row (idempotent)
    - addWordEntry throws if wordForm is empty string

    wordFormMeaning.service:
    - linkMeaningToWordForm(1, 1) where link already exists → does NOT insert duplicate, returns void
    - linkMeaningToWordForm(1, 2) where link does not exist → inserts new junction row
    - Existing linkMeaning and unlinkMeaning exports remain untouched
  </behavior>
  <implementation>
    Step 1 (RED): Write failing tests in wordForm.service.test.ts, meaning.service.test.ts, wordEntry.service.test.ts. Use fake-indexeddb (already installed). Tests must fail because findOrCreateWordForm and searchMeanings do not exist yet, and wordEntry.service.ts does not exist. Run npm test to confirm failures.

    Commit: test(02-02): add failing tests for findOrCreateWordForm, searchMeanings, addWordEntry

    Step 2 (GREEN): Implement the functions.

    In src/db/services/wordForm.service.ts, add findOrCreateWordForm:
    - Export async function findOrCreateWordForm(formText: string): Promise&lt;number&gt;
    - Uses db.transaction('rw', db.wordForms, ...) for atomicity
    - Normalizes: const normalized = formText.toLowerCase()
    - Queries: db.wordForms.where('form').equals(normalized).first()
    - If found: return existing.id!
    - If not found: return db.wordForms.add({ form: formText, createdAt: new Date().toISOString() }) as Promise&lt;number&gt;
    - Throws if formText.trim().length === 0

    In src/db/services/meaning.service.ts, add searchMeanings:
    - Export async function searchMeanings(prefix: string): Promise&lt;Meaning[]&gt;
    - If prefix.length === 0 return []
    - return db.meanings.where('text').startsWithIgnoreCase(prefix).limit(10).toArray()
    - Existing addMeaning and toggleMeaningActive must remain unchanged

    In src/db/services/wordFormMeaning.service.ts, update linkMeaning to be idempotent:
    - Rename existing linkMeaning to linkMeaningToWordForm (keep old linkMeaning as alias for backward compat)
    - Before inserting, query: db.wordFormMeanings.where('[wordFormId+meaningId]').equals([wordFormId, meaningId]).first()
    - If exists: return (do nothing)
    - If not: db.wordFormMeanings.add({ wordFormId, meaningId })
    - Existing unlinkMeaning must remain unchanged

    Create src/db/services/wordEntry.service.ts:
    - Import findOrCreateWordForm, addMeaning (from meaning.service), linkMeaningToWordForm (from wordFormMeaning.service), db
    - Export interface WordEntryMeaningInput { text: string; categories: Category[]; firstUseDate?: string }
    - Export interface WordEntryInput { wordForm: string; meanings: WordEntryMeaningInput[] }
    - Export interface WordEntryResult { wordFormId: number; meaningIds: number[] }
    - Export async function addWordEntry(data: WordEntryInput): Promise&lt;WordEntryResult&gt;
    - Validates: if data.wordForm.trim().length === 0, throw new Error('Word form cannot be empty')
    - Validates: if data.meanings.length === 0, throw new Error('At least one meaning is required')
    - Calls findOrCreateWordForm(data.wordForm)
    - For each meaning: calls addMeaning({ text, categories, firstUseDate, lastUseDate: firstUseDate, isActive: true })
    - For each meaningId: calls linkMeaningToWordForm(wordFormId, meaningId)
    - After all links: check total count: if ((await db.wordFormMeanings.count()) === 1) { navigator.storage?.persist?.() }
    - Returns { wordFormId, meaningIds }

    Run npm test to confirm GREEN.

    Commit: feat(02-02): implement findOrCreateWordForm, searchMeanings, addWordEntry

    Step 3 (REFACTOR): Review for code clarity. Extract the navigator.storage.persist guard to a separate inline helper comment. No logic changes. Run npm test to confirm still GREEN.

    Commit: refactor(02-02): clarify service layer atomicity and storage persist guard
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component → service | Form input text from user passed to service functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-02-02-T1 | Tampering | findOrCreateWordForm input | low | mitigate | Validate that formText is non-empty before transaction; Dexie auto-escapes values in IndexedDB; React JSX escapes on render |
| T-02-02-T2 | Tampering | duplicate junction rows | medium | mitigate | linkMeaningToWordForm checks for existing pair before insert; compound index [wordFormId+meaningId] enforces uniqueness at query layer |
| T-02-02-I1 | Information Disclosure | navigator.storage.persist | low | accept | Permission result (granted/denied) is not logged; failure is silent as specified; no data leaves the device |
</threat_model>

<verification>
After RED→GREEN→REFACTOR cycle:
npm test — must show all tests passing including new service tests
npx tsc --noEmit — must exit 0

Spot check: grep -c "findOrCreateWordForm" src/db/services/wordForm.service.ts — expect 1
Spot check: grep -c "searchMeanings" src/db/services/meaning.service.ts — expect 1
Spot check: test file count: ls src/db/services/*.test.ts — must show 3 test files
</verification>

<success_criteria>
- RED commit: failing tests for findOrCreateWordForm, searchMeanings, addWordEntry
- GREEN commit: all new service functions implemented, all tests pass
- REFACTOR commit: code clarity improvements, tests still pass
- Total test count increases from 28 to at least 40 (12+ new service tests)
- TypeScript strict mode: npx tsc --noEmit exits 0
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/02-onboarding-data-entry/02-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**New files:**
- `src/db/services/wordEntry.service.ts` — exports addWordEntry(WordEntryInput): Promise&lt;WordEntryResult&gt;, WordEntryInput, WordEntryMeaningInput, WordEntryResult
- `src/db/services/wordForm.service.test.ts` — tests for findOrCreateWordForm (find existing, create new, case-insensitive, idempotent)
- `src/db/services/meaning.service.test.ts` — tests for searchMeanings (prefix match, empty string guard, limit 10)
- `src/db/services/wordEntry.service.test.ts` — tests for addWordEntry (atomicity, find-or-create, idempotent links, validation)

**Modified files:**
- `src/db/services/wordForm.service.ts` — adds findOrCreateWordForm(formText: string): Promise&lt;number&gt;
- `src/db/services/meaning.service.ts` — adds searchMeanings(prefix: string): Promise&lt;Meaning[]&gt;
- `src/db/services/wordFormMeaning.service.ts` — adds idempotent linkMeaningToWordForm(wordFormId, meaningId): Promise&lt;void&gt;; keeps existing linkMeaning and unlinkMeaning

**Types/functions exported:**
- findOrCreateWordForm — from wordForm.service.ts
- searchMeanings — from meaning.service.ts
- linkMeaningToWordForm — from wordFormMeaning.service.ts
- addWordEntry — from wordEntry.service.ts
- WordEntryInput — interface from wordEntry.service.ts
- WordEntryMeaningInput — interface from wordEntry.service.ts
- WordEntryResult — interface from wordEntry.service.ts
</output>
