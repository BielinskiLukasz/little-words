---
phase: 05-pwa-polish
plan: 01
subsystem: pwa
tags: [pwa, vite-plugin-pwa, service-worker, i18n, sonner, manifest]

requires:
  - phase: 04-doctor-report-data-management
    provides: Sonner toast pattern established; i18n locale files in place

provides:
  - PWA service worker update prompt flow (registerType: 'prompt' + useRegisterSW)
  - Manifest branding: theme_color/background_color '#0D9488', short_name 'Little Words', privacy-first description
  - i18n keys pwa.updateAvailable and pwa.refresh in both pl/en locales

affects: [05-pwa-polish plan-02 (icons), 05-pwa-polish plan-03 (CI/CD)]

actuals:
  tokens: 950
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "useRegisterSW from virtual:pwa-register/react wired in App.tsx for SW update control"
    - "Persistent Sonner toast (duration: Infinity) with action prop for user-triggered SW update"

key-files:
  created: []
  modified:
    - vite.config.ts
    - src/App.tsx
    - src/i18n/locales/pl/common.json
    - src/i18n/locales/en/common.json

key-decisions:
  - "registerType: 'prompt' — SW waits in 'waiting' state until updateServiceWorker(true) called; prevents silent mid-session reload (D-04)"
  - "useRegisterSW mounted unconditionally at App() top level — single mount point, no per-page logic (D-05)"
  - "Toast duration Infinity — update prompt cannot disappear by timeout; user must act (D-07)"
  - "No Dismiss button on update toast — single Refresh action only per D-06"

patterns-established:
  - "PWA update hook: useRegisterSW({ onNeedRefresh }) -> toast(t('pwa.updateAvailable'), { action, duration: Infinity })"

requirements-completed:
  - PWA-01
  - PWA-02
  - PWA-03

coverage:
  - id: D1
    description: "registerType switched to 'prompt' in vite.config.ts — SW update waits for user consent"
    requirement: PWA-03
    verification:
      - kind: other
        ref: "grep -c \"registerType: 'prompt'\" vite.config.ts → 1"
        status: pass
    human_judgment: false
  - id: D2
    description: "Manifest branding updated: theme_color '#0D9488', background_color '#0D9488', short_name 'Little Words', privacy-first description"
    requirement: PWA-02
    verification:
      - kind: other
        ref: "grep -c '0D9488' vite.config.ts → 2; grep short_name vite.config.ts shows 'Little Words'"
        status: pass
    human_judgment: false
  - id: D3
    description: "pwa.updateAvailable and pwa.refresh i18n keys present in both pl/en locale files"
    requirement: PWA-03
    verification:
      - kind: other
        ref: "node -e \"const j=require('./src/i18n/locales/pl/common.json'); console.log(j.pwa)\" → {updateAvailable, refresh}"
        status: pass
    human_judgment: false
  - id: D4
    description: "useRegisterSW wired in App.tsx; onNeedRefresh fires persistent Sonner toast with Refresh action"
    requirement: PWA-03
    verification:
      - kind: other
        ref: "grep checks: useRegisterSW×2, virtual:pwa-register/react×1, duration: Infinity×1, updateServiceWorker(true)×1"
        status: pass
    human_judgment: false
  - id: D5
    description: "npm run build passes with sw.js and manifest.webmanifest in dist/"
    requirement: PWA-01
    verification:
      - kind: other
        ref: "npm run build exit code 0; dist/sw.js and dist/manifest.webmanifest present"
        status: pass
    human_judgment: false

duration: 20min
completed: 2026-08-31
status: complete
---

# Phase 5 Plan 01: PWA Update Notification Flow Summary

**PWA update prompt wired end-to-end: registerType 'prompt' + useRegisterSW persistent Sonner toast + manifest teal branding (#0D9488)**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-08-31T17:40:00Z
- **Completed:** 2026-08-31T18:05:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Switched vite.config.ts registerType to 'prompt' (D-04) — service worker now waits for explicit user action before applying updates
- Updated manifest branding: theme_color and background_color to '#0D9488' (teal), short_name to 'Little Words', description to privacy-first copy (D-10, D-11, D-12)
- Added pwa.updateAvailable and pwa.refresh i18n keys to both pl/en common.json locale files (D-07)
- Wired useRegisterSW in App.tsx with persistent Sonner toast (duration: Infinity) and single Refresh action calling updateServiceWorker(true) (D-05, D-06)
- Build passes: dist/sw.js and dist/manifest.webmanifest generated by Workbox generateSW strategy

## Task Commits

1. **Task 1: Update vite.config.ts manifest + add PWA i18n keys** - `5a4d613` (feat)
2. **Task 2: Wire useRegisterSW in App.tsx with persistent Sonner toast** - `bbc9d87` (feat)

## Files Created/Modified

- `vite.config.ts` — registerType 'prompt', theme_color/background_color '#0D9488', short_name 'Little Words', updated description
- `src/App.tsx` — useRegisterSW hook with onNeedRefresh Sonner toast
- `src/i18n/locales/pl/common.json` — added pwa.updateAvailable and pwa.refresh
- `src/i18n/locales/en/common.json` — added pwa.updateAvailable and pwa.refresh

## Decisions Made

- registerType 'prompt' chosen over 'autoUpdate' to give the user explicit control — no silent mid-session page reloads
- useRegisterSW mounted at App() top level (not in RootLayout) for a single hook mount point with no per-page logic
- Toast duration Infinity ensures the update prompt persists until user acts (no auto-dismiss)
- No Dismiss button on the update toast per D-06 — one action only (Refresh)

## Deviations from Plan

None — plan executed exactly as written. All four targeted edits (vite.config.ts × 4 fields, two locale files, App.tsx) applied without issues.

## Issues Encountered

**Pre-existing test failure (out of scope):** `src/features/doctor-report/services/reportGenerator.test.ts` has one failing test at line 198 (`expect(result).toContain('report.newInLast3Months: 0')`). This is a date-sensitive assertion unrelated to our changes (we touched none of the report generation code). The failure was present before this plan's commits and is deferred to the plan that owns reportGenerator.test.ts. Logged as deferred item.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Plan 05-01 complete: PWA update prompt flow proven end-to-end
- Plan 05-02 (icons) can now safely modify vite.config.ts manifest icons array — the registerType and branding fields are locked in
- dist/sw.js and dist/manifest.webmanifest confirm Workbox precaching works; plan 05-02 will extend with PNG icon entries

---
*Phase: 05-pwa-polish*
*Completed: 2026-08-31*
