---
phase: quick-260914-gm6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/PairsPage.tsx
  - src/pages/DoctorReportPage.tsx
  - src/features/doctor-report/services/reportGenerator.ts
  - src/i18n/locales/en/common.json
  - src/i18n/locales/pl/common.json
autonomous: true
requirements:
  - UAT-11-pairs-table
  - UAT-16-additions-limit
  - UAT-17-forgotten-limit

estimate:
  tokens: 25000
  raw_tokens: 15000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - PairsPage renders a 5-column table (word form, meaning, first observed, last used, active) replacing the Collapsible card list
    - PairsPage has a Download CSV button that downloads sorted pairs as a UTF-8 CSV with BOM
    - DoctorReportPage has two number inputs (recent additions limit, recently forgotten limit) defaulting to 5
    - DoctorReportPage has a Regenerate button that applies the current input values and updates the report text
    - All new user-visible strings exist in both en and pl common.json
    - TypeScript build passes with no new errors
  artifacts:
    - src/pages/PairsPage.tsx (table layout, CSV export)
    - src/pages/DoctorReportPage.tsx (limit inputs + regenerate button)
    - src/features/doctor-report/services/reportGenerator.ts (recentAdditionsLimit, recentForgottenLimit params)
    - src/i18n/locales/en/common.json (pairs.downloadCsv; report.recentAdditionsLabel, report.recentForgottenLabel, report.regenerate; updated report.recentAdditions, report.recentForgotten with count interpolation)
    - src/i18n/locales/pl/common.json (same additions)
  key_links:
    - DoctorReportPage appliedAdditionsLimit/appliedForgottenLimit -> generateReport recentAdditionsLimit/recentForgottenLimit -> .slice(0, limit)
    - PairsPage CSV: sorted pairs array -> Blob with UTF-8 BOM -> createObjectURL -> programmatic anchor click -> revokeObjectURL
---

<objective>
Deliver three deferred UAT follow-ups from phase 06 as a single atomic quick task:
(1) replace the PairsPage Collapsible list with a proper sortable table and add a CSV download;
(2) make the Doctor Report "recently added" count configurable with a number input + Regenerate button;
(3) make the Doctor Report "recently forgotten" count configurable the same way.

Purpose: These were intentionally deferred during UAT as self-contained enhancements with no architectural dependencies.
Output: Updated PairsPage, DoctorReportPage, reportGenerator service, and i18n files for both locales.
</objective>

<execution_context>
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/little-words/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/little-words/.planning/STATE.md
@C:/my-code/vibe-coding/little-words/src/pages/PairsPage.tsx
@C:/my-code/vibe-coding/little-words/src/pages/DoctorReportPage.tsx
@C:/my-code/vibe-coding/little-words/src/features/doctor-report/services/reportGenerator.ts
@C:/my-code/vibe-coding/little-words/src/i18n/locales/en/common.json
@C:/my-code/vibe-coding/little-words/src/i18n/locales/pl/common.json
@C:/my-code/vibe-coding/little-words/src/components/ui/table.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: PairsPage — table layout and CSV export</name>
  <files>
    src/pages/PairsPage.tsx,
    src/i18n/locales/en/common.json,
    src/i18n/locales/pl/common.json
  </files>
  <action>
Replace the Collapsible card list in PairsPage with a Shadcn Table layout. Add a CSV download
button in the page header.

Import from @/components/ui/table: Table, TableHeader, TableBody, TableRow, TableHead, TableCell.
Import Badge from @/components/ui/badge (already imported).
Remove the Collapsible, CollapsibleTrigger, CollapsibleContent imports — they are no longer needed.

Table columns (5), using existing i18n keys for headers:
  - t('wordForm.text') — the word form text, rendered as a Button variant="link" that navigates to /word-forms/:wordFormId
  - t('meaning.text') — the meaning text, rendered as a Button variant="link" that navigates to /meanings/:meaningId
  - t('pair.firstObserved') — pair.firstObservationDate (ISO date string, display as-is)
  - t('pair.lastUsed') — pair.lastUsedDate (ISO date string, display as-is)
  - t('pair.active') — Badge: variant="default" with text t('pair.active') when isActive is true; variant="secondary" with text t('wordForm.inactive') when isActive is false

Wrap the Table in a div with overflow-x-auto so it scrolls horizontally on narrow viewports.

Sort control: keep the existing select at the top right (no changes to sort logic).

Download CSV button: place it to the left of the sort select in the header row. Use Button variant="outline" size="sm" with label t('pairs.downloadCsv'). The onClick handler must:
  1. Build a header row by joining the five column header strings (translated) with commas.
  2. Build one data row per entry in `sorted`. Wrap wordFormText and meaningText in double-quotes to handle commas in values. Use the ISO date strings directly. For the active column use the translated active/inactive string.
  3. Prepend a UTF-8 BOM (\uFEFF) to the full CSV string before creating the Blob so Excel opens it without encoding issues.
  4. Create a Blob with type 'text/csv;charset=utf-8;', call URL.createObjectURL, create an anchor element, set href and download='little-words-pairs.csv', call .click(), then call URL.revokeObjectURL immediately.

Add to src/i18n/locales/en/common.json under the "pairs" key:
  "downloadCsv": "Download CSV"

Add to src/i18n/locales/pl/common.json under the "pairs" key:
  "downloadCsv": "Pobierz CSV"
  </action>
  <verify>
    <automated>npm --prefix C:/my-code/vibe-coding/little-words run build 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    PairsPage renders a 5-column table (not Collapsible cards). A Download CSV button is visible in the header. npm run build exits 0 with no TypeScript errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Doctor Report — configurable recent additions and forgotten limits</name>
  <files>
    src/features/doctor-report/services/reportGenerator.ts,
    src/pages/DoctorReportPage.tsx,
    src/i18n/locales/en/common.json,
    src/i18n/locales/pl/common.json
  </files>
  <behavior>
    - generateReport({..., recentAdditionsLimit: 3}) slices activeMeanings to 3 entries in the recentAdditions section
    - generateReport({..., recentForgottenLimit: 2}) slices inactiveMeanings to 2 entries in the recentForgotten section
    - generateReport({...}) (no limit params) defaults both to 5, matching the prior hardcoded behavior
    - The section heading passed to bulletSection uses t('report.recentAdditions', { count: limit }) so the count appears in the report text
  </behavior>
  <action>
Update src/features/doctor-report/services/reportGenerator.ts:

Add two optional fields to the ReportInput interface:
  recentAdditionsLimit?: number
  recentForgottenLimit?: number

In generateReport, destructure them with default 5:
  const recentAdditionsLimit = input.recentAdditionsLimit ?? 5
  const recentForgottenLimit = input.recentForgottenLimit ?? 5

Replace the two hardcoded .slice(0, 5) calls:
  - recentAdditionsLines: change .slice(0, 5) to .slice(0, recentAdditionsLimit); change the bulletSection heading to t('report.recentAdditions', { count: recentAdditionsLimit })
  - recentForgottenLines: change .slice(0, 5) to .slice(0, recentForgottenLimit); change the bulletSection heading to t('report.recentForgotten', { count: recentForgottenLimit })

Update src/pages/DoctorReportPage.tsx:

Add four state variables at the top of the component:
  - additionsInput (number, useState(5)) — tracks the number input value for recent additions
  - forgottenInput (number, useState(5)) — tracks the number input value for recently forgotten
  - appliedAdditionsLimit (number, useState(5)) — the value actually used for report generation
  - appliedForgottenLimit (number, useState(5)) — the value actually used for report generation

Pass appliedAdditionsLimit and appliedForgottenLimit to generateReport:
  recentAdditionsLimit: appliedAdditionsLimit,
  recentForgottenLimit: appliedForgottenLimit,

Add a controls section between the parentNotes textarea and the report pre block. It should contain:
  - A label + input pair for the additions limit: label text t('report.recentAdditionsLabel'), input type="number" min="1" max="50" value={additionsInput} onChange sets additionsInput (parseInt, clamp to 1..50 with Math.max(1, Math.min(50, v)))
  - A label + input pair for the forgotten limit: label text t('report.recentForgottenLabel'), same constraints
  - A Button variant="outline" className="w-full" onClick={() => { setAppliedAdditionsLimit(additionsInput); setAppliedForgottenLimit(forgottenInput) }} with text t('report.regenerate')
  - Style each label+input pair with a wrapping div className="flex items-center gap-2 text-sm" and the input at className="w-20 rounded-md border p-1 text-sm"
  - Wrap the two pairs and the button in a div with className="mb-4 flex flex-col gap-3"

Update i18n keys in src/i18n/locales/en/common.json:
  Under "report":
    "recentAdditions": "Recently added ({{count}})"    [was: "Recently added (5)"]
    "recentForgotten": "Recently forgotten ({{count}})"  [was: "Recently forgotten (5)"]
    "recentAdditionsLabel": "Recent additions to show"
    "recentForgottenLabel": "Recently forgotten to show"
    "regenerate": "Regenerate"

Update i18n keys in src/i18n/locales/pl/common.json:
  Under "report":
    "recentAdditions": "Ostatnio dodane ({{count}})"    [was: "Ostatnio dodane (5)"]
    "recentForgotten": "Ostatnio zapomniane ({{count}})"  [was: "Ostatnio zapomniane (5)"]
    "recentAdditionsLabel": "Liczba ostatnio dodanych"
    "recentForgottenLabel": "Liczba ostatnio zapomnianych"
    "regenerate": "Odśwież raport"
  </action>
  <verify>
    <automated>npm --prefix C:/my-code/vibe-coding/little-words run test 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    generateReport respects recentAdditionsLimit and recentForgottenLimit parameters with default of 5. DoctorReportPage shows two labeled number inputs and a Regenerate button. Clicking Regenerate updates the report text with the new limits. npm run test exits 0, npm run build exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → CSV download | CSV content is derived from in-device IndexedDB data; no external data flows in or out |
| user → number input | User-supplied limit values; must be clamped before use to prevent degenerate rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-gm6-01 | Tampering | number input (limit fields) | low | mitigate | Clamp input to 1..50 range using Math.max/Math.min before applying to state; prevents blank or negative slice counts |
| T-gm6-02 | Information Disclosure | CSV download | low | accept | CSV contains only data the user themselves entered; file stays on device; no server involved |
| T-gm6-03 | Denial of Service | CSV Blob URL leak | low | mitigate | URL.revokeObjectURL called immediately after anchor.click() to release the Blob memory |
</threat_model>

<verification>
After both tasks are complete:

1. npm run build exits 0 — TypeScript reports no errors across all modified files
2. npm run lint exits 0 — no new ESLint violations
3. npm run test exits 0 — existing tests still pass (reportGenerator unit tests if any)
4. PairsPage: navigate to /#/pairs; confirm the table renders with 5 column headers, each data row shows a word form link, meaning link, two dates, and an active badge; Download CSV button is present and on click triggers a file download named little-words-pairs.csv
5. DoctorReportPage: navigate to the Doctor Report; confirm two labeled number inputs defaulting to 5 are visible above the report; change one value and click Regenerate; confirm the report section heading count changes accordingly
</verification>

<success_criteria>
- PairsPage renders all pairs in a 5-column sortable table, not Collapsible cards
- Download CSV on PairsPage produces a valid CSV file with a UTF-8 BOM and all sorted rows
- DoctorReportPage accepts user-configurable limits (1..50) for recently added and recently forgotten sections
- Clicking Regenerate applies the input values and updates the visible report text
- All new strings are translated in both en and pl
- npm run build, npm run lint, npm run test all exit 0
</success_criteria>

<output>
Create .planning/quick/260914-gm6-implement-three-deferred-follow-ups-from/260914-gm6-SUMMARY.md when done
</output>
